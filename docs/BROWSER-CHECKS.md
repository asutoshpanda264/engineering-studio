# Pending Browser Checks

**Workshop canvas (+ LLD editor, Failure Demo) now themeable — was hardcoded dark regardless of site theme** — new
- `ThemeProvider.tsx`: `colorMode` was a literal `"dark"` always (comment: "React Flow canvases haven't been redesigned for a light ground yet"). Turned out every canvas consumer (`ComponentNode`, `AnimatedEdge`, `ClassNode`, `RelationshipEdge`, `ArchitectureCanvas`/`DiagramCanvas`'s `Background`/`Controls`) was already fully token-driven with zero hardcoded-dark literals — the redesign work this comment warned about had already happened, just never wired up. `colorMode` is now `theme === "light" ? "light" : "dark"` (night-ops still maps to react-flow's "dark", it's not a third canvas mode react-flow needs). This alone fixed `/workshop`, `/tutorial`, `/lld/editor`, and the entity-page failure demos — react-flow's own `.react-flow.dark` class was painting an opaque `#141414` background over our own token-driven radial-gradient layer underneath; dropping that class in light theme let the real gradient (`var(--color-bg)`→`var(--color-bg-panel)`) show through as originally intended.
- `ComponentNode.tsx`: direct follow-up feedback once the canvas itself was visible in light — the node card's `bg-bg-panel` sat almost on top of the canvas's own panel-tinted background gradient (Paper's panel #eef4ff vs. bg #f4f7fc are nearly the same color), reported back as "hard to see." Card background and its two inset strips (routing-distribution bar, utilization bar) now swap which neutral tier they use per theme (light: white `bg-elevated` card / `bg-panel` insets; dark: unchanged `bg-panel` card / `bg-elevated` insets — dark already had good contrast, not touched). Icon chip also went from a flat `text-text-muted` gray to a real accent color per the entity's sidebar category (Core/Modules/Agentic → azure/retrieve/embed via `trackAccent.ts`'s existing 5-hue table, reused rather than hand-tuning ~20 per-entity colors), and the card's dead `shadow-elevated` (always `none` in every theme) became a real `--shadow-workspace-card` in light (still `none` in dark, unchanged).
- Live-checked in Chrome, both themes: `/workshop` — added a Core (Client), Modules (Load Balancer), and Agentic (LLM Call) node in light, confirmed white cards with blue/teal/purple icon chips and a visible shadow separating them from the canvas; confirmed the exact same graph in dark still reads as before (panel-gray cards, colorful icon chips as a bonus, no regression). Header/sidebar/inspector panels were already correctly light before this pass (double-checked via computed styles after an initial screenshot misread). `tsc --noEmit`, `npm run lint`, and full `vitest run` (1537 tests) pass clean (the two `ChallengeBriefing.tsx` errors are pre-existing/unrelated).
- **Not yet done**: `/lld/editor`'s `ClassNode.tsx` has the identical "card blends into canvas" issue in light (same `bg-bg-panel` card language, explicitly documented as a sibling of `ComponentNode`) — not fixed in this pass since it wasn't named, flagged back to the user. Not yet live-checked: actually connecting two nodes with an edge in light theme (attempted via automated drag, couldn't reliably hit the small connection-handle hit target — the edge/arrow color is confirmed token-driven from source, just not eyeballed live), and `/lld/editor`/failure-demo canvases in light theme specifically (only `/workshop` was clicked through with real nodes).

**Platform-wide light-mode retheme — Paper's tokens promoted from the old warm/copper palette to the `/learn`+`/foundations` blue-accent one** — new
- `globals.css`: `:root[data-theme="light"]`'s core tokens (`--color-bg/-elevated/-panel/-hover`, `--color-border(-hover)`, `--color-text(-muted/-subtle)`, `--color-signal(-hover/-active)`, `--color-status-*`) replaced with the exact values `/learn`/`/foundations` were already using; `--landing-shadow-card/-hover` retinted cool-neutral (was warm rgb(30 27 20), now rgb(17 24 39)) and `--landing-glow`'s first blob moved from copper (hue 28) to the new signal blue (hue 221) to match. New global `--color-signal-soft` token added (dark/night-ops/light all defined) for any surface wanting a translucent accent wash. The `--color-workspace-*`/`--radius-workspace*`/`--shadow-workspace-*` layer `/learn`/`/foundations` already used is now a pure alias (to plain tokens and to `--landing-*`) rather than a second hand-maintained palette — no visual change there, pure cleanup.
- Live-checked in Chrome (light theme): landing page (hero, live-architecture diagram card, "How it works," problem cards — all correctly blue/white/cool-gray now, `.landing-reveal` scroll-fade unaffected), `/entities`, `/problems`, `/interview-questions`, `/lld` index — all inherited the new palette for free via existing `Card.tsx`/`Badge`/`LinkButton`/`.reading-room-card` light-theme styling, no code changes needed in any of those files. `tsc --noEmit`, `npm run lint`, and full `vitest run` (1537 tests) pass clean (the two `ChallengeBriefing.tsx` errors are pre-existing/unrelated).
- **Found, not fixed**: `/workshop`, `/tutorial`, and `/lld/editor`'s React Flow canvas (the actual node/edge diagramming surface — header, sidebar, and inspector panels around it are already correctly light) stays hard-black regardless of theme. This is a pre-existing, deliberate, documented decision (`ThemeProvider.tsx`'s `colorMode: "dark"`, comment: "React Flow canvases... haven't been redesigned for a light ground yet") — not a regression from this pass, but the one place the "entire platform" ask doesn't yet reach. Making it theme-aware would need real design work on `ComponentNode`/`AnimatedEdge`/minimap colors for a white ground, not just flipping the `colorMode` flag — out of scope for this pass, flagged back to the user.
- Not yet live-checked: `/agentic`, `/case-studies` index pages and any reading-room `[slug]` detail page (very likely fine — same shared components as `/lld`/`/entities`, just not individually clicked through), and hover/mobile-width states platform-wide (still only checked at ~1516px desktop).

**`/learn` + `/foundations` light-mode "workspace" redesign — new blue-accent palette, hero, dashboard row, per-lesson current state** — new
- New scoped `--color-workspace-*`/`--radius-workspace*`/`--shadow-workspace-*` tokens (globals.css), consumed by a rebuilt `/learn` (`LearnHubView.tsx`, new) and `/foundations`' light-only `FoundationsJourney.tsx` (dashboard row, milestone strip, per-lesson `current` ring state, Workshop callout). Live-checked directly in-browser (Chrome, both themes, `/learn` and `/foundations`): hero CTA correctly reads "Continue: `<next lesson>`"/"Start with Foundations" off real `localStorage` progress, primary CTA and header background render the new blue/`#F4F7FC` palette in light theme, dark theme (`/learn`'s plain layout, `/foundations`' `FoundationsAtlas`) is pixel-unchanged, and the `/foundations` dashboard (Continue card, Progress card + bar, milestone strip, current-lesson ring, Workshop callout) all render correctly with real progress data. `tsc --noEmit`, `npm run lint`, and full `vitest run` (1537 tests) all pass clean (the two `ChallengeBriefing.tsx` errors are pre-existing/unrelated).
- Not yet live-checked: hover/lift states on the new room and lesson cards (mouse hover wasn't exercised, only click/scroll), and the whole redesign at mobile/tablet widths (`sm`/`lg` breakpoint reflow on the room grid, the dashboard row's `lg:flex-row` collapse, the milestone strip's `overflow-x-auto` on narrow screens) — only checked at a ~1516px desktop viewport.

**`ReadingRoomBadge` (Agentic/LLD/Case Studies' zone/track badge) — animation reworked twice: "pop in pop out", then "you stopped the animation"** — new
- First report: the badge's entrance (copied from `TrackBadge`'s `popIn` variant — spring scale+opacity, `repeat: Infinity, repeatType: "mirror"`) kept shrinking back to invisible and popping back out on a timer for as long as it stayed in view. `TrackBadge` does the same mirrored-loop thing but its own doc comment documents *why* that reads fine there (a bolt genuinely flickers, a gauge needle sweeps back and forth) — a plain reused lucide icon has no such theme to justify it, so the same mechanism here just looked like a glitch. `TrackBadge`/Foundations itself is untouched both times (that oscillation is an explicit, already-tuned design choice there, not a bug).
- First fix went too far the other way: a one-shot entrance pop plus a small, delayed (`repeatDelay`) breathe read as the animation having stopped entirely. Second fix: still one settling spring pop on first scroll into view, but the ongoing motion is now a genuinely continuous scale-and-tilt drift (`scale: [1, 1.16, 1, 1.08, 1]`, `rotate: [0, -6, 0, 6, 0]`, 3.2s, `easeInOut`, **no** `repeatDelay`) — never coasts at a fixed frame between cycles, and never dips anywhere near the entrance's hidden/invisible values, so it reads as one smooth unbroken loop rather than either a pop cycle or a static icon.
- `tsc --noEmit` and `npm run lint` pass clean (the two `ChallengeBriefing.tsx` errors are pre-existing/unrelated); no test coverage on this component specifically.
- Not yet live-checked: `/agentic`, `/lld`, `/case-studies` in dark theme (Atlas) and Paper (Journey) — scroll a zone/track title into view and confirm the badge pops in once, then keeps drifting continuously (a gentle scale+tilt wobble, no pauses, no shrink-to-nothing) for as long as it's on screen. Scroll back up past it and confirm it fades out smoothly rather than snapping.

**`ReadingRoomAtlas`/`ReadingRoomJourney` (Agentic/LLD/Case Studies) brought up to parity with Foundations' latest dark/Paper treatment — was "half implemented"** — new
- Reported back directly: dark theme on `/agentic`, `/lld`, `/case-studies` still showed a persistent/frozen background, faded watermark-icon "logos," and no animated logos, unlike `/foundations`. Root cause: the generic `ReadingRoomAtlas`/`ReadingRoomJourney` components those three reading rooms share had never been updated to match fixes already applied to Foundations' own bespoke `FoundationsAtlas`/`FoundationsJourney`:
  - `ReadingRoomAtlas`'s `AtlasBackdrop` still painted `SystemMeshBackground` on its own `fixed inset-0` layer — the exact "stays frozen while the page scrolls" bug Foundations hit and fixed by moving the mesh out to `*IndexView`'s `<main>` as a normal-flow `absolute` layer instead. `AgenticIndexView`/`LLDIndexView`/`CaseStudiesIndexView` now render that same `SystemMeshBackground` layer inline (they'd already picked up the `bg-blueprint-grid` half of that fix from the theme-driven-view change below, but not the mesh half) — this is "the background is still persistent."
  - `AtlasZone` still drew the old oversized, 7%-opacity watermark icon bleeding off each zone's corner — the exact look Foundations replaced with the small animated `TrackBadge` next to the zone title, flagged back there as "barely visible and not worth the space." New generic `ReadingRoomBadge` (`src/components/readingRoom/ReadingRoomBadge.tsx`) does the same job for the generic reading rooms — same pop-in-on-scroll mechanism as `TrackBadge`, reusing the group's own lucide icon instead of `TrackBadge`'s five Foundations-themed hand-drawn variants. This is "the faded logos are still there."
  - Neither `AtlasZone` nor `ReadingRoomJourney`'s `TrackSection` had `FoundationsAtlas`/`FoundationsJourney`'s scroll-into-view reveal animation (`sectionRevealVariants`, replays on every re-entry) — both are now `motion.section`/`motion.div` with the same variants. This plus the badge above is "there is no animation logos."
  - `ReadingRoomJourney`'s `TrackSection` panel also picked up the two-layer elevation shadow `FoundationsJourney`'s own panel got in an earlier fix (was still on the old barely-visible 1px/2px contact shadow), and its header now shows the same badge Paper's `FoundationsJourney` shows (Paper-only, matching that file's own `accent &&` gate).
  - `ReadingRoomAtlas`'s `AtlasBackdrop` blob opacities also corrected to match `FoundationsAtlas`'s exactly (0.1/0.08, were 0.06/0.05) — its doc comment already claimed to mirror Foundations exactly; now it actually does.
- `tsc --noEmit`, `npm run lint`, and full `vitest run` (1537 tests) all pass clean (the two `ChallengeBriefing.tsx` errors are pre-existing/unrelated).
- Not yet live-checked: `/agentic`, `/lld`, `/case-studies` in dark theme — confirm the mesh graph now scrolls up and out of view with the hero (not frozen in place), each zone shows a small animated icon badge next to its title instead of a giant faded watermark in the corner, and zones/track sections fade/slide in as they scroll into view (replaying on scroll-back-up too). Also check Paper on all three: track sections should show the same badge + a visibly raised elevation shadow on each panel. Compare directly against `/foundations` (Atlas in dark, Journey in Paper) for a side-by-side look.

**Theme-driven Journey/Atlas rolled out to every reading room: `/agentic`, `/lld`, `/case-studies`** — new
- Same change just made for `/foundations` (previous entry above), translated to the other three reading rooms that share the identical Journey/Atlas toggle pattern (`AgenticIndexView.tsx`, `LLDIndexView.tsx`, `CaseStudiesIndexView.tsx`) — `/entities` has no such toggle and is unaffected; night-ops is unaffected on all of them (never reached this branch). Each file's `const [view, setView] = useState<ReadingRoomView>("atlas")` + `<ViewToggle .../>` in the hero replaced with a plain derived `const view: ReadingRoomView = theme === "light" ? "journey" : "atlas"` — Paper always shows `ReadingRoomJourney`, dark always shows `ReadingRoomAtlas`, no manual control left to switch.
- The shared `src/components/readingRoom/ViewToggle.tsx` component itself is now dead (only its `ReadingRoomView` type is still imported, by all four `*IndexView` files) — removed the component function, kept the type with a doc comment explaining why.
- `tsc --noEmit`, `npm run lint`, and full `vitest run` (1537 tests) all pass clean (the two `ChallengeBriefing.tsx` errors `tsc` reports are pre-existing/unrelated).
- Not yet live-checked: `/agentic`, `/lld`, `/case-studies` — toggle the theme (via `ThemeToggle` in the header) between Paper and dark on each and confirm the page body switches automatically (Paper -> Journey's tracked list, dark -> Atlas's immersive zone-bus layout), with no visible toggle control left in the hero section; confirm night-ops still shows each one's own arcade map, untouched. Also spot-check `/foundations` again alongside these three, since it went through the identical change first.

**`/foundations`: Journey/Atlas is now tied to theme, not a manual toggle — Paper gets Journey, dark gets Atlas** — new
- `FoundationsIndexView.tsx`: removed the manual `ViewToggle` segmented control (the file-local one in this component, not the shared `@/components/readingRoom/ViewToggle` other reading rooms use — that's untouched) and its `useState`. `view` is now a plain derived value: `theme === "light" ? "journey" : "atlas"`. Per explicit feedback once both had been live side-by-side long enough to pick a winner per theme — Atlas's environment (fixed copper/status-healthy backdrop glow, `TrackBadge`'s icon-glow chip, blurred corner blobs) is tuned for dark's near-black ground; Journey's flatter bordered-panel treatment (now with a real elevation shadow) reads better against Paper's near-white ground. Night-ops is unaffected — it never reached the Journey/Atlas branch in the first place (`FoundationsMap` instead).
- `tsc --noEmit`, `npm run lint`, and full `vitest run` (1537 tests) all pass clean (the two `ChallengeBriefing.tsx` errors `tsc` reports are pre-existing/unrelated).
- Not yet live-checked: `/foundations` — toggle the theme (via `ThemeToggle` in the header) between Paper and dark and confirm the page body switches automatically: Paper always shows Journey (tracked list, readout strip, Continue band), dark always shows Atlas (the immersive backdrop/zone-bus layout) — with no visible toggle control left in the hero section to switch manually. Confirm night-ops still shows the arcade map, untouched.

**`/foundations` Journey: the big track panel gained a real elevation shadow in Paper (was clarifying the previous icon-shadow request)** — new
- `FoundationsJourney.tsx` (`TrackSection`): the outer bordered panel that holds a whole track's lesson grid — not the per-lesson icon chips from the previous change — had only a barely-visible `shadow-[0_1px_2px_rgba(15,23,42,0.04)]` contact shadow (same minimal value `AtlasZone`'s equivalent panel still uses, untouched here per this request being Journey-only). Replaced with a two-layer shadow (`0 2px 4px` contact + `0 16px 32px -16px` soft lift, plain neutral gray, not the track's own hue — this is a whole content block, not a per-track logo) so the panel actually reads as raised off the page. Paper only (`accent` is already the light-mode gate for this whole conditional branch); dark/night-ops unchanged.
- `tsc --noEmit`, `npm run lint`, and full `vitest run` (1537 tests) all pass clean (the two `ChallengeBriefing.tsx` errors `tsc` reports are pre-existing/unrelated).
- Not yet live-checked: `/foundations` Journey view in Paper (light theme) — each track's outer panel (the bordered box containing that track's lesson cards) should now show a clear, soft elevation shadow beneath it, not the near-invisible hairline shadow from before; confirm it doesn't look muddy/too heavy at any of the 5 tracks' widths; switch to dark/night-ops and confirm the panel looks exactly as before (still borderless/flat there).

**`/foundations`: Paper's icon-chip boxes gained an elevation shadow (were flat)** — new
- `trackAccent.ts`: new `iconShadow` field on `TrackAccentClasses` — Paper's counterpart to dark's `iconGlow`, but a crisp, tight two-layer drop shadow (`0 1px 2px` contact + `0 8px 16px -6px` lift, both in the track's own hue at low opacity) rather than a diffuse blurred halo — that specific look is still correctly avoided for Paper (see `iconGlow`'s own comment: reads as "a smudge" on near-white). Populated per-track for all 5 `TRACK_ACCENTS` entries; empty string in every `TRACK_ACCENTS_DARK` entry (a gray-ish shadow is invisible on a near-black chip, and `iconGlow` already gives dark its depth cue there).
- Wired into all three places that render one of these tinted icon-chip boxes: `FoundationsAtlas.tsx`'s `AtlasLessonNode` (completed lessons) and `TrackBadge`'s own chip (both themes — `iconGlow`/`iconShadow` are mutually exclusive per table, so exactly one renders), and `FoundationsJourney.tsx`'s `LessonCard` (completed lessons, Paper only). In every case the shadow only applies where the tinted background box itself applies (completed lessons only for the two lesson cards; always for `TrackBadge`, which stays a box regardless of completion).
- `tsc --noEmit`, `npm run lint`, and full `vitest run` (1537 tests) all pass clean (the two `ChallengeBriefing.tsx` errors `tsc` reports are pre-existing/unrelated).
- Not yet live-checked: `/foundations` in Paper (light theme) — completed lesson cards in both Atlas and Journey should show their icon chip with a subtle, crisp elevation shadow (not a soft blurred glow) instead of sitting flat; each track's animated `TrackBadge` logo (both views now, per the Journey addition) should show the same kind of shadow under its chip; confirm the shadow's color roughly matches that track's own hue rather than reading as generic gray; switch to dark/night-ops and confirm nothing changed there (still just the existing `iconGlow` halo).

**`/foundations` Journey: brought Atlas's animated per-track badge over, Paper only** — new
- `FoundationsJourney.tsx` (`TrackSection`): each track header now renders `TrackBadge` (the small SVG logo that plays its themed scroll-triggered animation — bolt drawing in, database layers dropping, etc.) to the left of the "Track NN" chip and title, same layout Atlas already uses. Gated on `accent` being non-null, which is itself Paper-only (`TRACK_ACCENTS`) — dark/night-ops Journey is unchanged, since Journey still has no dark-theme accent table the way Atlas does (`TRACK_ACCENTS_DARK`), and this wasn't asked to change that, only to bring the badge into Paper.
- `tsc --noEmit`, `npm run lint`, and full `vitest run` (1537 tests) all pass clean (the two `ChallengeBriefing.tsx` errors `tsc` reports are pre-existing/unrelated).
- Not yet live-checked: `/foundations` Journey view in Paper (light theme) — confirm each of the 5 track headers now shows the animated badge (electric/database/gauge/network/checklist per track, matching Atlas's own assignment) playing its intro as it scrolls into view, positioned cleanly next to the "Track NN" chip without crowding the title on narrow widths; switch to dark or night-ops and confirm Journey's track headers look exactly as before (no badge, nothing changed).

**`/foundations` Atlas: fixed floating disconnected tick on row-2+ lesson cards** — new
- `FoundationsAtlas.tsx` (`AtlasZone`/`AtlasLessonNode`): the per-lesson connecting tick (a short vertical hairline meant to hang each card off the zone's horizontal "bus" spine) only actually has a spine to reach when the card is in the grid's *first visual row* — the spine is drawn once, at the very top of the whole (possibly multi-row) grid. Any lesson wrapped onto row 2+ still rendered a tick anyway, floating in the row gap connected to nothing above it — reported via screenshot as looking like the card was "flying." New `getTickVisibilityClass(rowIndex)` hides the tick outright once a lesson's index puts it past row 1 for every breakpoint the grid supports (2/3/4 columns at `sm`/`lg`/`xl`); each `AtlasLessonNode` now takes a `rowIndex` prop (its position in `lessons.map`) to compute this. Known, accepted limitation: doesn't account for the "current" lesson's own `sm:col-span-2` shifting later cards' actual grid position — noted in the code as a rare, cosmetic-enough edge case rather than replicating full grid auto-placement math.
- `tsc --noEmit`, `npm run lint`, and full `vitest run` (1537 tests) all pass clean (the two `ChallengeBriefing.tsx` errors `tsc` reports are pre-existing/unrelated).
- Not yet live-checked: `/foundations` Atlas view, a track with more lessons than fit in one row at the current viewport width (e.g. narrow the window to `sm` width with a 3+ lesson track) — confirm row-1 cards still show their connecting tick up to the spine, and row-2+ cards show *no* tick at all (not a floating disconnected one). Resize across the `sm`/`lg`/`xl` breakpoints and confirm the tick appears/disappears correctly as cards move between row 1 and row 2 at each width. Specifically check a track that contains the "current"/next-up lesson (the one with `sm:col-span-2`) for the known edge case above — see if a stray tick shows up on whichever card ends up shifted by the wider current card.

**`/foundations` lesson cards: dropped the green "completed" tint, moved the icon glow/box from upcoming to completed-only** — new
- `FoundationsAtlas.tsx` (`AtlasLessonNode`): the icon no longer turns plain green (`text-status-healthy/70`) on completion — it now keeps the same track-accent color every other card uses. The tinted chip *background* (`iconAccent.soft`) and its `iconGlow` halo (dark theme only) both moved from "every not-yet-done card" to "only completed" — an upcoming chapter is now a bare colored icon with no box/glow behind it; a finished one gets the filled, glowing chip. Previously it was the reverse (every upcoming chapter had the box+glow, completed ones didn't). The small tick mark above each card also dropped its green-on-completed branch (now just accent/border regardless of completion). The "current"/"you are here" card's own distinct box-shadow glow (`glow`/`accent.glow`) is untouched — that's a separate navigational cue, not a completion indicator, and `current` is never `completed` so the two never overlap; flag if this one should also lose its glow.
- `FoundationsJourney.tsx` (`LessonCard`/`LessonCardCorners`): same green-icon-on-completion removed (icon is always the track-accent color); same box-only-on-completed change (upcoming = bare colored icon, completed = filled chip); the corner-bracket "reticle" no longer gets a persistent green tint when done either (was `border-status-healthy/60` at rest, not just on hover) — corners now look identical regardless of completion, with the `Badge`'s "Done" text/dot carrying that signal alone.
- `tsc --noEmit`, `npm run lint`, and full `vitest run` (1537 tests) all pass clean (the two `ChallengeBriefing.tsx` errors `tsc` reports are pre-existing/unrelated).
- Not yet live-checked: `/foundations` in dark theme, Atlas view — confirm an *upcoming* (not-yet-done) card's icon is a bare colored glyph with no background box and no glow; mark a lesson complete and confirm it now gets both a filled tinted chip *and* a colored glow halo; confirm the "current"/next-up card still shows its own distinct shadow-glow as before, separate from this. Journey view — same upcoming-vs-completed box difference (Journey has no glow, box only); corner brackets look identical regardless of completion. Paper (light) theme on both views — confirm the upcoming icon's plain colored glyph (no box) still reads clearly against the card background.

**`/foundations` scroll-reveal simplified: dropped spine/parallax/pulse, replays every scroll pass** — new
- Cut back down to one effect per explicit feedback ("keep the reveal"): removed the 6-way `MotionModeSelector`/`motionPreference.ts` mode-selector infrastructure, the spine/rail draw-in, `AtlasBackdrop`'s blob parallax, and the current-lesson pulsing halo entirely (deleted `motionPreference.tsx`, `MotionModeSelector.tsx`). `AtlasBackdrop` and the Journey rail are back to their original always-static markup.
- What remains: `AtlasZone` (`FoundationsAtlas.tsx`), `TrackSection` (`FoundationsJourney.tsx`), and `TrackBadge`'s own draw-in all fade/slide-up via `whileInView`, each gated on plain `useReducedMotion()` (no custom preference/toggle — OS-level only, same as before this feature existed).
- Changed behavior per this request: every one of those now uses `viewport={{ once: false }}` (was `once: true`) — the reveal replays every time a section re-enters the viewport, in *either* scroll direction, instead of firing once and staying spent. `TrackBadge` (a separate session, in parallel) took this further and made its own per-track badge animation loop continuously (`repeat: Infinity, repeatType: "mirror"`) for as long as it's in view, resetting when scrolled away — not reverted, since it's consistent with "replay on every pass" and adds real character (the gauge needle actually sweeps back and forth).
- `tsc --noEmit`, `npm run lint`, and full `vitest run` (1537 tests) all pass clean (the two `ChallengeBriefing.tsx` errors `tsc` reports are pre-existing/unrelated).
- Not yet live-checked: `/foundations` default theme, both views — scroll down past a track section, then scroll back up past it again, and confirm the fade/slide-in replays both times (down and up), not just the first; confirm there's no jank/flicker if you scroll back and forth quickly across a section boundary; confirm Atlas's `TrackBadge` icon animations (bolt/database/gauge/network/checklist) now loop while the badge stays in view rather than playing once and freezing, and reset cleanly if scrolled away mid-loop; confirm nothing from the removed spine/parallax/pulse effects is still visible anywhere; with OS-level "reduce motion" on, confirm everything renders in its static resting frame with no animation at all.

**`/foundations` widened (was leaving large dead margins on wide screens)** — new
- `FoundationsAtlas.tsx` and `FoundationsJourney.tsx`: the phase/readout strip, continue-band, and track-section containers went from `max-w-5xl` (1024px) to `max-w-7xl` (1280px); each lesson grid gained `xl:grid-cols-4` (was capped at 3 columns). The hero tagline in `FoundationsIndexView.tsx` stays `max-w-3xl` on purpose (unwidened, for prose readability). `tsc --noEmit` clean on both files (pre-existing, unrelated `ChallengeBriefing.tsx` errors aside).
- Not yet live-checked: `/foundations` on a wide (~1920px) viewport in both Journey and Atlas views — confirm the extra width reads as filled-in, not just wider gutters; confirm the lesson grid actually reaches 4 columns at `xl` and cards don't look cramped; confirm the "featured"/"current" card's `sm:col-span-2` still reads fine as half-width in a 4-col row; spot check narrower widths (~1024–1280px) didn't regress.

**Track 04's Atlas/Journey accent recolored (was reading as a second "current" marker)** — new
- `src/components/foundations/trackAccent.ts`: `TRACK_ACCENTS.generate`/`TRACK_ACCENTS_DARK.generate` (track index 3 — "Distributed Systems Patterns" on `/foundations`, and whatever the equivalent 4th track is on `/case-studies`, `/entities`, `/lld`, `/agentic`) moved off `--color-rag-generate` (hue 16) to a dedicated hue-95 olive literal — reported via screenshot: at the Atlas stepper's small badge size, hue 16's idle `borderFaint` blended against Paper's near-white `--color-bg` to almost the same peach as `--color-signal` (hue 28, the actual "current"/"you are here" color), so the *idle* track-04 badge looked just as "lit" as the real current-track badge next to it. No other track's hue changed. `tsc --noEmit` and lint pass clean (pre-existing, unrelated `ChallengeBriefing.tsx` errors aside).
- Not yet live-checked: `/foundations` in Paper (light theme) — track 04's stepper badge and lesson-card icon chip/corner-leak now read as a clearly distinct olive, not confusable with the orange "current" badge or the "you are here" pin; toggle through Paper/dark and confirm track 04 still looks intentional (not a jarring one-off) next to the other four track colors; spot-check `/case-studies`, `/entities`, `/lld`, `/agentic` for the same track-04 recolor since they share `TRACK_ACCENTS`.

**Default theme void recolor: warm charcoal → cool near-black (signal stays copper)** — new
- `globals.css` `:root` (default "Trace" theme, not Night Ops): `--color-bg`/`-elevated`/`-panel`, `--color-border`/`-hover`, and `--color-text`/`-muted`/`-subtle` moved from the warm hue-28/30/40° family to a cool hue-220/228/230° family (same lightness ladder — 5/8/11% for bg tiers, 93/66/44% for text). `--color-signal` and friends are unchanged (still copper, `hsl(28 85% 58%)`). Chosen live via a temporary "Palette Lab" tool (now removed) as the "cool blue-black void + copper signal" combination. Check across the app that the copper accent now reads as a clear, deliberate pop against the cooler background (that was the whole point) rather than blending in the way it did against the old warm bg, and that nothing (borders, panels, text hierarchy) feels low-contrast or off after the hue shift — spot-check `/workshop`, `/entities`, `/foundations`, the landing page, and Batman Mode's cave torch glow (`--color-signal`-keyed `box-shadow`) since its warm glow is now against a cooler backdrop.

**Six-item bug pass: Workshop canvas (fitView/palette overlap, duplicate node label, 4-way handles), shared `AppHeader`, header responsive floor, zero-radius cleanup** — new
- `/workshop`, `/tutorial`: `ArchitectureCanvas.tsx` no longer re-runs `fitView` on every node-count change (only on mount now) — adding components one at a time keeps the viewport where the user left it instead of re-centering on every add; `ComponentSidebar.tsx`'s click-add grid (`nextClickPosition`) now starts at `x: 340` instead of `120`, clearing the open palette panel's right edge (~300px) so the first click-added node doesn't land hidden behind it. `ComponentNode.tsx` dropped the redundant uppercase eyebrow line above the title (was always the same word as the title in two type treatments) — the icon alone now carries the entity type, via `title`/`sr-only` text. `ComponentNode.tsx` also gained Top/Bottom handles alongside the existing Left/Right (4 total), and `ArchitectureCanvas.tsx`'s `<ReactFlow>` now sets `connectionMode={ConnectionMode.Loose}` so any handle can be either end of a drag — together these let a connection to a node placed below-and-left (etc.) of its source take the short way instead of visibly looping around.
- New `src/components/layout/AppHeader.tsx` replaces 16 hand-rolled `<header>` copies (`/`, `/learn`, `/entities` + `[slug]`, `/foundations` + `[slug]`, `/agentic` + `[slug]`, `/case-studies` + `[slug]`, `/lld` + `[slug]`, `/problems` + `[id]/solution`, `/interview-questions` + `[id]`) with one shared shell (sticky, border, backdrop-blur, height, back-link treatment) — `WorkshopHeader.tsx`, `EditorShell.tsx`'s header, and `FailureDemoHeader.tsx` are deliberately untouched (real toolbars, not this "back link + nav" shape). `/learn`'s header was the one genuine outlier (`max-w-5xl` vs. every sibling reading-room index at `max-w-6xl` — a real ~130px jump navigating `/learn` → `/entities`) and is now `max-w-6xl` to match; every other page kept its existing max-width (narrower single-item pages and wider two-column article pages are legitimately different shapes, not part of the drift). All previously-non-sticky index headers (`z-20`, no `sticky`) are now `sticky top-0 z-10` like every other page, via the shared component.
- Landing page (`src/app/page.tsx`): the header's four nav links (Tutorial/Learn/Problems/Interviews) now collapse to icon-only below `xl` (1280px) via a new `NavLink` helper (`aria-label`/`title` keep the accessible name even with the text hidden) — this row previously needed ~820px with zero breakpoint classes to avoid overflowing. `WorkshopHeader.tsx` similarly collapses Tutorial/Problems/Learn/Clear/Reset to icon-only below `xl` — that row previously needed ~1335px, well past the project's own documented ~1024px responsive floor, with no breakpoints at all.
- `globals.css`'s "radius is zero throughout, no exceptions" rule: removed 68 dead `rounded-sm`/`rounded-md`/`rounded-lg` classes (already silently resolving to 0px via the tokenized `--radius-*` vars, so purely dead code, but real enough places someone assumed a rounded corner that never happened) across 29 files. Also fixed 12 genuine violations — `rounded-full` isn't tokenized to 0, so 4 progress-bar/meter widgets (`InspectorPanel.tsx` ×4 pairs, `CDNImpactComparison.tsx`, `FailureDemoInspector.tsx`) were actually rendering as real pills — now plain hairline bars, matching the pattern `ComponentNode.tsx`'s own utilization bar already used correctly. Left every literal circular glyph alone (status dots, legend/bullet dots, the Info-icon hit target, WeaponWheel, TorchToggle, quest map nodes, Bat Signal) — those are round by design (a dot, a wheel, a badge), not "a rounded corner on a rectangular container," which is what the rule is actually about.
- `tsc --noEmit`, `npm run lint`, full `vitest run` (1504 tests) all pass clean.
- Not yet live-checked: `/workshop` — add 3+ components by clicking the sidebar catalog and confirm none land hidden behind the open palette panel, and confirm the viewport doesn't jump/re-center after the first add; drag a connection from a node to one placed below-and-left (or above-and-right) of it and confirm it takes a direct path rather than looping around either node; every Workshop node card shows the icon + title once, not title twice. `/`, `/learn`, `/entities`, `/foundations`, `/agentic`, `/case-studies`, `/lld`, `/problems`, `/interview-questions` and their detail pages — header still looks and behaves identically (sticky, back-link, right-side actions) at normal widths, and `/learn`'s back-link now lines up with `/entities`'s instead of sitting ~130px left of it. Narrow the landing page and `/workshop` windows down toward ~1024px and confirm the nav labels/toolbar labels cleanly drop to icon-only (with working tooltips) rather than wrapping or overflowing. Spot-check a few of the de-radius'd elements (buttons, inputs, modals, badges) still look identical, and the four meter bars (Load Balancer/Cache/Rate Limiter panels, CDN latency comparison, failure-demo remedy bars) now read as flat hairline bars instead of pills.

**Three small fixes: scenario-card truncation, React Flow watermark, playback scrubber color** — new
- Landing page (`src/app/page.tsx`): scenario cards now run `scenario.story` through `truncateAtWord` (`src/lib/truncateText.ts`, `SCENARIO_STORY_MAX_CHARS = 150`) before the existing `line-clamp-3`, instead of relying on the CSS clamp's own mid-word cut (was producing fragments like "…and ord…" for "order"). `globals.css` gained a themed override for React Flow's attribution watermark (`.react-flow__attribution` — border/radius/mono font/muted color via the library's own `--xy-attribution-background-color` hook plus direct `!important` overrides, same pattern as the existing edge-path overrides above it; kept visible per XY Flow's license, just restyled) — affects both `/workshop` and `/lld/editor` since it's a global rule. And `PlaybackControls.tsx` + `FailureDemoPlaybackBar.tsx` both had `accent-primary` on their scrubber `<input type="range">` — a dead class (no `--color-primary` token exists in this theme; `accent-signal` is the real one, already used correctly elsewhere e.g. `InspectorPanel.tsx:307`), which is why the thumb/fill was rendering as the browser's native blue instead of the app's orange/yellow signal color. Both fixed to `accent-signal`. `tsc --noEmit`, `npm run lint`, full `vitest run` (1504 tests) all pass clean.
- Not yet live-checked: `/` — the 4 scenario cards at `lg:grid-cols-4` (narrowest case) end their story excerpt on a whole word, never mid-word, and still read as a natural-length excerpt (not jarringly short); `/workshop` and `/lld/editor` — the React Flow watermark bottom-right no longer looks like a stray unstyled browser default (bordered, muted mono text, still clickable/visible); `/workshop`'s playback scrubber (run a simulation first) — thumb and filled-track portion render in the app's signal color, not OS-default blue, in both light and dark and at both zoom levels; same check on the failure-demo's playback bar wherever that's reachable (`FailureDemoInspector`/`CompareModal` flow).

**Foundations map torch/cave-shroud reveal restored (Batman Mode)** — new
- Re-enabled a mechanic that existed before but was disabled (see `foundationsProgress.ts`, `MapNode.tsx`, `FoundationsMap.tsx`, `MapEdges.tsx` — the "LOCKING DISABLED"/"batman-only arcade maps" comments): `getLessonStatus` once again produces `"locked"` for a lesson whose prerequisites aren't all completed (24 lessons already carry real `prerequisites` data, only the root has none). Only affects `FoundationsMap` (the Batman-Mode-only arcade map) — `FoundationsList` (default theme's flat grid) never calls `getLessonStatus` and stays exactly as it was, "nothing here is locked." On the map: a locked lesson is now a plain, `aria-hidden`, non-interactive card hidden inside `CaveShroud`'s CSS-mask darkness rather than a normal node; its incoming prerequisite trail is *also* routed through that same `CaveShroud` (a second `<MapEdges>` call rendered inside the shroud, alongside the locked nodes) so a bit of trail lights up together with whatever card it leads to. Toggling the torch (`TorchToggle`, bottom-left) swaps the cursor for a flame glow (`TorchCursor`) and sweeps a spotlight through the mask (a `document`-level `pointermove` effect in `FoundationsMap.tsx` writes `--torch-x`/`--torch-y`/`--torch-radius` straight onto the shroud + repositions the cursor via `translate3d`, converting screen coordinates into the shroud's own unscaled space via `rect.width / MAP_WIDTH` so the beam stays a constant on-screen size at any zoom level) — letting a reader preview future chapters without being able to open them, and **only for as long as the beam is actually on them**: there's no click-to-pin/persist mechanic (an earlier pass had one — a locked node was a `<button>` toggling a `revealed` flag that survived the torch moving away; removed per explicit feedback that a trail should vanish the instant the torch leaves it, so visibility is now 100% mask-driven with zero extra state to fall out of sync). `PannableMapCanvas` gained an optional `hideCursor` prop (native cursor → `cursor-none` on the scroll container while torch is on). `tsc --noEmit`, `npm run lint`, and full `vitest run` (1504 tests) all pass clean — no dedicated tests were added for the restored gating/torch itself.
- Not yet live-checked (this is the part automated checks can't cover — it's real pointer/CSS-mask/zoom coordinate math): open `/foundations` in Batman Mode (toggle theme, since the map only renders in `night-ops`) and confirm most lessons past the first couple render as genuinely locked (dark, dimmed, no lock-icon card fully hidden by darkness) rather than fully lit; toggle the torch on (bottom-left control) and confirm the cursor becomes a flame-blue glow and a circular hole opens in the darkness following it, revealing locked node cards (title/icon/time) *and* a bit of their incoming trail line together as the cursor passes near them, at both 100% zoom and after zooming in/out (radius should look the same physical size on screen at any zoom, not shrink/grow); confirm panning (drag) still works normally with the torch on; sweep the torch onto a locked node and then away — confirm both the card and its trail go fully dark again immediately, with nothing left pinned/visible; toggle the torch off and confirm the native cursor and pan-grab cursor both return; refresh the page with the torch left on and confirm it's still on (persisted); switch back to the default theme and confirm `/foundations`'s flat grid is completely unaffected — no lock icons, every card still a plain clickable `<Link>`; no console errors.

**New `/case-studies` reading room (Pillar E Phase 1, entry 1 — RAG System)** — new
- Fifth reading room alongside `/foundations`, `/lld`, `/entities`, `/agentic`: `src/content/caseStudies/` (mirrors `agentic/`'s shape exactly), `/case-studies` + `/case-studies/[slug]` routes (same article shell as the other four, plus two new optional sections — "Builds on" cross-links and a "Build it" CTA into `/workshop?scenario=research-assistant`), a 5th `/learn` tile, and a Batman-Mode arcade map (`CaseStudiesMap`) reusing the existing shared map machinery. Only one entry ships so far (RAG System). `tsc --noEmit`, full `vitest run` (1504 tests, 10 new), and `npm run lint` all pass clean.
- Not yet live-checked: `/learn`'s new 5th card renders correctly in the grid (now 5 cards at `sm:grid-cols-2 lg:grid-cols-3`, so the last row has 2 rather than 3) and the hero's `ReadingRoomsDiagram` strip still reads fine narrowed to 5 items; `/case-studies` index in the default theme (card grid, single "Agentic Systems" category group since Phase 2 hasn't landed) and in Batman Mode (`CaseStudiesMap` — pan/zoom, single populated tier/region, decorative edge, `MarkCompleteButton` turning the node green); `/case-studies/rag-system` renders its animated `flow` diagram (Client → Retriever → LLM Call, the traveling-dot loop pulsing each step) correctly — swapped in after the original `architecture` block read as three bare boxes on an oversized empty canvas — the "Builds on" chip links to `/agentic/rag-architectures` actually resolve, the table/insight blocks with the 39%/85.1%/87.2% numbers render cleanly, and the "Build it" button actually deep-links to `/workshop?scenario=research-assistant` and loads that scenario; no console errors on any of the three new/changed pages.

**Arcade maps added to `/entities`, `/lld`, `/agentic` (Batman Mode only)** — new
- Same pattern the previous entry set up for `/foundations` (theme-gated `IndexView` + list/map split) now applied to the other three reading rooms: `EntitiesIndexView`/`LLDIndexView`/`AgenticIndexView` each render their existing grid (`EntitiesList`/`LLDList`/`AgenticList`, extracted unchanged from the old page bodies) in the default theme, and a new arcade map (`EntitiesMap`/`LLDMap`/`AgenticMap`, all under `src/components/maps/`) in `night-ops`. Shared map plumbing extracted out of `FoundationsMap` into `src/components/maps/`: `PannableMapCanvas` + `usePannableCanvas` (pan/drag/zoom/vignette/loading-state/zoom-controls, `FoundationsMap` itself refactored onto this with no behavior change), `MapRegions`/`MapEdges`/`MapLegend`/`MapZoomControls` (moved from `components/foundations/`, widened to take generic props instead of importing Foundations' own data), and a new `ArcadeNode` (simpler card for the 3 new maps — no locked state, `MapNode` stays Foundations' own richer version). New `src/lib/mapAutoLayout.ts` (`computeTieredMapLayout`, unit-tested) gives each new map a deterministic tier/lane layout (category or entity domain = column) instead of hand-placed positions. LLD and Agentic gained lightweight completion tracking (`lldProgress.ts`/`agenticProgress.ts`, via a new shared `createProgressStore` factory) + a `MarkCompleteButton` on each lesson page, feeding their map's green/completed node styling — Entities has no lesson content, so its map has no completed state. `LockInChapterAction` now also calls `markLLDLessonComplete` when advancing an `lld` chapter, keeping that map in sync with a lock-in run same as it already did for `foundations`. `tsc --noEmit`, full `vitest run` (1494 tests, 6 new), and `npm run lint` all pass clean.
- Not yet live-checked: toggling the theme on all three new routes actually swaps list ↔ map correctly; each new map's pan/zoom/region labels/tier layout look right and don't overlap awkwardly at the node counts involved (Entities ~23, LLD 15, Agentic 20); `LLDMap`/`AgenticMap`'s decorative course-order path renders sensibly across tiers; the new `MarkCompleteButton` on `/lld/[slug]` and `/agentic/[slug]` actually toggles and the corresponding map node turns green; `/lld`'s lock-in run still completes/advances correctly with the new `markLLDLessonComplete` call; no console errors on any of the three new map components.

**`/foundations` map is now Batman-Mode-only; prerequisite locking disabled** — new
- Default (dark) theme now renders `FoundationsList` (flat card grid, same shape as `/entities`) instead of the arcade `FoundationsMap`; the map is reserved for `night-ops` (Batman Mode) theme only. New `FoundationsIndexView.tsx` (client component) does the `useTheme()` switch; `page.tsx` is now just metadata + that view. Prerequisite lock/unlock gating (`getLessonStatus`'s `"locked"` branch) and the cave torch/shroud reveal mechanic (`CaveShroud`/`TorchCursor`/`TorchToggle`) are commented out app-wide, not deleted — every lesson is always open on both the list and the map now; completion ("Mark complete", green) tracking is unaffected. `tsc --noEmit`, full `vitest run` (1488 tests), and `npm run lint` all pass clean.
- Not yet live-checked: toggling the theme on `/foundations` actually swaps list ↔ map with no flash/jank beyond the expected one-frame hydration correction; the map in Batman Mode still pans/zooms/shows region labels/legend correctly with every node open (no locked/dimmed nodes, no leftover torch cursor or shroud darkness); the list view's card grid, hover states, and completed-checkmark all look right; no console errors from the removed torch pointer handlers.

**Removed the free-text answer box on `/interview-questions/[id]`** — new
- `AnswerWorkspace` no longer renders a `<textarea>`/draft-saving scratchpad (it wasn't graded or read by anything, just an empty box); it's now just a "Mark as answered" toggle + status badge, moved to the bottom of the detail page (after the optimal-answer section / no-answer-guide fallback, where it previously sat above them). `interviewAnswerProgress.ts` dropped `draftAnswer`/`getAnswerEntry`/`saveDraftAnswer` and the `"drafted"` status; `/interview-questions`'s list page and its `X / Y answered` count now only track `unattempted`/`answered`. `tsc --noEmit`, full `vitest run` (1488 tests), and `npm run lint` all pass clean.
- Not yet live-checked: the detail page's bottom section — confirm the toggle still reads/writes `localStorage` correctly and the badge/list-page status update live; confirm the reordering (progress toggle now below the optimal-answer disclosure, or below the "no answer guide yet" fallback paragraph) looks right rather than orphaned at the very bottom.

**Stripe added to `/interview-questions` (fintech company, 4 new questions)** — new
- New `src/content/interviewQuestions/companies/stripe.ts` (4 questions, following `google.ts`/`uber.ts`/`airbnb.ts`'s shape/doc-comment convention exactly, each with a full authored `optimalAnswer`), spread into `INTERVIEW_QUESTIONS` in `index.ts`. See `docs/interview_exp.md`'s Stripe sections for the full ~18-item research catalog, the no-browser-session research workaround (`r.jina.ai` reader-proxy), and ship-bar reasoning. No code changes needed outside the new file + the `index.ts` import/spread lines. `tsc --noEmit`, full `vitest run` (479 tests in this module at the time of this pass), and `npm run lint` all pass clean.
- Not yet live-checked: the Company filter chip row on `/interview-questions` now has a real Stripe option — confirm filtering and chip-row wrapping still read cleanly at the now-larger company count (this pass landed alongside a parallel PayPal pass, so the exact chip count depends on merge order); the one `fde-agentic` card (`stripe-ai-customer-support-agent`) shows the right category badge alongside the three `system-design` cards; the two long multi-part prompts (`stripe-webhook-delivery-noisy-neighbor-ssrf`, `stripe-payments-api-idempotent-charge` — both bundle several follow-up questions into one prompt) don't break card layout; each of the 4 detail pages' `optimalAnswer` disclosure renders correctly, including the new Workshop deep-links (`stripe-webhook-delivery-noisy-neighbor-ssrf` → `newsletter-send-confirmations`, `stripe-payments-api-idempotent-charge` → `checkout-timeout-mystery`, `stripe-ai-customer-support-agent` → `customer-support-agent`) actually resolving rather than 404ing, and the Foundations/Agentic links (sql-deep-dive, database-replication, database-sharding, message-queues, circuit-breakers, rate-limiting, redis-deep-dive, and — a first for this content module — the `/agentic/` lesson links tool-use-pattern, guardrails-as-architecture, the-agent-failure-taxonomy) all resolving too.

**PayPal added to `/interview-questions` (new company, 5 new questions)** — new
- New `src/content/interviewQuestions/companies/paypal.ts` (5 questions, following `google.ts`'s shape/doc-comment convention exactly, each with a full authored `optimalAnswer`), spread into `INTERVIEW_QUESTIONS` in `index.ts` alongside `STRIPE_QUESTIONS` (landed in parallel this pass) and the rest. See `docs/interview_exp.md`'s PayPal sections for the full 15-item research catalog and ship-bar reasoning — PayPal's public trail was notably thinner than every prior company's (Exponent's own DB has only 3 items), and the `claude-in-chrome` browser was unavailable this pass so every LeetCode/Glassdoor-sourced question ships at `confidence: "medium"` rather than the `"high"` a full page reread would give. No code changes needed outside the new file + the two `index.ts` lines. `tsc --noEmit`, full `vitest run` (509 tests in this module at the time of this pass), and `npm run lint` all pass clean.
- Not yet live-checked: the Company filter chip row on `/interview-questions` now has a new PayPal option — confirm filtering and chip-row wrapping still read cleanly at the now-larger company count (this pass landed alongside Stripe's, so the exact chip count depends on which merged first); all 5 cards are `system-design` category (no other category shipped this pass) — confirm the badge and card layout are fine with a company whose set is single-category, unlike every prior company; each of the 5 detail pages' `optimalAnswer` disclosure renders correctly, including the new Workshop deep-links (`paypal-irctc-ticket-booking` → `concert-ticket-drop`, `paypal-parking-lot-multi-floor-t24` → `parking-reservation-platform`) actually resolving rather than 404ing, and the Foundations/LLD links (rate-limiting, caching, database-replication, sql-deep-dive, message-queues, load-balancers, redis-deep-dive, and both `/lld/elevator-system` and `/lld/parking-lot`) all resolving too.

**LinkedIn added to `/interview-questions` (7th company, 6 new questions)** — new
- New `src/content/interviewQuestions/companies/linkedin.ts` (6 questions, following `google.ts`/`uber.ts`/`airbnb.ts`'s shape/doc-comment convention exactly, each with a full authored `optimalAnswer`), spread into `INTERVIEW_QUESTIONS` in `index.ts` between `AIRBNB_QUESTIONS` and `TWITTER_QUESTIONS`. See `docs/interview_exp.md`'s LinkedIn sections for the full ~23-item research catalog and ship-bar reasoning. No code changes needed outside the new file + the `index.ts` import/spread lines. `tsc --noEmit`, full `vitest run` (452 tests in this module at the time of this pass), and `npm run lint` all pass clean.
- Not yet live-checked: the Company filter chip row on `/interview-questions` now has a real LinkedIn option — confirm filtering and chip-row wrapping still read cleanly at the now-larger company count (this pass landed alongside Airbnb's and Twitter/X's, so the exact chip count depends on which of those merged first); the one `scenario-operational` card (`linkedin-legacy-app-quality-triage-behavioral`) shows the right category badge and its longer multi-part prompt doesn't break card layout; the other 5 cards' prompts (all `system-design`, a couple multi-sentence — `linkedin-flexible-cache-eviction-policies`, `linkedin-metrics-gathering-system`) render cleanly too; each of the 6 detail pages' `optimalAnswer` disclosure renders correctly, including the new Workshop deep-links (`linkedin-top-shared-posts-time-windows` → `trending-hashtags-feed`, `linkedin-internal-notification-system-scale` → `wildfire-alert-broadcast`) actually resolving rather than 404ing, and the Foundations/LLD links (kafka-deep-dive, redis-deep-dive, caching, consistent-hashing, message-queues, rate-limiting, circuit-breakers, load-balancers, database-sharding, lru-cache) all resolving too — `linkedin-legacy-app-quality-triage-behavioral` deliberately has no `relatedLinks` (no Foundations lesson maps cleanly onto an engineering-judgment/prioritization scenario), confirm it renders fine with that section simply absent rather than empty/broken.

**Airbnb added to `/interview-questions` (sixth company, 6 new questions)** — new
- New `src/content/interviewQuestions/companies/airbnb.ts` (6 questions, following `google.ts`/`uber.ts`'s shape/doc-comment convention exactly, each with a full authored `optimalAnswer`), spread into `INTERVIEW_QUESTIONS` in `index.ts` alongside the other five companies. See `docs/interview_exp.md`'s Airbnb sections for the full ~27-item research catalog and ship-bar reasoning. No code changes needed outside the new file + the two `index.ts` lines. `tsc --noEmit`, full `vitest run` (413 tests in this module), and `npm run lint` all pass clean.
- Not yet live-checked: the Company filter chip row on `/interview-questions` now has a sixth real option (Airbnb) — confirm six-way filtering and chip-row wrapping still read cleanly; the one `lld-ood` card (`airbnb-in-memory-file-system`) renders correctly alongside the mostly-`system-design` set; a couple of unusually long multi-sentence prompts (`airbnb-booking-platform-hot-partitions`, `airbnb-booking-waitlist-demand-notifications`, `airbnb-in-memory-file-system`) don't break card layout; each of the 6 detail pages' `optimalAnswer` disclosure renders correctly, including the new Workshop deep-links (`airbnb-booking-platform-hot-partitions` → `concert-ticket-drop`, `airbnb-booking-waitlist-demand-notifications` → `parking-reservation-platform`, `airbnb-host-guest-messaging-fanout` → `flight-status-push-updates`) actually resolving rather than 404ing, and the Foundations/LLD links (sql-deep-dive, nosql-deep-dive, redis-deep-dive, message-queues, database-sharding, consistent-hashing, rate-limiting, caching, oop-fundamentals, lld-interview-approach) all resolving too.

**`/foundations` map: `MapNode` card redesign — richer, less "just a box" (user follow-up — "the components in foundation in learn look very dull, just a box")** — new, not yet live-checked (verified via `tsc`/lint only)
- Restructured every node card (`MapNode.tsx`) from one flat `p-3` padded box into the same three-tier shape `ComponentNode.tsx` already uses for Workshop canvas nodes: a full-width top status strip (`STRIP_TONE` — border-colored per locked/available/completed, flush to all three edges), a header row with the entity icon in its own small bordered swatch (`SWATCH_TONE`, falls back to the lesson number when a lesson has no related entity icon) instead of a bare inline icon, and a `border-t`-separated footer for the minutes readout instead of it just being the last line in the same padded block. Number + lock/check/dot status glyph moved into a header sub-row next to the title instead of sharing a row with the icon. All existing state logic (locked/available/completed tone, the completing yellow→green wipe overlay, the unlocking pop-in, the cave-mode glow) is unchanged — only `renderContent`'s internal markup and the outer wrapper's classes (`p-3` → no padding, now that padding lives on the header/footer sub-blocks) changed. Also removed an `overflow-hidden` I'd initially added to mirror `ComponentNode` — it would have clipped the `emphasis` "Start"/"Final challenge" tag, which deliberately sits at `-top-3` (partly outside the card's own box).
- Not yet live-checked: cards actually read as richer/less flat at a normal zoom level across all three states (locked/available/completed) and in both the default theme and Batman Mode (cave glow should still layer on top of the new shape correctly, same `box-shadow` bloom, no visual conflict with the new top strip); the icon swatch renders correctly both when a lesson has a related entity icon and when it falls back to the plain number tile (e.g. DNS/TCP/REST, which have no simulated counterpart); the completing wipe overlay (mark a lesson complete, return to the map) still visually holds yellow then wipes to green with the new three-tier shape, and the checkmark's delayed pop-in still lands at roughly the right moment under the new layout; the emphasis "Start"/"Final challenge" tag still renders correctly sitting above the card, unclipped; nothing collides or overflows oddly at the map's min zoom (0.4×) where every card is smallest on screen; card height didn't grow enough to cause any visual overlap in the map's densest lane clusters (verified by hand against `mapLayout.ts`'s coordinates that there's large margin, but not eyeballed in the real browser).

**`/foundations` map: Batman Mode cave treatment — dark cave + torch cursor (user follow-up — "keep this in batman mode... entire screen dark like a cave, glowing stick in the left corner, torch cursor to see nearby entities")** — new, not yet live-checked (verified via `tsc`/lint/`vitest run`, 1279 tests, all pass; not yet opened in a real browser)
- Gated entirely on `theme === "night-ops"` (`FoundationsMap`'s new `cave` flag) — the default theme's map is untouched, confirmed by keeping its original single-pass render branch byte-for-byte. In cave mode: every `available`/`completed` node/edge now renders with an added glow (`box-shadow`/`drop-shadow` in the same signal/healthy tone `toneClasses` already used, root/`start` node gets a slower `animate-cave-beacon-pulse` on top), reusing the existing `node-complete-wipe`/`node-light-in`/`edge-draw-in` reveal animations completely unchanged for *when* something lights up — only the resting "already lit" look changed, per the ask to keep "the same technique and transitions." Every still-`locked` node/edge is now routed through a new `CaveShroud` wrapper (new file) instead of rendering dim-but-visible — `CaveShroud` applies a CSS `mask-image` (the standard alpha-based "flashlight" trick) driven by two CSS custom properties (`--torch-x`/`--torch-y`/`--torch-radius`) written directly onto its DOM node by `FoundationsMap`'s pointer-move handler, not React state, so locked content defaults to fully invisible (`--torch-radius: 0px`) until the torch is lit.
- New `TorchToggle.tsx` — the "glowing stick," a HUD button stacked directly above `MapZoomControls` (bottom-left, so "the left corner" reads as one cluster), only rendered when `cave` is true. New `lib/torchMode.ts` persists on/off to `localStorage` (`engineering-studio:foundations-torch`), same pattern as `foundationsProgress.ts`. New `TorchCursor.tsx` — a flame-blue glow (lucide `Flame`, blue tone `hsl(205 90% 62%)` distinct from the signal-yellow used for progress) that follows the real pointer via `fixed` + imperative `transform`, replacing the native cursor (`cursor-none`) while the torch is lit; hidden (`opacity: 0`) until the first real pointer position arrives, and again whenever the pointer leaves the map. Torch radius is zoom-compensated (`TORCH_RADIUS_PX / zoom`) so it keeps the same apparent on-screen size at any zoom level. `MapLegend` swaps its "Locked" key entry for "Unlit" and adds a one-line hint ("Light the torch..." / "Move your torch near the dark...") whenever `cave` is true, so a mostly-black canvas doesn't read as broken.
- **Two real bugs found from user follow-up and fixed (not yet re-verified live)**:
  1. *"The screen becomes so slow when using the torch"* — root cause was two compounding costs on every raw `pointermove` (which can fire far more often than the screen repaints): a fresh `getBoundingClientRect()` call forcing a synchronous layout flush, and `CaveShroud`'s mask being fully regenerated (`radial-gradient(circle R at X Y, ...)` with `X`/`Y`/`R` baked into the gradient's own arguments) forcing a full re-rasterize of the mask and everything under it. Fixed: the container rect is now cached (only invalidated on window resize — panning/zooming move content *inside* the container, never the container's own screen position) and all torch DOM writes are throttled to once per `requestAnimationFrame` regardless of how many pointer events land in that frame; `CaveShroud`'s mask shape is now one fixed, never-changing gradient repositioned/rescaled via `mask-position`/`mask-size` (cheap, like animating `background-position`) instead of regenerated per frame.
  2. *"After marking [a lesson] and going back to the main page, if the torch is still on, the torch effect stops working"* — the effect that writes `--torch-radius` onto the shroud ran on mount before `CaveShroud` actually existed in the DOM (it's behind the async `ready` gate), silently no-op'd, and had no way to retry: its dependencies (`cave`/`torchOn`/`zoom`) are unchanged from before the navigation (a remount reuses the same persisted `torchOn`), so the effect never re-ran once the shroud actually mounted a moment later. The toggle showed "on" but the radius was simply never set. Fixed by adding `ready` to the effect's dependency list purely to force a re-run once the shroud exists, regardless of whether `cave`/`torchOn`/`zoom` actually changed.
- Not yet live-checked (nothing here has been opened in a browser yet): switching into Batman Mode actually renders the map with locked lessons fully invisible and available/completed ones visibly glowing; the torch toggle button's dim/lit "stick" visual and its bottom-left stacking doesn't collide with `MapZoomControls` at a narrow viewport; clicking it lights the torch, swaps the native cursor for the flame-blue `TorchCursor`, and moving the mouse near a previously-invisible locked node actually reveals it (and its incoming edge, if any) with a soft-edged circular falloff, not a hard-edged pop; the revealed circle stays visually pinned under the cursor correctly while panning and at both zoom extremes (0.4x and 1.5x) — this is the one part of the math (canvas-local coordinate conversion accounting for scroll + scale) that's never been run against a real pointer; sweeping the torch around now feels smooth, not sluggish (the specific complaint the perf fix targets); marking a lesson complete from its own page with the torch on, returning to the map, and confirming the torch still actually reveals locked nodes on the first mouse-move (the specific complaint the second fix targets) — not just that the toggle still visually reads "on"; the flame cursor's own idle flicker animation (`animate-torch-flicker`) reads as a subtle live flame, not a distracting blink; turning the torch off snaps locked content immediately back to fully invisible rather than lingering; the root/"Start" node's beacon pulse (`animate-cave-beacon-pulse`) is visible without being distracting at rest; reloading the page with the torch previously on restores it (localStorage persistence); the yellow→green completing-transition and the newly-added glow don't visually fight each other mid-animation; leaving Batman Mode (theme toggle, if unlocked) cleanly drops back to the original non-cave rendering with no leftover mask/cursor artifacts.

**`/foundations` map: click-drag panning + zoom controls (user follow-up — "not have grab and move option, nor any zoom in zoom out option")** — new, not yet live-checked (Claude-in-Chrome extension was disconnected this session; code verified via `tsc`/lint/tests only)
- New `MapZoomControls.tsx` (bottom-left HUD cluster, mirrors `MapLegend`'s chrome — Zoom Out / percentage / Zoom In / Reset). `FoundationsMap.tsx` gained: click-drag panning via Pointer Events + `setPointerCapture` (skips starting a pan when the pointerdown lands on an `<a>`/`<button>`, so lesson-card and locked-node-reveal clicks are untouched), `cursor-grab`/`cursor-grabbing` on the scroll container, and zoom (0.4×–1.5×, `MIN_ZOOM`/`MAX_ZOOM`/`ZOOM_STEP`) via a real (untransformed) "sizer" div sized to `MAP_WIDTH * zoom` × `MAP_HEIGHT * zoom` wrapping a `transform: scale(zoom)` content div — the sizer is what the `overflow-auto` parent measures for its scrollable range, since a transform alone doesn't change that. Zoom is anchored (not always top-left): a `useLayoutEffect` keyed on `zoom` computes the scroll adjustment after the new scaled size has actually committed to the DOM, so the content under the cursor (wheel-zoom) or viewport center (+/- buttons) stays visually still. Ctrl/Cmd+scroll-wheel zoom uses a native (non-passive) `addEventListener`, not React's `onWheel`, specifically because React's default-passive wheel listener silently no-ops `preventDefault()`. "Reset view" recenters on the frontier lesson at zoom 1, reusing the same `recenterOnFrontier` the mount effect already had (extracted, not duplicated). `tsc --noEmit`, `npm run lint`, and the full `vitest run` (1240 tests) all pass clean.
- Not yet live-checked: drag-panning from empty canvas space actually scrolls smoothly and doesn't accidentally trigger a browser-native drag/text-selection; dragging *starting on* a lesson card still navigates normally on a clean click (no drag) and doesn't accidentally pan instead; the three zoom buttons at both extremes (confirm they visibly disable — not hide — at 40%/150% rather than erroring or over/under-shooting); Ctrl+scroll (and, if testable, real trackpad pinch) zooms smoothly anchored under the cursor with no jump; the percentage readout updates live and matches; "Reset view" both re-centers on the actual frontier node *and* snaps back to 100%, from a state that's both panned away and zoomed in/out; `MapZoomControls`' bottom-left placement doesn't collide with `MapLegend`'s bottom-right one at a narrow viewport; the loading state (`ElectricLoader`) still renders correctly with the new sizer-wrapper DOM structure now sitting around it; Batman Mode (`night-ops`) renders the new control cluster sanely (no bespoke theming written for it, should just inherit the shared button/border tokens like `MapLegend` does).

**`/foundations` map: edges rewired to stop cutting through unrelated node cards ("paths under other nodes," user follow-up)** — live-verified
- Root cause, found programmatically before touching anything: a throwaway script (line-vs-card-rect intersection test, every prerequisite edge against every unrelated node's box — same discipline `movieTicketBooking.ts`'s header documents for tuning against the real engine, deleted after use) found 11 of the map's 33 edges genuinely cut through a card that wasn't one of their own two endpoints, concentrated in the "Delivery & Speed" tier (Load Balancers/Caching both feed CDN *and* Consistent Hashing, plus Message Queues/Kafka and the long Database Indexing → Database Sharding skip-edge all sharing the same cramped vertical band) — exactly what read as "messy."
- Three-part fix, in order of what actually moved the number: (1) `MapEdges.tsx`'s bow direction used to alternate by array index (`i % 2`), unrelated to the real geometry — replaced with `EDGE_BOW_SIGN`, an explicit per-edge lookup baked in from the same script, so every curve bows toward the side that's actually clear. (2) The bow magnitude's cap/scale (6/0.18 → 18/0.32) — the map's longest skip-edges need real room to arc around a full tier's worth of cards, not a light wobble. (3) `mapLayout.ts`'s "Delivery & Speed" tier reordered (Load Balancers+Redis share the top lane, the CDN/Consistent Hashing pair shares the middle since both are fed by both parents, Kafka moved down to sit under its actual parent Message Queues instead of near the top) — this is what a script-only fix couldn't reach, since some crossings are structurally forced by ordering, not curve tuning. `FoundationsMap.tsx`'s `MAP_HEIGHT` also went 900→1300 (shrinks every card's height as a percentage of the canvas, more breathing room for free, zero coordinate changes needed since everything's percent-based). Re-ran the same script after: 0 of 33 edges hit an unrelated card (down from 11), and stays at 0 even measuring the endpoints under a modest safety margin for all but a couple of the very longest edges. `tsc --noEmit`, `npm run lint`, and the full `vitest run` (1240 tests) all pass clean.
- **Live-verified in the real browser**: navigated `/foundations` and panned to every previously-offending cluster. The Load Balancers/Caching/CDN/Consistent Hashing/Redis/Kafka/Rate Limiting group (the worst offender) now reads as clearly separated lanes — Caching and Consistent Hashing sit side by side with a short clean connector, CDN sits above both parents' curves with visible clearance, Message Queues→Kafka is a short direct arc with no crossing traffic. The Database Indexing→Database Sharding skip-edge (previously threading straight through Message Queues and Consistent Hashing) now sweeps a wide arc that visibly clears Kafka Deep Dive, Rate Limiting, and Database Sharding's neighbors with real margin, confirmed by zoomed screenshots of that exact corridor. The DNS→Vertical Scaling edge (previously grazing REST APIs) now dips well clear of it. Zero console errors throughout. Not independently re-confirmed: the capstone's 5-way convergence (it's currently locked in this profile's saved progress, so those edges aren't drawn by design — same "don't pre-draw locked trails" rule as always, not a gap in this fix).

**`/foundations` map reveal: stage 1 changed from a uniform yellow→green crossfade to a left-to-right wipe (then slowed down, user follow-up)** — new
- Replaced `MapNode`'s `transition-colors` (whole card recoloring at once) with a stacked-layer `clip-path` wipe: the real completed-status card renders from the first frame, with a decorative available-styled duplicate (`pointer-events-none`, `aria-hidden`) on top that holds for 1s then clips itself away from the left edge (`animate-node-complete-wipe`, `globals.css`, `linear` timing), uncovering the green card underneath left-to-right like a progress fill. Removed the old `useState`/`useEffect` hold-then-flip logic entirely — the hold and the wipe are now both pure CSS (`animation-delay`/`animation-duration`), so `FoundationsMap`'s stale-comment reference to `MapNode`'s own `settled` state was also updated. Checkmark's `animate-pop-in` gets an inline `animationDelay` on the real card so it visibly pops right as the wipe uncovers it, instead of finishing invisibly under the overlay early on.
- Follow-up: the initial 700ms wipe was reported back as still too quick to actually watch the color sweep rather than just notice it had happened — slowed to 1500ms. Stage 2/3's start delays shifted to match, preserving the same relative sequencing: `edge-draw-in` 1650ms→2450ms, `node-light-in` 2550ms→3350ms, checkmark's inline delay 1600ms→2350ms. `tsc --noEmit` and `npm run lint` both pass clean after each pass.
- Not yet live-checked: reproduce a genuine "just completed" diff (mark a lesson complete via its own page, then a lesson slug missing from the "seen" `localStorage` snapshot before reloading `/foundations`) and confirm the card visibly holds yellow for ~1s, then a clean vertical boundary sweeps left→right over a noticeably slow ~1.5s turning it green (not a flicker/pop, not both colors visible at once outside the sweep, genuinely watchable now rather than a blink), the checkmark pops in right around when the sweep passes it rather than being already-visible or popping noticeably late, the emphasis tag (if the completing node is `start`/`capstone`) doesn't visibly double up or flicker as the sweep crosses it, and the subsequent edge-draw-in/node-light-in beats still fire in the same relative sequence as before this change.

**`/lld/editor` — Pillar B Phase 5 (stretch): "download your design as code" (TS codegen export)** — new
- New pure `src/lld-modeling/codegen.ts` (`generateTypeScript`) — one `export class`/`export interface` block per class/interface box, `extends`/`implements` derived from inheritance/realization edges, Java/C#-flavored scalar types (`String`, `double`, `int`, ...) mapped to their TS equivalents, every concrete method body a `throw new Error("Not implemented")` stub (always type-valid regardless of declared return type — deliberately not a real implementation). New "Export" button in `EditorShell.tsx`'s header (disabled on an empty canvas), triggering a browser download (`Blob` + temporary `<a download>`, object URL revoked right after) of `diagram.ts`. `tsc`, full `vitest run` (1240 tests, 5 new in `codegen.test.ts`), `lint` all pass clean.
- Not yet live-checked (nothing here has been opened in a browser yet): the Export button is disabled on a blank canvas and enabled once a class exists; clicking it actually triggers a file download named `diagram.ts` (not just a console error) and the downloaded file's content matches what a hand-traced diagram should produce — a small interface + a class realizing it + a class extending another, checking `implements`/`extends` both render, field/method visibility modifiers, and that the file is valid TypeScript when opened in an editor with type-checking (no red squiggles from the generator's own output); a class with an `abstract`-flagged method inside a non-"abstract"-stereotype class falls back to a stub body instead of emitting invalid bodyless `abstract` syntax; an untitled/blank class on the canvas is correctly omitted from the export rather than emitting `export class Untitled {}`; the button's placement/spacing next to Clear in the header doesn't crowd at a narrow viewport.

**`/lld/editor` — Pillar B Phase 4: the 7 case studies as buildable challenges** — mostly CONFIRMED live, one direction-sensitive modeling nuance found (not a bug), one item genuinely blocked (automation limitation, not a code issue)
- New `src/lld-modeling/challenges.ts` + `ChallengeBriefing.tsx`, `lldStore.ts`'s `activeChallengeId`/`startChallenge`/`exitChallenge`, a "Build it" CTA on every case-study `/lld/[slug]` page, and `?challenge=` deep-link handling on `/lld/editor`. `tsc`, full `vitest run` (1235 tests, 20 new), `lint` all pass clean.
- **Live-verified on Parking Lot**: `/lld/parking-lot`'s "Build it" section renders cleanly (heading, copy, orange CTA) and its link (`href="/lld/editor?challenge=parking-lot"`) lands on a genuinely blank canvas (0/5) — confirmed both via the CTA click and via a fresh direct navigation to the `?challenge=...` URL, so it's not merely "blank because Exit happened to leave it that way". Header shows a "PARKING LOT" badge next to "Class Diagram Editor". Inspector shows `ChallengeBriefing` (title, prompt, N/M badge, checklist with circle icons) when nothing is selected, and correctly swaps to the plain Class/Relationship editor panel when a node or edge is selected, falling back to `ChallengeBriefing` again on deselect. Adding a class renamed to `ParkingSpot` with a method renamed to `park` flipped the "A way to park/assign a vehicle to a spot" requirement live to a green check + strikethrough and bumped the badge 0/5 → 1/5. Header "Clear" reset the diagram to 0/5 while staying in challenge mode (badge unchanged); "Exit" returned to the plain untitled editor (badge and `ChallengeBriefing` both gone, ordinary "Nothing selected" state instead) while leaving the just-added node on canvas untouched — Clear vs Exit are correctly distinct actions. "Re-read the lesson →" is confirmed via DOM inspection to carry `target="_blank" rel="noreferrer"` pointing back at `/lld/parking-lot`. All 7 case-study lesson pages' HTML was fetched directly and each carries the correct `/lld/editor?challenge=<slug>` link. Visiting `/lld/editor` with no `?challenge=` param still opens the plain blank editor (Phase 1/2/3 unaffected).
- **Live-verified end-to-end on a second challenge (LRU Cache)**, exercising the field/method-only requirement path (no relationship needed): naming a class `LRUCache`, adding a `capacity` field and `get`/`put`/`evict` methods flipped all 4 requirements live and the badge turned "4/4" in a distinct completed-green tone (not just the same orange styling with a higher number) — confirms the fully-complete state renders correctly, not only partial progress. The Lint drawer opened correctly alongside the still-open Challenge panel with its own "nothing to flag" empty state — the two panels coexist without collision.
- **One real nuance worth flagging, found while live-building Rate Limiter's "algorithms" requirement** (`hasInterfaceWithImplementers`: needs 2 realization edges whose `target` is the same interface) — not a bug, but a non-obvious modeling constraint: this editor's connector handles are fixed by side (left = target-type, right = source-type) on every node, and `RelationshipEdge.tsx`'s own documented convention ("triangle sits at the target end — the interface") only comes out correct if the student drags from the *implementing class's* right-hand connector to the *interface's* left-hand connector. Dragging the equally-plausible reverse way (interface's right connector → class's left connector) still creates a valid, normal-looking realization edge, but with source/target reversed, so the hollow triangle lands on the class end instead of the interface end — a visible tell a UML-literate student would likely notice and self-correct — and the "algorithms" requirement silently never flips true from that edge. Confirmed the correct direction does register properly (verified via React-internal edge inspection: `source: class_2 (the class), target: class_1 (the interface), kind: "realization"`) and renders the triangle on the correct (interface) end. This is inherent to Phase 1's original connection model, not introduced by Phase 4, and the app's own `RelationshipEdge.tsx` doc comment already states the intended convention correctly — flagging only because a student hitting this while building Rate Limiter or Splitwise (both use this same requirement shape) may be confused about why a correct-looking diagram doesn't pass.
- **Genuinely blocked, not a code issue**: reliably reproducing *two* live realization edges into the same interface (to see "algorithms" flip 0→ satisfied) took dozens of retries — native synthetic drags and raw `PointerEvent` dispatch at short, medium, and long handle-to-handle distances all intermittently registered as a node-move instead of a connection-drag, with no consistent distance/position rule found this session. This is the same "flaky synthetic-pointer-event" limitation already documented multiple times elsewhere in this file for React Flow's small connector handles (see the Phase 4 Agentic AI entry above) — not new, not caused by this phase's changes. A `next dev` crash also interrupted this sub-test partway through (server was simply down; unrelated to the code under test, restarted cleanly mid-session, and all other checks in this entry were completed against a healthy server). A future session with real mouse input should confirm the full 2-realization "algorithms" flip directly; the underlying mechanism (Kind-switching, checklist reactivity, correct-direction registration) is otherwise confirmed working above.

**`/lld/editor` — Pillar B Phase 1: the class-diagram editor (new route, new canvas paradigm)** — live-verified, one real bug found (not yet fixed)
- New `/lld/editor` route (`EditorShell.tsx`, `DiagramCanvas.tsx`, `ClassNode.tsx`, `RelationshipEdge.tsx`, `InspectorPanel.tsx`, `PaletteSidebar.tsx`, all under `src/components/lld/`), a new Zustand `lldStore.ts`, and a new framework-independent data model `src/lld-modeling/types.ts` (reuses `UmlRelationshipKind` from `content/shared/lesson.ts` and the existing `UmlMarkers.tsx` marker components rather than duplicating either). `/lld`'s header gained a primary "Open Editor" CTA linking here.
- `tsc --noEmit`, full `vitest run` (1187 tests), and `npm run lint` all pass clean.
- **Live-verified in the real browser**: both the palette's real drag-and-drop and its click-to-add fallback create nodes correctly (drag lands the node exactly at the drop point; click uses the default cascade placement); dragging a connection between two boxes' left/right handles produces a default plain-line "association" edge; the Inspector's Kind dropdown cycles through all 6 relationship kinds with the edge live-updating; realization and dependency render correctly dashed, dependency's open arrowhead is correctly unfilled; multiplicity/label text on both a short and a long diagonal edge sits cleanly with no overlap of the node cards or the line itself; editing a class's Name field and Stereotype dropdown live-updates the `ClassNode` card (confirmed «interface»/italic-name for Interface stereotype and italic-name-no-tag for Abstract class); adding/removing fields and methods live-updates the card, including the visibility +/-/#/~ picker, the static-underline and abstract-italic method styling, and the "Remove field"/"Remove method" buttons; the empty-state "Start modeling" copy is centered on a blank canvas; the "Clear diagram" header button resets to that empty state; Batman Mode (`night-ops`) renders the whole editor sanely — undecorated (no special theming for this canvas) but not broken. (There is no separate light theme to check — confirmed via `ThemeProvider.tsx` the app is dark-only, toggling only between `dark` and `night-ops`.)
- **Real bug found, not fixed**: when a relationship edge is selected, the aggregation diamond and the inheritance/realization triangle markers lose their hollow (outline-only) rendering and become solid-filled instead — making a selected aggregation edge visually indistinguishable from composition, and a selected inheritance/realization edge look like a filled arrowhead rather than a hollow one. Confirmed via the live marker DOM: unselected uses `fill-bg-elevated`/`fill-text-muted` (correctly hollow vs. filled) but selected always swaps to `fill-signal stroke-signal` regardless of kind, dropping the hollow/filled distinction. Root cause is almost certainly in `RelationshipEdge.tsx`'s marker-fill class logic conflating "selected color" with "filled shape" — worth a fix pass.

**Apple added to `/interview-questions` (fifth company, 10 new questions)** — new
- New `src/content/interviewQuestions/companies/apple.ts` (10 questions, following the other four companies' shape/doc-comment convention exactly, each with a full authored `optimalAnswer`), spread into `INTERVIEW_QUESTIONS` in `index.ts` alongside the other four. See `docs/interview_exp.md`'s Apple sections for the full research catalog and ship-bar reasoning. No code changes needed outside the new file + the two `index.ts` lines. `tsc --noEmit`, full `vitest run` (299 tests in this module), and `npm run lint` all pass clean.
- Not yet live-checked: the Company filter chip row on `/interview-questions` now has a fifth real option (Apple) — confirm five-way filtering and chip-row wrapping still read cleanly; the second `ml-ai-system-design` card for a non-Google/Meta/Microsoft company (`apple-ondevice-vs-cloud-decision-feature`) and the second `scenario-operational` card overall (`apple-observability-storage-migration-block-store`, alongside `apple-subscription-form-billions-scale` which is `system-design`) show the right category badges; two `lld-ood` cards (`apple-view-hierarchy-hit-test`, `apple-canvas-app-shapes-layers-dragdrop`) render correctly; a few unusually long multi-sentence prompts (`apple-observability-storage-migration-block-store`, `apple-auth-migration-oidc`, `apple-applicant-tracking-system`) don't break card layout; each detail page's `optimalAnswer` disclosure renders correctly, including the new Workshop deep-links (`apple-subscription-form-billions-scale` → `flash-sale`, `apple-icloud-photos-sync-devices` → `fitness-tracker-step-sync`, `apple-imessage-delivery-guarantees` → `flight-status-push-updates`, `apple-ondevice-vs-cloud-decision-feature` → `cost-constrained-edge-assistant`) actually resolving rather than 404ing, and the Foundations/LLD links (consistent-hashing, database-replication, oop-fundamentals, uml-class-diagrams, behavioral-patterns, caching, redis-deep-dive, rest-apis, database-indexing-deep-dive, load-balancers, message-queues) all resolving too.

**Microsoft added to `/interview-questions` (fourth company, 13 new questions)** — new
- New `src/content/interviewQuestions/companies/microsoft.ts` (13 questions, following `google.ts`/`meta.ts`/`amazon.ts`'s shape/doc-comment convention exactly, each with a full authored `optimalAnswer`), spread into `INTERVIEW_QUESTIONS` in `index.ts` alongside the other three. See `docs/interview_exp.md`'s Microsoft sections for the full research catalog and ship-bar reasoning. No code changes needed outside the new file + the two `index.ts` lines. `tsc --noEmit`, full `vitest run` (249 tests in this module), and `npm run lint` all pass clean.
- Not yet live-checked: the Company filter chip row on `/interview-questions` now has a fourth real option (Microsoft) — confirm four-way filtering and chip-row wrapping still read cleanly at that width; the first `ml-ai-system-design` card for a non-Google/Meta company (`microsoft-chatgpt-style-platform-inference-nodes`) and the first `scenario-operational` cards overall (`microsoft-idempotent-checkout-fintech`, `microsoft-azure-region-migration-ai-prompt-surge`) show the right category badges; two `lld-ood` cards (`microsoft-file-system-lld-coded-exercise`, `microsoft-parking-lot-lld-to-hld`) render correctly alongside the mostly-`system-design` set; a few unusually long multi-sentence prompts (`microsoft-multi-region-customer-data-ux-split`, `microsoft-url-shortener-snowflake-multi-region`, `microsoft-azure-region-migration-ai-prompt-surge`) don't break card layout; each detail page's `optimalAnswer` disclosure renders correctly, including the new Workshop deep-links (`microsoft-visual-studio-live-share` → `live-sports-scoreboard`, `microsoft-chatgpt-style-platform-inference-nodes` → `cost-constrained-edge-assistant`, `microsoft-top-10-products-billions-records` → `global-leaderboard-updates`, `microsoft-url-shortener-snowflake-multi-region` → `url-shortener`, `microsoft-idempotent-checkout-fintech` → `checkout-timeout-mystery`, `microsoft-twitter-celebrity-tweet-fanout` → `trending-hashtags-feed`) actually resolving rather than 404ing, and the Foundations/LLD links (cdn, circuit-breakers, oop-fundamentals, uml-class-diagrams, message-queues, database-sharding, estimation-and-interview-framework, redis-deep-dive, kafka-deep-dive, load-balancers, database-indexing-deep-dive, parking-lot, database-replication, rest-apis, caching) all resolving too.

**Amazon added to `/interview-questions` (third company, 16 new questions)** — new
- New `src/content/interviewQuestions/companies/amazon.ts` (16 questions, following `google.ts`/`meta.ts`'s shape/doc-comment convention exactly, each with a full authored `optimalAnswer`), spread into `INTERVIEW_QUESTIONS` in `index.ts` alongside `GOOGLE_QUESTIONS`/`META_QUESTIONS`. See `docs/interview_exp.md`'s Amazon sections for the full research catalog and ship-bar reasoning. No code changes needed outside the new file + the two `index.ts` lines. `tsc --noEmit`, full `vitest run` (184 tests in this module), and `npm run lint` all pass clean.
- Not yet live-checked: the Company filter chip row on `/interview-questions` now has a third real option (Amazon) — confirm three-way filtering and chip-row wrapping still read cleanly; several of the 16 new cards carry unusually long multi-sentence prompts (`amazon-ab-experimentation-platform`, `amazon-delivery-locker-capacity`, `amazon-mars-rover-update`, `amazon-genai-fluency-production-incident`) — confirm the card layout doesn't break; each detail page's `optimalAnswer` disclosure renders correctly, including the new Workshop deep-links (`amazon-delivery-locker-capacity` → `warehouse-inventory-sync`, `amazon-discount-coupon-stacking` → `coupon-code-redemption`, `amazon-mars-rover-update` and `amazon-alexa-peloton-integration` → `iot-sensor-ingestion`, `amazon-ecommerce-black-friday-surge` → `flash-sale`, `amazon-genai-fluency-production-incident` → `checkout-timeout-mystery`, `amazon-dream11-leaderboard-100k-teams` → `global-leaderboard-updates`) actually resolving rather than 404ing, and the Foundations/LLD links (cdn, consistent-hashing, database-indexing-deep-dive, database-replication, database-sharding, estimation-and-interview-framework, load-balancers, message-queues, kafka-deep-dive, rest-apis, sql-deep-dive, caching, circuit-breakers, oop-fundamentals, solid-principles, tic-tac-toe) all resolving too.

**Meta added to `/interview-questions` (second company, 13 new questions)** — new
- New `src/content/interviewQuestions/companies/meta.ts` (13 questions, following `google.ts`'s shape/doc-comment convention exactly, each with a full authored `optimalAnswer`), spread into `INTERVIEW_QUESTIONS` in `index.ts` alongside `GOOGLE_QUESTIONS`. See `docs/interview_exp.md`'s Meta sections for the full ~130-item research catalog and the ship-bar reasoning (13/~130 shipped, similar hit rate to Google's 8/34). No code changes needed outside the new file + the two `index.ts` lines, exactly per the tracker's documented "code touchpoints" — the list page, filters, detail page, and answer workspace are all already generic over `INTERVIEW_QUESTIONS`.
- `tsc --noEmit`, full `vitest run` (105 tests in this module), and `npm run lint` all pass clean.
- Not yet live-checked: the Company filter chip row on `/interview-questions` now has a second real option (Meta) — confirm it filters correctly and reads fine alongside Google in the chip layout; the new `ml-ai-system-design` category badge shows correctly on `meta-em-recommendation-system`'s card (first time this category has shipped for a non-Google company, and the first EM-flavored / ML-framed card in the list); card layout doesn't break on the two longer prompts (`meta-post-privacy-visibility`, `meta-audio-system-cross-app`, both multi-sentence); each of the 13 new detail pages' `optimalAnswer` disclosure renders correctly, including the two Workshop deep-links (`meta-ticketmaster-single-event` → `concert-ticket-drop` + `flash-sale`, `meta-price-tracker-camelcamelcamel` → `price-alert-notifications`) actually resolving rather than 404ing, and the Foundations links (cdn, consistent-hashing, database-indexing-deep-dive, estimation-and-interview-framework, load-balancers, message-queues, nosql-deep-dive, rate-limiting, rest-apis, caching, circuit-breakers, database-sharding) all resolving too.

**`/foundations` map reveal: real bug found and fixed — the yellow→green transition was never actually playing (user follow-up, twice: first "still too quick to see," then "the transition begins before the page even loads")** — live-verified
- Root cause, not just another timing tweak: `MapNode` could mount for its *first-ever* paint before `FoundationsMap`'s `reveal` diff (`justCompleted`/`justUnlocked`) had resolved — `mounted` (gating first paint) flips true via `useSyncExternalStore`'s synchronous pre-paint resync, but `reveal` is computed in an ordinary passive `useEffect` that only runs after that paint. At that first mount, `transition` was still `undefined`, so a just-completed lesson's card painted fully green immediately, no "completing" state at all. Worse, `MapNode`'s `settled` state is a `useState(!isCompleting)` initializer — it latched `true` at that premature mount (since `isCompleting` read `false`) and never un-latched once `reveal` resolved a moment later and the real `transition="completing"` prop arrived, so the whole hold-then-transition mechanism silently no-opped from then on. This is exactly "the transition begins before the page loads": the card was already green before the reader could see anything, and the follow-up re-render just replayed a transition into the same color.
- Fix: `FoundationsMap` now gates its entire content render (regions/edges/nodes/legend) on a combined `ready = mounted && reveal !== null`, not `mounted` alone — `MapNode` never mounts until `transition` is already correct, so `settled`'s initializer sees the real value from the start and the hold-then-transition actually runs. The (now-guaranteed-brief, but non-zero on a slow device) gap before `ready` shows `ElectricLoader` (existing route-level loading UI, new `fullScreen={false}` prop to embed it instead of forcing `min-h-screen`) centered on the map's viewport instead of a blank canvas — reuses the same "this interface is doing something" language as every route's own `loading.tsx`.
- Also carried forward from the prior pass: stage 1's pre-flip "available" hold lengthened 50ms→1000ms, stages 2/3's start delays shifted to match (`edge-draw-in` 700ms→1650ms, `node-light-in` 1600ms→2550ms) — now meaningful, since the mechanism they depend on actually runs.
- **Live-verified in the real test browser profile** (dev server restarted first — it had gone down mid-session). Real tool round-trip latency (several seconds between a `navigate` call returning and the next script actually executing) made racing the production 1s/700ms timing directly unreliable to observe, so verified properly instead: temporarily slowed the hold to 15000ms in `MapNode.tsx`, reproduced a genuine "just completed" diff via `localStorage` (marked `vertical-vs-horizontal-scaling` complete but absent from the "seen" snapshot, unlocking Load Balancers/Caching/Message Queues), reloaded, and confirmed via both `className` inspection and a zoomed screenshot that the card was still rendering `border-signal/70` (the "available"/yellow tone) at `performance.now() ≈ 6.5s` post-mount — proving the hold is real, not skipped — then confirmed it had flipped to `border-status-healthy/50` (green, checkmark) with the `transition-colors duration-[700ms]` class present at `performance.now() ≈ 41s`, past the artificial hold. Reverted the temporary 15000ms back to the real 1000ms, recompiled, and did one more real-timing reload: renders correctly (no loader stuck, no layout shift, node 12 green, edges/siblings unaffected), zero console errors. Restored the browser profile's original `localStorage` (12/24 real progress, "seen" matching "completed") afterward so this test didn't leave that profile's saved state altered.
- Not yet live-checked: the `ElectricLoader` embed itself (the `mounted && reveal !== null` gap was too brief to actually catch mid-flight in this session — confirmed it doesn't get stuck and doesn't break layout, but never visually caught it *showing*); the edge-draw-in/node-light-in beats' relative sequencing at real (non-slowed) timing, since this session only confirmed stage 1 in isolation.

**Phase 6 of `docs/Agentic_AI.md`: the five agentic scenarios — built, verified against the real engine, and cross-tested with Phase 5** — new
- Five new scenarios under `src/scenarios/`, following the existing "given + budget" model exactly (only the Client given, everything else the student's own design): `customerSupportAgent.ts` (tool-use + human-in-the-loop — a refund policy only a real `human_in_loop_gate` enforces), `codingAgent.ts` (Reflection retry loop + a second, harder gate before the one deploy action — deny-first Max Iterations checked via a real Total Iterations constraint), `researchAssistant.ts` (Retriever mode choice is the whole point — Pipeline structurally fails a relationship-query-heavy traffic mix GraphRAG/Adaptive clear comfortably), `autonomousOpsAgent.ts` (Orchestrator-Worker + Multi-Agent Collaboration composed in one build — two peer Agent Orchestrators, one gated Tool Call), `costConstrainedEdgeAssistant.ts` (a budget + quality floor no single lever clears alone — needs the full `model_router` cascade + quantization + edge + semantic Cache composition). All five registered in `scenarios/index.ts`; 6 new `ScenarioTopic`s added (`tool-use`, `human-in-the-loop`, `retrieval`, `multi-agent-orchestration`, `model-routing`, `agentic-system-design`), surfaced automatically in `/problems`'s topic filter (derived from `SCENARIO_TOPIC_LABEL`, no hardcoded list to update).
- New `Scenario.requiresGatedToolCalls` flag + `hasUnguardedIrreversibleAction` in `architectureValidation.ts` (same barrier-BFS shape as the existing `hasUnguardedBackendAccess`, barrier = `human_in_loop_gate`, target = `tool_call`) — the reversibility-weighted-risk architecture gate `docs/Agentic_AI.md` Part 4 calls for, wired into `scenarioScoring.ts` alongside the existing backend-access gate.
- `ScenarioMetric` widened to include `totalIterations`/`guardrailRejectionRate` (Phase 5's new fields), so a scenario's own constraints can check them directly — `codingAgent`'s Total Iterations constraint is the real, working example.
- **Two real bugs found and fixed by actually composing Phase 5 with Phase 6 (not found any other way — both surfaced only once a real multi-node agentic scenario was scored end-to-end)**:
  1. `guardrailRejectionRate` (Phase 5) was silently broken for any `guardrail_validator` not directly Client-adjacent — its real, documented placement (inside an Agent Orchestrator's retry loop) — because it read `errorCount`/`REQUEST_FAILED`, which only the client-adjacent entity along the response path ever emits. Fixed with a new self-addressed `GUARDRAIL_EVALUATED` marker event (same pattern as `CONTEXT_COMPACTED`), emitted on every `evaluate()` call, pass or fail — `MetricsCollector.ts` now reads these directly. `GuardrailValidator.test.ts`/`MetricsCollector.test.ts` both updated; a new regression test specifically reproduces the original bug shape.
  2. `hasUnguardedBackendAccess` (from an earlier phase) didn't know agentic primitives exist — it flagged `Client → Cache → Model Router → LLM Call` (§2.9's own documented semantic-caching composition) as the same "raw storage exposed to the Client" exploit it was built to catch for the HLD domain, since its only recognized barrier was `api`. Fixed with a `cache`-specific carve-out: a Cache reachable directly from a Client is only a violation if nothing legitimate (an `api` *or* an agentic service-layer primitive) sits immediately behind it — verified this doesn't regress any of the 8 existing `architectureValidation.test.ts` cases (all still pass unchanged).
- Verified programmatically: all 5 scenarios' `optimalSolution`s tuned against the real engine (a throwaway script, same discipline `movieTicketBooking.ts`'s own header documents — deleted after use) — real numbers, not asserted ones, including a genuine surprise (every entity here rolls its own reliability dials on both the request *and* response leg, so a configured 10% Denial Rate is really ~19% in practice — this shaped every scenario's tuning, not just customerSupportAgent's). New `src/scenarios/__tests__/agenticScenarios.test.ts` (24 tests): each scenario's reference solution scored against the real `scoreScenario`/`evaluateScenario` pipeline; the architecture gate proven to actually reject an ungated build (not just asserted); the budget gate proven against a real always-LLM baseline that blows it by ~40x; Total Iterations proven to discriminate a deny-first build from a lenient one using two *real* runs, not fixtures; the Phase 5 reliability engine (`computeReliabilityScore`) run directly against these scenarios' configs, including a test proving a tuned build is measurably more reliable across 8 seeds than the same architecture with every dial left at its default.
- `tsc --noEmit`, full `vitest run` (877 tests), and `npm run lint` all pass clean.
- Not yet live-checked (nothing here has been opened in a browser yet): all 5 new scenarios loading correctly from the header Scenarios dropdown on `/workshop` (locked Client field(s), correct starting canvas); the 6 new topic filter chips on `/problems` (do they read sensibly next to the existing HLD ones, does the catalogue visually distinguish or mix the two domains reasonably); each scenario's "Before You Run"/capacity-estimate prompt and worked answer; revealing each `optimalSolution` reference build on canvas and confirming it renders/runs/scores exactly as the regression tests already prove headlessly; the reflection copy's `{{totalIterations}}`/`{{guardrailRejectionRate}}` placeholders actually resolving correctly once wired into a live run (only checked programmatically via `resolveReflection` so far, not eyeballed in the Inspector); `codingAgent`'s and `autonomousOpsAgent`'s multi-node topologies being genuinely buildable by hand on the canvas (drag-connect wasn't exercised for these specific shapes this pass — see the Phase 4 entry above's own note on this environment's drag-connect limitation).

**Phase 5 of `docs/Agentic_AI.md`: metrics (iteration count, guardrail rejection rate) + pass^k reliability score** — new
- `MetricsSnapshot` gained two new top-level aggregate fields, both purely event-derived (no new EventType, no entity-type coupling needed in `MetricsCollector.ts` beyond two new optional id-list params, same precedent `clientIds` already set): `totalIterations` (sum, across every Agent Orchestrator, of dispatches beyond one-per-session — a session's own admission accounts for one, every dispatch past that is a retry or a fan-out worker; directly surfaces failure modes #5/#6) and `guardrailRejectionRate` (aggregate rejections/checks across every Guardrail Validator). Both `undefined` when the run has no entities of that type — same "absent means not applicable" convention every other optional metric here already follows. `SimulationResult` gained `agentOrchestratorIds`/`guardrailValidatorIds` (mirrors `clientIds`'s existing shape exactly), threaded from `Simulator.ts` through to `PlaybackController.ts`'s repeated `getMetrics()` calls during playback. `SimulationResultsPanel.tsx` gained two new conditionally-rendered Stats ("Iterations", "Guardrail rejections") alongside the existing cost Stat.
- New `src/simulation/engine/reliabilityScore.ts` — `docs/Agentic_AI.md` §2.5's pass^k reliability score, explicitly named this pillar's one genuinely novel contribution. `computeReliabilityScore(config, options)` re-runs the identical `SimulationConfig` across N consecutive seeds (default 10, starting from the config's own seed — so the result is itself reproducible) and reports the fraction whose `successRate` clears a threshold (default 95%) — the same "re-run and compare" shape `compareArchitectures.ts` already established for entity-splice comparisons, just varying the seed instead of the topology. Deliberately reinterprets τ-bench's literal "solved the same task every try" for this engine's many-concurrent-requests-per-run traffic model as "cleared a success-rate bar," with the bar a caller-supplied parameter, not something the module asserts.
- New `ReliabilityPanel.tsx` — a toggleable drawer (new `reliabilityPanelOpen`/`isComputingReliability`/`reliabilityScoreError`/`reliabilityScoreResult` store fields, new `runReliabilityScore()` action reusing a newly-factored `buildCurrentSimulationConfig()` helper that `runSimulation()` itself now also calls) showing pass/fail count, a per-seed dot grid (hover for exact success rate), and a "Run Check" trigger — universal, not agentic-domain-gated, since reliability-under-repeated-seeds is a real question for any architecture this app can build. Mounted next to `TracePanel.tsx` in the bottom bar; the two drawers share one fixed position and are now mutually exclusive (opening one closes the other) since they'd otherwise render on top of each other.
- **Deliberate deviation from the doc, flagged rather than silently done**: the roadmap line says "cost... added to `MetricsSnapshot`." Not done — `costEngine.ts`'s pricing already depends on `ArchitectureNode` (a React Flow type), a real framework dependency the pure `src/simulation/` engine has never carried and that pulling cost computation into `MetricsCollector.ts` would require either duplicating or threading through in a way nothing else here does. Cost is already fully surfaced in the UI (`SimulationResultsPanel`'s "Est. monthly cost" Stat, `InspectorPanel`'s per-entity breakdown) via the existing, working, tested `costEngine.ts` mechanism from Phase 1 onward — this satisfies the user-facing requirement without violating the engine's own architectural boundary. Worth revisiting only if a future need specifically requires cost inside a *pure* (non-UI) metrics computation.
- Verified programmatically: 6 new tests in `MetricsCollector.test.ts` (totalIterations undefined/zero/counting-retries/summing-across-orchestrators; guardrailRejectionRate undefined/zero/dividing-by-total-checks) + 6 new tests in `reliabilityScore.test.ts` (reliable architecture passes every seed, unreliable passes none, determinism across repeated calls, custom seed count, custom threshold, seed-sequence sampling).
- `tsc --noEmit`, full `vitest run` (889 tests, 12 new this phase), and `npm run lint` all pass clean.
- Not yet live-checked (nothing here has been opened in a browser yet): the new "Iterations"/"Guardrail rejections" Stats in the results bar — only render with the right entity types on canvas, don't crowd/overflow the existing Stat row at a normal viewport width; the Reliability toggle button and its drawer — the "Run Check" button, the spinner while computing, the pass/fail count color coding (green at 100%, amber ≥50%, red below), the per-seed dot grid's hover tooltips, the mutual-exclusivity with the Trace drawer (opening one while the other is open should close it, not stack them); running a reliability check against a genuinely flaky architecture (e.g. an `llm_call` with a high `hallucinationRate`) and confirming the pass count reads meaningfully lower than a reliable one on the same canvas.

**New `/interview-questions` tab (company-tagged, sourced interview questions)** — new
- New route `src/app/interview-questions/page.tsx`, mirroring `/problems`'s filter-list pattern (search + Company/Category chip filters, `Badge`/`Input` reuse). New content module `src/content/interviewQuestions/` (`types.ts`, `companies/google.ts`, `index.ts` aggregator) — see `docs/interview_exp.md` for the research process. Pruned from an initial 34 questions down to **8**: only prompts with real specificity (numbers, constraints, a concrete narrative) ship to this practice UI — a bare "Design a rate limiter." reads as a topic label, not something to actually answer, so those stayed research-only in the tracker doc. Nav links added: home page header ("Interviews", `Building2` icon) and `/problems` header (same, `sm:` and up only).
- Each question also links through to a detail page (`/interview-questions/[id]`) with a free-text `AnswerWorkspace` (autosaves to `localStorage` via `interviewAnswerProgress.ts`, mirrors `problemProgress.ts`'s pattern) and a "Mark as answered" toggle. List cards show a per-question status badge (Unattempted/Drafted/Answered) and an aggregate counter.
- Not yet live-checked: the page's actual rendering — chip filter layout/wrapping at the 5-badge-per-card width (Company/Category/Level/Confidence/Status badges plus the title line), the "Verified"/"Reported" confidence badge color read (success/warning) actually looks right against real content, long prompts (e.g. the translation-service and pharmacy-shop questions, both multi-sentence) don't break the card layout, the Source link + reported-date + note row wrapping on narrow widths, the detail page's textarea/autosave "Saving…"→"Saved" transition and the Mark-as-answered button, and that the two new header nav links render correctly in both light/dark/night-ops themes.

**Optimal-answer outlines added to all 8 shipped Google interview questions** — new
- `InterviewQuestion` gained an optional `optimalAnswer` (`types.ts`): clarifyingQuestions, requirements, approach, keyPoints, tradeoffs, followUps, relatedLinks — all 8 Google questions in `google.ts` now have one, authored (not sourced — the prompt/context stay cited, the answer is this app's own content, the file's doc comment says so explicitly). Rendered via new `OptimalAnswerSection.tsx` behind a native `<details>` disclosure ("Show the optimal answer outline"), same reveal-after-attempt pattern as `/foundations/[slug]`'s exercise section, placed after the `AnswerWorkspace` textarea on `/interview-questions/[id]`. List page cards get an "Answer guide" badge (Lightbulb icon) when one exists. Content-validator test extended to check every `optimalAnswer`'s core fields are non-empty and every `relatedLinks` href looks real. `tsc`/full `vitest` (877 tests)/`lint` all pass.
- Not yet live-checked: the `<details>` disclosure open/close interaction and its arrow/summary styling in a real browser; the nested subsections (Clarify first / Requirements / Approach / Key design points / Trade-offs / Follow-ups / Go deeper) reading cleanly at the reduced font sizes, especially the "Go deeper" pill-link row wrapping; the "Answer guide" badge's Lightbulb icon rendering inside a `Badge` (first time that component's held an icon+text together); confirm the `/foundations/[slug]` and `/lld/[slug]` links inside answers (message-queues, kafka-deep-dive, database-sharding, redis-deep-dive, rate-limiting, consistent-hashing, caching, cdn, circuit-breakers, load-balancers, oop-fundamentals, solid-principles, behavioral-patterns) and the two `/workshop?scenario=` deep links (flash-sale, movie-ticket-booking) actually resolve rather than 404.

**Phase 4 of `docs/Agentic_AI.md`: failure catalog + `human_in_loop_gate` + OTel trace panel** — mostly CONFIRMED live, one real bug found and fixed, one class of item genuinely blocked (not a code issue)
- All ten §1.7 failure modes traceable to a named, reproducible entry in `entityDeepDive.ts`; new `human_in_loop_gate` entity (approval branch + latency injection); new OTel-shaped trace panel (§2.4). Full implementation detail unchanged from the original entry — see git history / `Agentic_AI.md` for the design. `compromised`/`compromiseReason` metadata mechanic (failure modes #4/#9/#10) and `compromiseCatchRate` on `guardrail_validator` as originally described.
- **Bug found live, fixed**: `TracePanel.tsx`'s drawer was `fixed inset-x-0 bottom-24`, spanning the full viewport width — it rendered on top of the always-mounted `w-80` `InspectorPanel`, covering its lower config fields whenever both were open. Fixed to `left-0 right-80` so it stops cleanly before the Inspector. Re-verified live after the fix: clear gap before the Inspector panel, no overlap.
- **Live-verified on `/workshop`**: the "Agentic AI" sidebar section now lists 8 items ending in Human-in-the-Loop Gate with its `UserCheck` icon; adding Client/LLM Call/Human-in-the-Loop Gate/Tool Call nodes all render correctly (icon, eyebrow label, config summary line); selecting the Human-in-the-Loop Gate node shows all 5 Inspector fields (Approval Latency 4000, Approval Latency Jitter 1500, Denial Rate 10%, Max Concurrent 5, Max Queue Length 30) plus the "How It Works" section rendering the exact `entityEducation.ts` copy; selecting a Tool Call node shows both new dials (Hallucinated Invocation Rate 2%, Silent Failure Rate 3%) in the right position (after Schema Failure Rate); the Trace toggle button appears in the bottom bar only once an agentic entity is on canvas, opens/closes a drawer with the correct "Run a simulation to see traces here" empty state and "TRACE — 0 OF 0 REQUESTS" header. Zero console errors/exceptions across the whole session.
- **Live-verified across all 7 touched `/entities/[slug]` deep-dive pages**: `/entities/human-in-loop-gate` (full page, both new failure modes with reproduce steps + "Watch for" callouts); `/entities/tool-call` (both new failure modes — "The Tool That Was Never Registered" #2, "The Silent Failure" #4 — alongside the 2 pre-existing ones, 4 total, correct order); `/entities/llm-call` ("Direct Prompt Injection" as the new #1 entry); `/entities/agent-orchestrator` ("Agent Paralysis" and "Error Cascade" as new #3/#4 entries, alongside the 2 pre-existing); `/entities/retriever` ("Indirect Prompt Injection" as new #3); `/entities/guardrail-validator` ("Catching (or Missing) a Compromised Response" as new #3, confirmed via accessibility-tree query); `/entities/memory-context-store` ("Context/Spec Drift" as new #3, confirmed via accessibility-tree query). All render with correct §1.7 failure-mode-number citations, numbered reproduce steps, and "SIMULATED" badges.
- **Genuinely blocked, not a code issue**: could not drag-connect nodes on canvas in this automation session (Client→LLM Call, etc.) — ~7 attempts at increasing precision (zoomed canvas, pixel-precise handle targeting via zoomed screenshots, well-separated nodes) all either selected/moved the node or panned the canvas instead of drawing an edge. This is the same "flaky synthetic-pointer-event" limitation already documented multiple times elsewhere in this file for React Flow's tiny connector handles — not new, not caused by this phase's changes. As a result, could not visually confirm: a fully wired Client → LLM Call → Human-in-the-Loop Gate → Tool Call chain; the Trace drawer populated with a real, non-empty span tree (nesting/indentation, attribute rows, the compromised-vs-failed badge distinction, scrolling with many traces) — this exact scenario (nested spans, only-the-originating-span-shows-error, compromised-trace flagging) is instead covered by `otelTraceFormatter.test.ts`'s 4 passing tests, run directly against the engine's real output; Batman Mode's `WeaponWheel` radial picker with the catalog now at 20 items (not opened this pass). A future session with reliable pointer-event support (or a real mouse) should confirm these directly.

**Phase 3.5 of `docs/Agentic_AI.md`: `model_router` (the SLM/LLM cascade) + prompt/semantic caching economics** — new
- New `EntityType` `model_router` (`ModelRouter.ts`) — §2.8's fourth policy-bearing entity. Four modes (Always-LLM, Always-SLM, Confidence-cascade, Cost-optimized-cascade) routing between two downstream `llm_call` nodes by **wiring order**, not a per-target config map: `downstream[0]` is always the SLM/cheap target, `downstream[1]` the LLM/escalation target — the same convention Agent Orchestrator's Sequential steps already use, so this needed no new Inspector UI the way `ReverseProxy`'s target-id-keyed `routes` does. Cost-optimized-cascade's "budget-aware threshold" is modeled as a running request-fraction cap (`maxEscalationRate`) rather than reimplementing `costEngine.ts`'s dollar pricing inside the entity — confirmed by test that it never exceeds its own cap however much traffic wants to escalate, and that it keeps a higher SLM share than plain Confidence-cascade on identical heavy-escalation traffic. Structurally LoadBalancer.ts's "no capacity of its own" shape (instant routing decision, then forward), not the bounded admit-queue-reject shape most other entities use. No new EventType/metric — the SLM-vs-LLM split is already derived for free from the existing `routingDistribution` mechanism; added to `ComponentNode.tsx`'s/`InspectorPanel.tsx`'s `ROUTING_ENTITY_TYPES` gating so the Dispatch Distribution section (relabeled "SLM / LLM Split" for this entity) renders with zero new UI code. Registered in `Simulator.ts`, `entityCatalog.ts` (new `Split` icon), `entityConfigSchema.ts` (5 fields), `entityEducation.ts`, `entityDeepDive.ts` (new `/entities/model-router` page), `tutorialPlanner.ts`. Not priced in `costEngine.ts` itself, by design — its cost impact is entirely a function of which already-priced `llm_call` node it routes to.
- Caching economics (§2.9), zero new entity types as the doc specifies: `Cache.ts` gained a `cachingMode` ("exact"/"semantic") + `semanticHitRate` config pair — semantic mode is *additive* on top of exact key matching (an identical-key repeat still hits for free either way), rolled only on an otherwise-missed, non-phantom request, scaled by how full the store currently is (`CacheStore.ts` gained a small `size` getter for this). `llm_call` gained a cost-only `promptCacheHitRate` field (same "lives in `entityConfigSchema.ts`/`costEngine.ts` only, not the entity class" precedent `tool_call`'s `perCallCostUsd` already set) — `costEngine.ts`'s `llmCallPricing` blends it toward the ~1/10th-price discount §1.13 describes.
- Still deliberately deferred (same note carried forward from Phase 3's three entities): the §2.8-mandated "comparison panel against always-LLM" for `model_router` — it gets a real, if partial, substitute for free (the live Dispatch Distribution section), but not the dedicated re-run-and-compare view the roadmap describes. `inference-and-serving` lesson content also stays unwritten here, on purpose — same precedent Phase 1 already set (llm_call's tier/quantization/deployment landed without pulling that lesson category forward too; it lands as its own dedicated pass, not opportunistically per config landing).
- Verified programmatically: 7 new `ModelRouter` tests (determinism, both baselines routing 100% to their fixed target, confidence-driven escalation share, the cost-optimized budget cap's own two behaviors, graceful single-downstream degradation) + 4 new `Cache` semantic-caching tests (higher hit rate than exact mode on identical traffic, converges back to exact mode as the rate approaches 0, never fires on a phantom key, determinism) + 1 new `costEngine` test confirming `promptCacheHitRate` at 1.0 discounts usage cost to almost exactly 1/10th of the undiscounted rate.
- `tsc --noEmit`, full `vitest run` (784 tests, 12 new), and `npm run lint` all pass clean.
- Not yet live-checked (nothing here has been opened in a browser yet): dragging a Model Router onto `/workshop`'s canvas from the "Agentic AI" sidebar section (now 7 items) and wiring it to two LLM Calls in order; the node card's compact Mode summary; the Inspector's Mode select and the 4 cascade-specific fields (confirm fields read as clearly scoped to specific modes, not confusingly "always active"); the Inspector's "SLM / LLM Split" distribution section actually renders and updates correctly for a Model Router (it reuses `LoadBalancerDistributionSection` verbatim, never rendered against this entity type before); the new `/entities/model-router` deep-dive page; a Cache node's new "Caching Mode"/"Semantic Hit Rate" fields (confirm Semantic Hit Rate's field only reads as meaningfully active when Caching Mode is Semantic); an `llm_call` node's new "Prompt Cache Hit Rate" field and that the Cost panel/node-card cost readout visibly drops as it's raised; Batman Mode's `WeaponWheel` radial picker with the catalog now at 19 items.

**Phase 3 of `docs/Agentic_AI.md` (part 3 of 3): `guardrail_validator` lands — Reflection and Evaluator-Optimizer are now buildable** — new
- New `EntityType` `guardrail_validator` (`GuardrailValidator.ts`) — the "Evaluator" primitive, completing Phase 3's three new entities. Two `mode`s: Gate (binary pass/fail — Reflection's shape, `rejectionRate`) and Scorer (graded score vs. `scoreThreshold` — Evaluator-Optimizer's shape). Neither mode implements its own retry loop: wired downstream of an `llm_call` inside a *Sequential* `agent_orchestrator`, a failure here propagates back through the `llm_call` to the orchestrator's own existing iteration-capped retry (Planning's mechanism, reused entirely as-is) — confirmed by a real composition test through all three entities together, not just asserted. Scorer mode's `verificationMethod` (execution vs. judge) makes Evaluator-Optimizer's own "real risk" section literally simulatable: under `judge`, a new per-`requestId` attempt counter adds `judgeDriftPerAttempt` to the score on every retry of the *same* logical request — the underlying `scoreMean` never moves, but the effective pass bar gets easier purely from repetition ("self-corrects into a more elaborate hallucination," made measurable); `execution` never drifts. No new EventType — Gate/Scorer failures surface as two distinct `REQUEST_FAILED` reasons (`guardrail_rejected` vs `guardrail_score_below_threshold`) on the standard event. Registered in `Simulator.ts`, `entityCatalog.ts` (new `ShieldCheck` icon), `entityConfigSchema.ts` (12 fields), `entityEducation.ts`, `entityDeepDive.ts` (new `/entities/guardrail-validator` page), `tutorialPlanner.ts`. Not priced in `costEngine.ts` yet — same deliberate precedent `agent_orchestrator`/`memory_context_store` already set.
- Content: lessons 09 (Reflection) and 10 (Evaluator-Optimizer) rewritten from their earlier honest "not buildable yet" framing to real, buildable "build it" sections (same shape as the other 4 pattern lessons) now that the primitive exists — both `architecture` blocks' `guardrail_validator` node updated from a `sublabel`-only placeholder to a real `entityType`-tagged node. All 6 canonical patterns are now fully buildable. `src/content/agentic/index.ts`'s own docblock updated to match.
- Verified programmatically: 8 new `GuardrailValidator` tests, including the composition test that actually builds Client → Agent Orchestrator (Sequential) → LLM Call → Guardrail Validator and confirms a rejecting Gate reproduces the exact `iteration_limit_exceeded` signature Agent Orchestrator's own infinite-loop test checks for, and a Judge-vs-Execution comparison confirming Judge-based verification's success rate comes back measurably higher than Execution-based's on identical seed/traffic/threshold — the drift risk, actually measured, not just asserted in a lesson.
- `tsc --noEmit`, full `vitest run` (772 tests, 8 new), and `npm run lint` all pass clean, aside from one pre-existing, unrelated lint error in `src/components/foundations/FoundationsMap.tsx` (untouched by this phase, carried over from the same earlier in-progress work its own still-pending entry above already covers).
- Still deliberately deferred (unchanged from parts 1-2's note, now covering all three of Phase 3's new entities): a dedicated Inspector section for any of the three entities' live metrics, and the §2.2-mandated comparison panel for `retriever`/`memory_context_store` (`guardrail_validator` itself isn't listed as policy-bearing in §2.2, so it doesn't need one). Phase 3 of `docs/Agentic_AI.md`'s roadmap is now content/engine-complete; this UI work is the natural first item whenever Phase 3.5 or a dedicated comparison-panel pass starts.
- Not yet live-checked (nothing here has been opened in a browser yet): dragging a Guardrail Validator onto `/workshop`'s canvas from the "Agentic AI" sidebar section (now 6 items — llm_call, tool_call, agent_orchestrator, memory_context_store, retriever, guardrail_validator — out of 18 catalog entries total); the node card's compact config summary (Mode via `shortLabel`); the Inspector's Validation Mode/Verification Method selects and the other 10 numeric/percent fields; building the full Reflection and Evaluator-Optimizer topologies from their now-rewritten `/agentic/reflection-pattern` and `/agentic/evaluator-optimizer-pattern` lesson pages and confirming the retry loop and Judge Drift comparison both read correctly on canvas; the new `/entities/guardrail-validator` deep-dive page; Batman Mode's `WeaponWheel` radial picker with the catalog now at 18 items.

**Phase 3 of `docs/Agentic_AI.md` (part 2 of 3): `retriever` lands on the Workshop canvas** — new
- New `EntityType` `retriever` (`Retriever.ts`) — `docs/Agentic_AI.md` §2.2's third policy-bearing primitive. Four selectable `mode`s, each a real, distinguishable trade-off rather than one hardcoded default: Pipeline (single-shot top-k, the 2023-era baseline); Agentic (a bounded retrieve-critique-reretrieve loop up to Max Retrieval Attempts — an exhausted loop is modeled as *more* likely to return a confidently-wrong `retrieval_hallucination` than an honest `retrieval_miss`, the real risk §1.5's research names); GraphRAG (graph traversal instead of chunk similarity — a genuine cost even on ordinary queries, but wins decisively on relationship-shaped ones); Adaptive (delegates to Pipeline's or GraphRAG's own real behavior per query, not a separate approximation). New shared traffic mechanism, same shape as Missing Key Rate → Cache's Negative Caching: Client gained a `relationshipQueryRate` config (`assignRequestRelationshipQuery` in `TrafficGenerator.ts`, purely additive at rate 0 — every existing scenario/test keeps drawing the identical RNG sequence), threaded through `RequestLifecycleMetadata.requiresRelationshipTraversal` via `Client.ts`, only ever read by `Retriever.ts`. Agentic's whole per-request attempt loop is resolved synchronously inside `beginProcessing` (documented as a legitimate, deliberate variation on the usual roll-at-completion shape, since its total duration depends on how many attempts its own rolls end up taking). No new EventType — unlike `memory_context_store`, every metric a future comparison panel needs (success rate, latency, cost) is already derivable from standard events plus `costEngine.ts`'s new `retrieverPricing` (usage-only, per-mode cost multiplier: Agentic ~1.8x, GraphRAG 2.5x, Adaptive a blended 1.3x). Registered in `Simulator.ts`, `entityCatalog.ts` (new `Search` icon), `entityConfigSchema.ts` (13 fields — most explicitly scoped "only meaningful under X mode" in their `impact` text), `entityEducation.ts`, `entityDeepDive.ts` (new `/entities/retriever` page), `tutorialPlanner.ts`.
- Still deliberately deferred (unchanged from part 1's note, now covering both new entities): a dedicated Inspector section for either entity's live metrics, and the §2.2-mandated comparison panel (re-run the same seed across all modes and show the trade-off) — both will share one new comparison mechanism once built, rather than twice.
- Verified programmatically: 9 new `Retriever` tests cover determinism, the no-downstream safe-ending case, `retrieval_miss` under Pipeline, `retrieval_hallucination` distinct from a plain miss under an exhausted Agentic budget, Relationship Penalty making Pipeline measurably worse on relationship-shaped traffic, Relationship Bonus making GraphRAG measurably win on that same traffic where Pipeline doesn't, Adaptive tracking GraphRAG's advantage (not Pipeline's weakness) on relationship queries, Agentic's higher average latency than Pipeline when queries need multiple attempts, and queue/reject behavior under overload.
- `tsc --noEmit`, full `vitest run` (764 tests, 9 new), and `npm run lint` all pass clean.
- Not yet live-checked (nothing here has been opened in a browser yet): dragging a Retriever onto `/workshop`'s canvas from the "Agentic AI" sidebar section (now 5 items); the node card's compact config summary line (Mode via `shortLabel`) at the node's fixed width; the Inspector's Mode select and its 12 numeric/percent fields render and update correctly, including benchmark popovers, and that mode-irrelevant fields (e.g. Agentic's Max Retrieval Attempts while Mode is Pipeline) don't read as confusingly "active" with no visible effect; the Client node's new "Relationship Query Rate" field renders and updates correctly; the new `/entities/retriever` deep-dive page renders every section correctly (tables, both failure-mode reproduce-step cards) in both the sidebar-triggered Inspector link and direct navigation; Batman Mode's `WeaponWheel` radial picker with the catalog now at 16 items.

**Phase 3 of `docs/Agentic_AI.md` (part 1 of 3): `memory_context_store` lands on the Workshop canvas** — new
- New `EntityType` `memory_context_store` (`MemoryContextStore.ts`) — `docs/Agentic_AI.md` §2.2's second policy-bearing primitive (`retriever` is the third, still to come). Genuinely new state shape for this engine: rather than one-admission-one-hop, it holds *running session state* (`contextSizeTokens`, `criticalInfoIntact`, `compactionCount`) that persists and accumulates across every turn it ever handles for the whole simulation run — every request through it is modeled as one more turn in a single long-running agent session, a deliberate simplification documented in the class doc. Three selectable `compactionPolicy` values (None/Summarization/Scratchpad, same "ship comparable options, not one hardcoded default" precedent as Cache's eviction policies) trigger once the running context exceeds `capacityTokens`: None truncates for free but permanently loses critical info on first overflow; Summarization pays latency per compaction and mostly preserves it; Scratchpad pays a sustained per-turn read cost after its first compaction but almost never loses it. Once critical info is lost, every turn independently rolls `driftFailureRate` and can fail with reason `context_truncation` — `docs/Agentic_AI.md` §2.3's failure mode #3, made observable. New self-addressed `CONTEXT_COMPACTED` event (mirrors `CACHE_HIT`/`CIRCUIT_OPENED`'s pattern — destination null, diagnostic marker only) feeds a new `MemoryContextMetrics` in `MetricsCollector.ts`/`EntityMetrics`. Registered in `Simulator.ts`, `entityCatalog.ts` (new `History` icon), `entityConfigSchema.ts` (9 config fields), `entityEducation.ts`, `entityDeepDive.ts` (new `/entities/memory-context-store` page), `tutorialPlanner.ts`. Not priced in `costEngine.ts` yet — same deliberate omission precedent `agent_orchestrator` already set.
- Deliberately deferred to the next part of this phase (alongside `retriever`, landing together since both need it): a dedicated Inspector "Memory Context" section surfacing compaction count/critical-info-intact/current size (the node currently only shows the generic Configuration/Live Metrics `CollapsibleSection`, no bespoke section yet — same gap Cache had before its stampede-specific Inspector section landed), and the §2.2-mandated comparison panel (re-run the same seed across all three policies, show the trade-off). Both `retriever` and `memory_context_store` will share one new comparison mechanism once it lands, rather than building it twice.
- Verified programmatically: 8 new `MemoryContextStore` tests cover determinism, the no-downstream safe-ending case, no compaction when capacity is never reached, None's permanent critical-info loss on first overflow, `context_truncation` failures only appearing once info is lost, Scratchpad measurably preserving critical info where None doesn't (same seed/traffic), Scratchpad's sustained read cost making it measurably slower than None once its file exists, and queue/reject behavior under overload.
- `tsc --noEmit`, full `vitest run` (755 tests, 8 new), and `npm run lint` all pass clean.
- Not yet live-checked (nothing here has been opened in a browser yet): dragging a Memory / Context Store onto `/workshop`'s canvas from the "Agentic AI" sidebar section (now 4 items); the node card's compact config summary line (Compaction Policy via `shortLabel`) at the node's fixed width; the Inspector's Compaction Policy select and the 8 numeric/percent fields render and update correctly, including their benchmark popovers; the new `/entities/memory-context-store` deep-dive page renders every section correctly (tables, both failure-mode reproduce-step cards) in both the sidebar-triggered Inspector link and direct navigation; Batman Mode's `WeaponWheel` radial picker with the catalog now at 15 items.

**`/foundations` index replaced with a branching forest map (`FoundationsMap`)** — new, revised three times (orientation/width, then full-bleed/node shape, then a "make it look finished" polish pass) — partially live-checked in revision 3, below
- Reworked from a flat `grid sm:grid-cols-2 lg:grid-cols-3` of lesson cards into a scrollable, branching path (discussed with the user first, then planned/approved before implementing). Every `FoundationLesson` (`src/content/foundations/types.ts`) now carries a `prerequisites?: string[]` field (set on all 24 lessons in `src/content/foundations/lessons/*.ts`) forming a real DAG — Networking and Databases run as separate branches converging into Vertical vs. Horizontal Scaling, which fans out into Load Balancers/Caching/Message Queues, funneling into a 5-parent Estimation-and-Interview-Framework capstone. New: `src/lib/foundationsProgress.ts` (localStorage-backed completed-slugs list, same cache+listeners+`useSyncExternalStore` convention as `problemProgress.ts`/`lockInMode.ts`, plus the pure `getLessonStatus` locked/available/completed derivation), `src/content/foundations/mapLayout.ts` (hand-placed percent x/y per lesson), `src/components/foundations/{MapNode,MapEdges,FoundationsMap,MarkCompleteButton}.tsx`. `/foundations/[slug]/page.tsx` gained a `MarkCompleteButton` next to the prev/next nav (hidden during an active Batman Mode lock-in run, same as prev/next, since `LockInChapterAction.advance()` now also calls `markLessonComplete` itself). Node/edge coloring reuses the existing `BoxTone` vocabulary (`neutral`=locked, `signal`=available, `healthy`=completed) rather than new colors.
- **Revision 1 (user feedback, before any live check)**: the first pass was a tall bottom-left → top-right portrait layout constrained to the hero's `max-w-6xl` reading width — reported back as "looks terrible." Reworked to a wide, horizontal, left-to-right layout: `mapLayout.ts`'s `x`/`y` swapped meaning (`x` = topological tier, root at `x:4` → capstone at `x:96`; `y` = the node's lane within a tier).
- **Revision 2 (user feedback, with reference images of game-world-map UIs — numbered circular stops on a path, full-bleed, no card chrome — before any live check)**: complaint was the map still sat inside a bordered box (`border border-border`, capped `min(75vh, 820px)` height) on an otherwise normal page. Now genuinely full-screen: `/foundations/page.tsx`'s `main` is a `h-screen flex-col` (was `min-h-screen`), the header stays a plain top bar, and the map fills every remaining pixel with the old hero copy now a small `pointer-events-none` HUD overlay in its top-left corner instead of a section pushing the map down the page. `FoundationsMap.tsx` dropped the border/card entirely, fills its parent (`h-full w-full`), and picked up `ArchitectureCanvas.tsx`'s own canvas visual language (radial vignette wash + a coarse dot-grid on the scrolling surface) instead of a flat panel fill. `MapNode.tsx` rebuilt from a small rectangular info-card to a round numbered badge (locked/available/completed via border+ring color, Lock/Check icon) with the title as a caption underneath, matching the reference images' "pin on a path" shape rather than a grid cell. `MapEdges.tsx` switched from straight lines to gently curved quadratic-bezier trails (alternating bow direction) for the "winding path" look in those references, still `viewBox="0 0 100 100"`/`preserveAspectRatio="none"` percent-space like `MapPath`. Canvas bumped to `2800×900`.
- **Revision 3 (user asked for a "top UI firm" design critique of the shipped map — "dull and incomplete with a lot of potential")**: agreed — a mostly-locked, mostly-empty canvas on a fresh visit read as gray circles floating in dead space. Added, all reusing existing tokens rather than new hues (per `primitives.tsx`'s "color means tone/status, not category" rule): `src/content/foundations/mapRegions.ts` + `MapRegions.tsx` (soft borderless `bg-text/[0.08] blur-3xl` zoning + margin labels — "Networking & the Web" / "Data Layer" / "Delivery & Speed" / "Distributed Resilience" — behind the 4 thematic clusters, bounding-box-computed from `mapLayout.ts` so it can't drift out of sync); `MapLegend.tsx` (a corner HUD readout: `N / 24 complete` + a locked/available/completed color key — nothing previously explained what the node colors meant); root/capstone "Start"/"Final challenge" callout tags on `MapNode.tsx` (computed from the DAG itself — the one node with no `prerequisites`, and the one node nothing else lists as a prerequisite — not hardcoded slugs), the capstone badge sized up (68px vs. 52px); `MapEdges.tsx` edges now vary stroke width by target status and a completed edge gets a soft `drop-shadow` glow, instead of every trail carrying identical visual weight regardless of how real it is yet; the canvas background gained a fine 18px line grid under the existing coarse dot grid (matching `ArchitectureCanvas.tsx`'s actual two-layer "major/minor gridline" pairing, not just its coarse layer); `/foundations/page.tsx`'s corner hero text gained a soft radial scrim behind it so it stays legible regardless of what the map auto-scrolls to underneath it, without reintroducing a hard-edged panel.
- Live-checked via screenshots this revision (dark theme, ~1516px viewport, both a fresh 0/24-progress load and a mid-progress 8/24 one found already saved in the test browser profile): full-screen with no page-level scrollbar; the region glow + labels visibly break up the canvas into readable territory; the root's glow ring + "Start" tag and the capstone's bigger badge + "Final challenge" tag both render correctly; the legend renders and its count matches the visible completed nodes; the completed trail's green glow and the available nodes' orange glow read clearly against locked/dim nodes; horizontal panning works; the fine+dot grid reads as texture, not noise; the hero scrim keeps the corner text legible.
- **Bug found and fixed (user asked to specifically re-check the auto-scroll flag from revision 3 on a clean load)**: confirmed real, root-caused, and fixed. Reproduced with real saved progress (8/24, via a genuine hard navigation, not a SPA transition) — `useFoundationsProgress()`'s `useSyncExternalStore` must render its *first* client pass matching the server snapshot (empty, since SSR has no `localStorage`) to avoid a hydration mismatch; the visible DOM (legend, node/edge colors) self-corrects to the real value a moment later, but `FoundationsMap`'s scroll-centering effect has `[]` deps, so it only ever fires once, off whichever render happened to commit first — on a returning visit, that's the stale empty one, so it locked onto the root every time regardless of real progress. Verified via direct `scrollLeft` inspection (not just eyeballing screenshots): before the fix it computed the root's position; after, it correctly computes the true frontier's (matched the hand-calculated expected `scrollLeft` for DNS — the actual next lesson — exactly). Fix (`FoundationsMap.tsx`): the effect now calls `getCompletedFoundationSlugs()` (a plain synchronous `localStorage` read, already exported from `foundationsProgress.ts`) directly instead of trusting the hook's value — effects never run during SSR, so this sidesteps the hydration-snapshot rule entirely and is correct from the very first fire, no race. Re-verified both the fresh (0/24, centers on root) and returning (8/24, centers on the true frontier) cases live after the fix; no regression.
- **Empty-state flash also fixed (user follow-up: "fix the empty-state flash on reload too")**: `FoundationsMap` now defers rendering `MapRegions`/`MapEdges`/every `MapNode`/`MapLegend` until after the component has mounted on the client (a `mounted` boolean), instead of painting them on the same first pass that must match SSR's empty snapshot. By the time `mounted` flips, `useFoundationsProgress()` has already settled to the real value, so the *first* time any of that content ever paints, it's already correct — no wrong-then-right flash, just a brief empty canvas (grid/vignette still visible) while progress loads. `mounted` itself is a second `useSyncExternalStore` call (no-op subscribe, snapshots `true`/`false` for client/server) rather than `useState`+`useEffect(() => setMounted(true), [])` — the latter trips `eslint-plugin-react-hooks`'s `set-state-in-effect` rule (calling `setState` synchronously inside an effect body); this way is the same "subscribe to an external fact" shape every other store in this file already uses, just with a fact that never changes twice.
- Live-verified the fix directly (not just theorized): restarted the dev server (had gone down mid-session), reproduced the exact repro from the bug above — a genuine hard navigation with real 8/24 progress already in `localStorage` — and screenshotted immediately on load, twice in a row. Both times the map painted already-correct (green completed trail, orange available DNS/HTTP, "8 / 24 complete" in the legend, correctly centered on the frontier) with no visible empty/locked flash at all. Re-checked the fresh 0/24 case too (root highlighted, centered, no regression).
- **Background grid removed (user: "the only thing which is annoying is the background... just some dotted random ugly looking thing")**: the fine 18px line grid + coarse 42px dot grid added in revision 3 (to match `ArchitectureCanvas.tsx`'s technical-canvas look) didn't land — reads as visual noise on a content/reading map rather than a drag-drop builder canvas. Removed both `backgroundImage`/`backgroundSize` layers from the inner canvas `<div>` entirely; the outer radial vignette and `MapRegions`' soft zoning glows are now the only background texture. Live-confirmed: clean smooth dark surface, no grid/dots, region glows and nodes read clearly against it.
- **Scrollbar hidden + title no longer overlaps chapters while scrolling (user follow-up)**: two related complaints. (1) The native horizontal scrollbar track under the map read as leftover browser chrome on what's meant to be an open canvas — new `.no-scrollbar` utility (`globals.css`: `scrollbar-width: none` + `::-webkit-scrollbar { display: none }`) on the scroll container hides it while wheel/trackpad/programmatic `scrollTo` all keep working. (2) The corner hero title was a viewport-fixed HUD overlay (from the earlier "keep it legible" scrim fix) — being fixed, it stayed put through every pan and ended up sitting over whatever node scrolled underneath it, exactly the "covers and overlaps some of the chapters" complaint. Fixed properly rather than patched again: `FoundationsMap` gained a `headerSlot` prop and now renders it *inside* the scrolling canvas at a fixed pixel spot near the root (clear of every node's position, verified against `mapLayout.ts`'s coordinates), not pinned to the viewport — so it scrolls away like any other part of the map once the reader pans past it. `/foundations/page.tsx` passes its title/tagline as `headerSlot` instead of rendering its own fixed overlay + scrim (both deleted, no longer needed).
- Live-verified both: scrollbar track is gone in a real screenshot while scroll still works (mouse-wheel panned the map in the same test); scrolled from the title's position (near root) out to a mid-progress frontier far to the right — title fully scrolled off-screen, no overlap with "Load Balancers"/"Caching"/"Message Queues" the way it did before; scrolled back to the start — title renders cleanly with no nearby node collision.
- **Region labels made prominent + node shape reverted to cards (two more user follow-ups)**: (1) "the distinction like (data layer, distributed resilience etc) is not visible, please make them more prominent" — went through two more passes on `MapRegions.tsx` in direct response to live feedback. First tried a dashed oval boundary + glow fill around each cluster (rejected: "remove the background circle"). Landed on dropping the shape entirely — no boundary, no glow, just the label itself made genuinely bold (`text-xs font-semibold text-text-muted`, up from `text-[10px]`/`text-subtle/70`) with a small leading tick-mark, so prominence comes from typography weight alone, nothing drawn over the canvas. (2) "I liked the square designs... now the circle just has a plain text, lets bring back the design" — `MapNode.tsx` reverted from the circular numbered badge back to the bordered rectangular card (corner number tag, title, minutes with a `Clock` icon, Lock/Check/available-dot per status) that revision 2 replaced; kept the `emphasis` (Start/Final challenge) tag from the circle era, now positioned above the card instead of above a badge. Confirmed via `AskUserQuestion` first (with ASCII previews) since this was a real fork — user picked "revert to rectangular cards" explicitly rather than "keep circles, add detail."
- Live-verified: cards render with all the expected detail (number, title, minutes, status icon) at every status; region labels ("DATA LAYER", "DISTRIBUTED RESILIENCE") read clearly bold with no shape/glow behind them; the `headerSlot` title still sits cleanly in empty space near the root with the new card layout; no scrollbar; horizontal panning still smooth.
- **Card "logo" brought back (user follow-up: "bring back the logo of each box")**: `MapNode.tsx` now shows each lesson's first `relatedEntitySlugs` entry as a small `ENTITY_CATALOG` icon next to the corner number tag — same icon shown on that entity's Workshop canvas card and `/entities/[slug]` hero (Load Balancer, Cache, Database, Kafka, etc. all get their real catalog glyph), colored by the card's own status tone (muted/signal/healthy) rather than a new color. Opt-in like `DiagramBox`'s own `icon` prop: lessons with no related entity (DNS, REST APIs, the estimation capstone, ...) render with no icon rather than a wrong/generic one. First implementation tripped a real `eslint-plugin-react-hooks` rule (`react-hooks/static-components` — "components created during render") by wrapping the `ENTITY_CATALOG.find(...)` lookup in a small named helper function; fixed by inlining the `.find()` call directly in the component body instead, matching every other `const Icon = someLookup` pattern already used throughout this codebase (`entities/[slug]/page.tsx`, `ComponentNode.tsx`, `InspectorPanel.tsx`, ...) — the rule's heuristic apparently trusts a direct member/array lookup but not the same lookup wrapped in a function call.
- Live-verified: icons render correctly per card (Load Balancer/Cache/Database/Kafka/etc. glyphs match their real catalog icons), colored per status, and lessons with no related entity still render cleanly with just the number (no placeholder icon).
- **Locked edges hidden by default + click-to-reveal + mount entrance animation (three-part user follow-up)**: (1) "Remove the thin path lines of the next chapters and just show the modules... when we reach and complete one module, then we can show the next path (with the current yellow configs)" — `FoundationsMap.tsx`'s edge list now skips any edge whose target is still `locked` entirely (previously every edge rendered, dim/dashed for locked targets) — the map now only ever draws a trail into a lesson that's actually `available`/`completed`, already in the existing signal/healthy styling ("the current yellow configs" needed no change, just needed to actually be the default rather than one tone among three always-visible ones). (2) "if a user clicks on a future module, then we can show him the path and the chapters need to complete before opening this (only the recent parent and not everything back)" — locked `MapNode`s are now `<button>`s (`aria-expanded`, no navigation — still locked) that toggle per-lesson membership in a new `revealed` `Set` state in `FoundationsMap`; a revealed lesson's edges render same as before the hide-by-default change (dim/dashed, from its own `prerequisites` only — the edge-generation loop was already per-direct-prerequisite with no multi-hop edges in this data model, so "immediate parent only" needed no extra restriction). (3) "when we come back to the learn page... lets create an animation of the path appearing and the dark entity lighting up" — new `.animate-node-light-in` (opacity+scale pop, `globals.css`) on `available` nodes and `.animate-edge-draw-in` (opacity fade) on edges pointing at them, both firing once on `FoundationsMap`'s existing `mounted` transition; `completed`/`locked` nodes and edges render statically (no motion) since they're not "just now" becoming reachable.
- **Real bug hit and fixed mid-implementation**: first attempt at the edge "path appearing" animation used the textbook SVG technique (`pathLength="1"` + animated `stroke-dashoffset`, so `stroke-dasharray` could use normalized 0–1 units). Live-checked and it rendered wrong — a repeating micro-dash pattern instead of one solid growing line. Inspected via `getComputedStyle` in the actual page rather than guessing: `stroke-dasharray` computed to a literal `1px`, not scaled by `pathLength` at all (likely an interaction with the same `<path>`'s `vector-effect: non-scaling-stroke`). Dropped the dasharray/pathLength technique entirely for a plain opacity fade instead — less literally "drawing," but doesn't depend on a fragile cross-property SVG interaction. Separately, the node entrance animation deliberately has no `animation-fill-mode: forwards` — `MapNode`'s `available` state also has a `hover:-translate-y-1` lift, and a `forwards`-held animation would permanently pin `transform` past the animation's end, shadowing that hover effect; confirmed unnecessary anyway since both keyframes' `to` state already matches the element's normal resting style, so the animation can just end and hand back to the normal cascade with no visible snap.
- Live-verified all three: with progress saved through lesson 12, only 3 real trails show by default (12→13/14/16, its now-`available` children) and every deeper locked lesson (Redis, Kafka, CDN, Consistent Hashing, Sharding, Replication, Rate Limiting, Circuit Breakers, the capstone) has no connecting line at all; clicking the locked "Redis Deep Dive" card revealed exactly one dim dashed trail from its real direct parent ("Caching") and nothing further back, clicking it again removed exactly that trail; queried the DOM directly (not just screenshots) to confirm `animate-node-light-in` is present on exactly the 3 currently-`available` nodes and no others; hovering an available card after its entrance animation finished still lifts/brightens correctly (the `forwards` conflict this was checking for doesn't happen).
- **Entrance animation slowed down + actually confirmed running (user follow-up: "check yourself the animation is not working... it should be triggered only when I open the learn screen... make it slow")**: did not just assume it was fine — checked with `element.getAnimations()` and `getComputedStyle` in the live page rather than eyeballing screenshots (a screenshot can't reliably catch a fast, already-mid-flight CSS animation, so this needed a real timing inspection). It genuinely was running (`playState: "running"`, correct 500ms duration) — the bug wasn't that it didn't fire, it's that 500ms with **no start delay** is too quick and starts *immediately* on mount, competing with the also-instant auto-scroll-to-frontier jump firing the same tick — easy to miss entirely on a real page load before a reader's eyes have even landed on the screen. Fixed by both slowing down and staggering: edges now start 200ms in and run 900ms, nodes start 700ms in (after the edge is mostly drawn) and run 900ms — "the path appears, then the destination lights up" as two visible beats, not one instant blip. Needed `animation-fill-mode: backwards` added to both (not `forwards` — still avoided, see the note above) so the delay period actually holds the *invisible* "from" state rather than showing the fully-lit end state until the delay elapses and then visibly popping backward into the animation's start.
- Live-verified the fix mechanically, not just by re-reading the CSS: on a fresh page load, queried `getAnimations()` on the animated elements and got back `delay: 200`, `duration: 900`, `fill: "backwards"` exactly as authored, confirmed again via `getComputedStyle` (`animationDelay: "0.2s"`, `animationDuration: "0.9s"`, `animationTimingFunction: "cubic-bezier(0.2, 0, 0, 1)"` — the intended emphasized easing, correctly resolved from the `--ease-emphasized` custom property despite `getAnimations()`'s own timing object misreporting it as `"linear"`, which turned out to be a quirk of that specific Web Animations API property, not a real bug — the computed style is the ground truth and it's correct).
- **Reveal rebuilt around a real progress diff, not "replay the frontier every visit" (user follow-up: mark 4/5 complete from their own lesson pages, come back to the map, and the reveal should tell that actual story — 4/5 turning green first, then the path opening, then the next one turning yellow)**: the previous pass animated whatever was *currently* `available` on every single mount, regardless of whether the reader had already seen that exact state before. Rebuilt as a genuine before/after diff. New `foundationsProgress.ts` key (`engineering-studio:foundations-map-seen`) snapshots completed slugs as of the *last* time the map was mounted (defaults to the current completed set — not `[]` — the first time it's ever read, so this doesn't spuriously animate a whole existing history as "new" the moment this ships). On mount, `FoundationsMap` diffs current-vs-seen into `justCompleted` (finished since last visit) and `justUnlocked` (went locked→available as a result), then immediately re-persists "seen" = current so the same diff can't replay on a later visit. Three staged beats, `justCompleted`/`justUnlocked` driving `MapNode`/`MapEdges` instead of a blanket `status === "available"` check: (1) 0–700ms — a `justCompleted` card renders as if still `available` for one tick, then flips to its real `completed` classes with `transition-colors duration-[700ms]` doing the actual color animating (a real CSS transition between two genuinely different Tailwind states, not a hand-authored two-color keyframe) — "yellow becomes green" for real; (2) 700ms–1.6s — the edge into whatever that unlocked fades in (`animate-edge-draw-in`, delay bumped 200ms→700ms to start after stage 1); (3) 1.6s–2.5s — the newly-`justUnlocked` node pops from dark to lit (`animate-node-light-in`, delay bumped 700ms→1600ms). A small `animate-pop-in` (new, generic, no baked-in translate) plays the Check icon popping in as a card settles.
- Live-verified the whole sequence end-to-end, not just the pieces: seeded `localStorage` directly to simulate the user's exact scenario (a "seen" snapshot with only lessons 1–4 done, then a "current" progress with 5 and 6 also marked complete — i.e., "I just finished two siblings"), reloaded fresh, and confirmed via the actual rendered class lists (not just screenshots) that both newly-finished cards carry the stage-1 `transition-colors duration-[700ms]` class and had already settled to their green `border-status-healthy/50` styling, while the one lesson that specifically unlocks (REST APIs, whose only prerequisite is one of the two just-finished lessons) carried `animate-node-light-in` and its `available`/yellow styling — matched a real screenshot of the same state. Reloaded again with no further progress change and confirmed neither card carries any transition/animation class the second time — the reveal genuinely doesn't replay once "seen" has caught up.
- Still not yet live-checked: whether the full ~2.5s three-stage sequence actually *reads* as clearly visible/satisfying to a human watching it play in real time (mechanically verified via rendered classes at each stage, not watched frame-by-frame — screenshots can't capture motion); the literal user flow of marking complete from the lesson page's own button, using its "next" nav link, marking the second lesson complete, and navigating back via the map's own back link (this pass verified the map's reveal logic directly via seeded `localStorage`, not that exact click path); hover states on available/completed cards beyond one spot-check; Night Ops theme (an earlier live check landed on a yellow-accented variant unexpectedly — worth confirming which theme that actually was and that it's not a regression); a narrow/responsive viewport; `.no-scrollbar` in Firefox specifically (only Chrome-family browsers available to test with here); whether the rectangular cards' fixed 168px width ever collides with a neighboring card; keyboard-only access to the locked-node reveal buttons; reduced-motion behavior for the new delays (global rule zeroes `animation-duration`, not `animation-delay` or the `MapNode`-local `setTimeout(50ms)`, so a reduced-motion reader still waits through the staged delays before content appears near-instantly — a minor, pre-existing-pattern tradeoff, not newly introduced, but worth a look); what happens if a reader has the map open in two tabs, or marks a third/fourth lesson complete in the same sitting the diff was already computed for (the diff is a one-shot mount computation, by design — a later change wouldn't retroactively re-animate without a fresh mount).
- `src/content/foundations/__tests__/content.test.ts`'s two prerequisite-graph assertions (referential integrity, no cycles) untouched by any revision. `npx tsc --noEmit` and `npx eslint` on the changed files pass clean; `npx vitest run src/content/foundations` (103 tests, scoped — the full suite currently has two unrelated pre-existing `tsc` errors from a concurrent session's in-progress `model_router` work in other files, not touched here).

**Phase 2 of `docs/Agentic_AI.md`: `agent_orchestrator` + 4 of 6 canonical patterns land** — new
- New `EntityType` `agent_orchestrator` (`AgentOrchestrator.ts`) — structurally new for this engine: it holds a multi-hop *session* per admitted request (Sequential = Planning, one downstream target at a time, retried in place up to Max Iterations on failure; Parallel = Orchestrator-Worker, every downstream target dispatched at the identical timestamp, joined once all report back), rather than the one-admission-one-hop shape every entity through Phase 1 used. Still zero new event types — sessions are entirely event-driven off the existing REQUEST_ROUTED response-direction convention every other entity already emits. Registered everywhere Phase 1's two entities were: `Simulator.ts`, `entityCatalog.ts`, `entityConfigSchema.ts`, `entityEducation.ts`, `entityDeepDive.ts` (new `/entities/agent-orchestrator` page), `tutorialPlanner.ts`; also added to `ComponentNode.tsx`'s and `InspectorPanel.tsx`'s routing-distribution gating (`ROUTING_ENTITY_TYPES`) so fan-out/sequencing gets the same visible distribution bar Load Balancer's traffic split already has, for free. Not priced in `costEngine.ts` yet — a deliberate, documented omission, same precedent Client already sets.
- Content: 6 new "patterns" lessons in `src/content/agentic/lessons/` (05-10), each pairing one canonical pattern with a real build-this-topology challenge using the existing `architecture` lesson block (no new diagram component). Tool Use, Planning, Orchestrator-Worker, and Multi-Agent Collaboration are fully buildable today. Reflection and Evaluator-Optimizer need `guardrail_validator` (Phase 3) and say so plainly rather than faking a canvas challenge for a primitive that doesn't exist yet — their own docblocks and lesson content explain why.
- Verified programmatically: 12 new `AgentOrchestrator` tests (22 total across the two new entity-test files this phase, once combined with a Multi-Agent addition) cover determinism, Sequential's staggered dispatch timing vs Parallel's simultaneous dispatch (the literal "demonstrably distinct in their event traces" acceptance criterion), the infinite-retry-loop failure mode reproduced on purpose (`iteration_limit_exceeded`), Parallel's partial-failure-fails-the-whole-session behavior, and a genuine 2-orchestrator peer chain (Multi-Agent Collaboration) with an exact assertion on the recorded `path` through both orchestrators.
- `tsc --noEmit`, full `vitest run` (747 tests, 32 new since Phase 1), and `npm run lint` all pass clean. One pre-existing, unrelated `tsc` error in `src/lib/foundationsProgress.ts` (from the in-progress `FoundationsMap` work above, a `ReadonlySet.includes` narrowing issue) — not touched or introduced by this phase.
- Not yet live-checked: dragging an Agent Orchestrator onto `/workshop`'s canvas and building each of the 4 buildable pattern topologies from their lesson pages; the node card's Routing Mode/Max Iterations summary line; the Inspector's new "Dispatch Distribution" section rendering correctly for both Sequential and Parallel runs; the 6 new `/agentic/*-pattern` lesson pages rendering their `architecture` diagrams correctly (multi-node topologies with `entityType`-tagged boxes pulling the right catalog icons); the new `/entities/agent-orchestrator` deep-dive page; prev/next nav across all 10 agentic lessons now that 6 more exist.

**Phase 1 of `docs/Agentic_AI.md`: `llm_call` + `tool_call` land on the Workshop canvas** — new
- New `EntityType`s `llm_call`/`tool_call`, implemented as `LlmCall.ts`/`ToolCall.ts` (same bounded-concurrency admit-queue-reject shape as `APIServer.ts`/`Database.ts`, no new event types). Registered in `Simulator.ts`, `entityCatalog.ts` (new `domain: "agentic"` field), `entityConfigSchema.ts` (Model Tier/Quantization/Deployment Target selects for `llm_call`; latency/failure/cost dials for both), `costEngine.ts` (usage-only pricing — `llm_call`'s $/1M-token rate scales with tier+quantization, Edge deployment zeroes it; `tool_call` prices off its own flat Cost/Call), `entityEducation.ts` and `entityDeepDive.ts` (full `/entities/llm-call` and `/entities/tool-call` deep-dive pages), and `tutorialPlanner.ts`'s `TUTORIAL_RECIPES`. `ComponentSidebar.tsx` got a new "Agentic AI" section (below Core/Modules) driven by the new `domain` field, separate from the existing HLD Core/Modules split. Node visuals, Inspector, playback, and metrics panel needed zero changes — confirmed already fully generic over entity type, per `Expansion_TODO.md`'s own prediction.
- Verified programmatically (not just visually): a script running `LLM / none / cloud` vs `int4` vs `SLM` vs `SLM+int4` vs `edge` deployment showed the monthly cost readout dropping from ~$147k → ~$2k across those configs, and Edge deployment zeroing usage cost entirely (on-device, no per-token bill) while its Edge+LLM-tier pairing produces a large *emergent* latency increase (queueing cascades once the simulated 3x compute penalty pushes the node over capacity) rather than a flat asserted number — matches the acceptance criteria in `Agentic_AI.md` Part 5's Phase 1 ("see a cost readout... change when quantization or model tier changes").
- `tsc --noEmit`, full `vitest run` (703 tests, 16 new), and `npm run lint` all pass clean.
- Not yet live-checked: dragging an LLM Call + Tool Call onto `/workshop`'s canvas from the new "Agentic AI" sidebar section and wiring Client → LLM Call → Tool Call; the node card's compact config summary line (Model Tier/Quantization/etc. via `shortLabel`) renders sensibly at the node's fixed width; the Inspector's select dropdowns for Model Tier/Quantization/Deployment Target render and update correctly; the cost readout on the node card and in the Cost panel updates live after a run; the two new `/entities/llm-call` and `/entities/tool-call` deep-dive pages render every section correctly (tables, failure-mode reproduce steps) in both the sidebar-triggered Inspector link and direct navigation; Batman Mode's `WeaponWheel` shows the two new entities in its radial picker without layout issues now that the catalog has 14 items instead of 12.
- Known, deliberate gaps carried forward openly rather than silently skipped: `docs/Entities.md` (the large hand-maintained contributor doc) wasn't updated with these two entities — no test enforces it staying in sync, and it's a separate, substantial doc-writing pass; the two new failure-mode entries have no "Try It" demo page (`FailureModeDemo` with a verified seed + remedies) — only Phase 4's dedicated villain-attack work per `Agentic_AI.md` §2.3 warrants that investment for this domain.

**New `/agentic` reading room (Phase 0 of `docs/Agentic_AI.md`) — index + lesson pages, `/learn` hub card** — new
- Added a fourth reading room mirroring `/lld`'s exact shape: `src/content/agentic/` (types/index/4 lessons across `fundamentals`/`protocols-and-infra` categories), `src/app/agentic/page.tsx` (category-grouped index grid) and `src/app/agentic/[slug]/page.tsx` (article shell reusing `ArticleSection`/`LessonBlockRenderer`/`EntityTableOfContents`, prev/next nav). `src/app/learn/page.tsx` got a 4th card (`Bot` icon, "04") after Entities, and the reading-rooms diagram/badge text updated from "Three" to "Four". Deliberately does **not** wire up Lock-In/Batman-mode (`LockInPanel`/`LockInChapterAction`) — that's a separate villain-trilogy feature with a hardcoded `"foundations" | "lld"` union, out of scope for a content skeleton (see the page's own docblock).
- `tsc --noEmit`, full `vitest run` (687 tests), and `npm run lint` all pass clean.
- Not yet live-checked: `/learn`'s new 4-card grid layout (3-col on `lg`, so the 4th card wraps to its own row — confirm this doesn't look broken/lopsided vs. an even 2x2); the `/agentic` index page's 5-category grouping renders correctly when only 2 of the 5 categories have lessons (patterns/inference-and-serving/production sections should simply not render, not show as empty); the new lesson pages' `flow` block (animated Thought→Action→Observation steps in `react-and-tool-use`) and multiple `table`/`qa`/`insight` blocks render correctly; prev/next nav across all 4 lessons; hover states on the new `/learn` card and `/agentic` index cards match the existing three.

**Homepage header: new "Problems" nav tab** — new
- `src/app/page.tsx` header nav previously had Tutorial / Learn links but no direct link to `/problems` (only reachable via the small "See all N problems" link inside the Scenarios section). Added a `Problems` link (ListChecks icon) between Learn and the ThemeToggle, styled identically to the existing Tutorial/Learn links.
- Not yet live-checked: confirm the new "Problems" tab renders correctly in the sticky header at both desktop and narrow widths (existing header row may wrap/crowd with the extra item — check against ThemeToggle/GitHub icon/Enter Workshop button spacing), hover state turns signal-colored like its neighbors, and it navigates to `/problems`.

**Night Ops: automatic weather cycle, now 3 phases (rain -> heavy rain with blur/glass/bloom -> snow with blue signal color -> repeat), base rain intensity bumped** — new, supersedes the original 4-phase entry (never live-checked, and already stale against the shipped code before this pass)
- Follow-up to the user's original "keep the weather rolling automatically" request: the loop's first phase (`"light-rain"`, intensity `0.35`) read as "very very mild, unnoticeable" — same numeric target the reduced-motion resting frame used, so it was deliberately the faintest look in the whole cycle. Per the user's explicit call ("keep it rain, heavy rain, and then snow"), the middle `"moderate-rain"` phase is removed and the base phase renamed `"rain"` — `weatherCycle.ts`'s `WeatherPhase` union and `CYCLE` array are now just `rain` (24s) -> `heavy-rain` (18s, `RainCanvas` at full intensity + `WeatherGlassOverlay` fades in) -> `snow` (30s, rain eases into falling snow *and* `--color-signal`/`-hover`/`-active` flip lime-yellow -> blue via `[data-theme="night-ops"][data-weather="snow"]` in globals.css) -> back to `rain`.
- `RainCanvas.tsx`'s `INTENSITY_TARGET.rain` bumped `0.35` -> `0.55` (was also the reduced-motion resting value, now shared logic still reads `INTENSITY_TARGET.rain` for that fallback) so the base loop phase is clearly visible weather from the start, not just a rounding error before heavy-rain. `heavy-rain` (1) and `snow` (0.55) targets unchanged. Two-dial easing (`intensity`, rain-vs-`snow`, `EASE_RATE = 0.006`) and `WeatherGlassOverlay`'s heavy-rain-only blur/bloom are otherwise untouched — `WeatherGlassOverlay` never referenced `moderate-rain` specifically (only `phase === "heavy-rain"`), so it needed no change.
- Reduced motion: unchanged behavior, just renamed — the cycle never advances under `prefers-reduced-motion`, holding at the resting `"rain"` look (now the more-visible 0.55 intensity, not 0.35) forever.
- `tsc`/lint both clean; no remaining code references to `light-rain`/`moderate-rain`.
- Not yet live-checked (needs to sit through a ~72s loop on `/workshop` with Night Ops active): the base rain phase now reads as clearly-falling rain from the moment the cycle starts (not the previous barely-there drizzle); rain visibly intensifies into the heavy-rain blur/glass/bloom moment (screen center staying legible, edges fogged); heavy rain visibly slows and dissolves into snowflakes while `--color-signal` shifts lime-yellow -> blue over a few seconds, not an instant snap; snow holds, then the cycle returns cleanly to visible rain (blue->yellow shift reversing) rather than a moment that reads as *nothing* falling; only 3 distinct "looks" over the loop now, not 4.

**Light theme removed entirely — app is now dark-only (+ Night Ops preview)** — new
- User's explicit call: delete the light theme rather than keep it around unmaintained. `globals.css`'s `:root[data-theme="light"]` block deleted outright (was ~35 lines of paper-toned token overrides); its doc comment updated to describe the app as dark-only with Night Ops layered on top. `ThemeProvider.tsx`'s `Theme` type narrowed to `"dark" | "night-ops"` (was `"light" | "dark" | "night-ops"`); `THEME_CYCLE` is now just `["dark", "night-ops"]`; `colorMode` simplified to the constant `"dark"` (previously computed off `theme === "light"`) since react-flow's `colorMode` prop only ever needs to distinguish light/dark and the app has no light state left. `ThemeToggle.tsx` drops the `Sun` icon/light `ICONS` entry and now cycles dark → night-ops → dark (was dark → night-ops → light → dark). `layout.tsx`'s blocking pre-paint init script no longer restores `data-theme="light"` from `localStorage`, only `"night-ops"`.
- **Supersedes every older pending item in this file that says "check/verify in light theme" or "both themes"** — light theme no longer exists, so those sub-checks are moot, not failed; don't try to verify them, and don't read their presence elsewhere in this file as still-outstanding work.
- `npx tsc --noEmit`, `npm run lint`, and `npx vitest run` (646 tests) all pass clean.
- Not yet live-checked: any page that previously looked correct in light theme now simply has no light option — confirm the `ThemeToggle` in a header only ever shows Moon ⇄ BatMark (never a Sun icon) and cycles two-way correctly; confirm a browser with `localStorage.theme = "light"` from before this change now falls back cleanly to dark on load (no broken/blank styling, no console error) since the init script no longer recognizes that stored value; confirm Batman Mode's toggle-lock behavior (disabled + Lock icon mid-run) still reads correctly against the shorter two-item cycle.

**Bat-signal transition: moon + skyline removed, beam steepened ~5-6°, scrim darkened — CONFIRMED live on `/workshop`**
- Follow-up to the two entries below: moon and hand-drawn skyline `<div>`/`<svg>` blocks deleted outright from `BatSignalTransition.tsx` (not just hidden). Scrim darkened `hsl(240 30% 3% / 0.55)` → `hsl(240 28% 2% / 0.72)`. Beam's `clip-path` top-edge points raised (`-4vh`→`-14vh`, `27vh`→`15vh`) to steepen the angle; disc nudged up slightly (`top-[3vh]`→`top-[1vh]`) to match.
- Live-verified: no moon, no skyline (bottom edge is clean canvas grid again), overall overlay reads visibly darker, beam sweeps higher/steeper through the frame, disc + bat mark unaffected.

**Bat-signal mark swapped to the real logo shape (user's explicit call) — CONFIRMED live on `/workshop`**
- Follow-up to the entry below: user rejected the hand-drawn generic silhouette outright ("still sucks"), supplied a real bat-signal SVG (`batman-svgrepo-com.svg`, sourced from svgrepo.com), and — after I flagged that this is a near-exact reproduction of the actual DC/Batman trademarked logo, not a generic icon — explicitly chose to ship it as-is anyway. `BatMark.tsx`'s path replaced with the traced/tightened-viewBox version of that shape; docblock updated to say plainly that this is a knowing fidelity-over-originality trade-off made once, for this project, not a default to repeat elsewhere without asking.
- Live-verified: the 16px theme-toggle icon reads unmistakably as the real bat-signal mark (not a blob); the large disc in `BatSignalTransition` now shows the same shape correctly centered with no leftover empty space (previous hand-drawn version's viewBox wasn't a problem here since this one was tightened to the real path's bounding box). Moon and skyline (previous entry) still render correctly alongside it. `tsc`/lint clean.

**Bat-signal silhouette redrawn + moon/skyline added to the toggle transition — CONFIRMED live on `/workshop`, superseded by the entry above (bat shape itself)**
- Reported complaint: the bat-signal in Night Ops "looks weird". User supplied a reference image (a painterly Bat-Signal beaming over a moonlit city skyline) as the target mood — didn't use it as a literal asset since it's a near-reproduction of the actual trademarked DC Bat-Signal emblem, which `BatMark.tsx` has deliberately avoided since it was first drawn (see its docblock).
- `BatMark.tsx`: silhouette redrawn from a straight-line polygon to a `<path>` with a smooth curved leading (top) edge per wing and a 2-scallop trailing (bottom) edge, tighter ear notch. Live-verified at both the small 16px toggle-icon size (reads as a clean bat, not a blob) and the large disc size in the transition (dark bat on the yellow disc, legible).
- `BatSignalTransition.tsx`: added a static pale moon (radial-gradient circle, top-left, opposite the disc) and a low hand-drawn city-skyline silhouette along the bottom edge. **Found and fixed live**: the skyline's first-pass fill (`hsl(240 35% 4%)`) was only ~1% lighter than the page's own near-black background (`--color-bg: hsl(240 18% 3%)`) and rendered completely invisible under the scrim — confirmed via a zoomed screenshot showing nothing but the grid dots. Fixed by bumping the fill to `hsl(235 22% 11%)` (a real step up in lightness, not a rounding-error one) and moving the opacity off the whole `<svg>` wrapper onto individual pieces so the lit windows (`hsl(45 80% 68% / 0.5–0.55)`) stay bright; re-verified live — skyline + windows now clearly visible along the bottom edge, moon and disc unaffected.
- Toggle click into Night Ops confirmed on `/workshop`: moon, beam, disc+bat, and skyline all render in sensible non-overlapping positions; the small 16px `BatMark` toggle icon reads cleanly post-toggle. `tsc`/lint both clean after the fix.


**`code` lesson blocks redesigned — line numbers, decorative syntax highlight, gradient surface, copy button, collapse for long blocks** — new
- Reported complaint (with screenshot of the movie-booking SQL schema in `09-sql-deep-dive`): the old `code` block was a flat, single-tone `<pre>` dump on a solid `bg-bg-panel` fill — no background variation, nothing breaking up a 50+ line block, unreadable/skippable. Same renderer serves every `kind: "code"` block across the app (~190 occurrences across Foundations + LLD lessons), so this was product-wide, not one page.
- Fix: new `src/components/content/CodeBlock.tsx` replaces the inline `case "code":` markup in `LessonBlockRenderer.tsx`. Adds: a left gutter with line numbers; a decorative regex tokenizer (`KEYWORDS` merged across sql/java/js/redis/lua/cypher — not a real per-language grammar) coloring keywords `text-signal` and strings/comments in the existing muted/subtle text tiers (deliberately did *not* reach for `status-*` colors, since globals.css reserves those for real simulated state); a subtle `bg-bg-elevated → bg-bg-panel` vertical gradient wash on the code surface instead of one flat fill; a header-bar Copy button (icon swaps to a check + "Copied" for 1.5s); and for any block over 14 lines, auto-collapse to 14 lines with a bottom fade + "Show N more lines" toggle (found the SQL schema block runs ~54 lines, so this is the common case, not an edge case).
- Needs a check on `/foundations/sql-deep-dive` (the exact block from the screenshot — confirm it's collapsed by default, expands/collapses cleanly, gutter numbers stay aligned with code rows through the toggle) and one `java`-language block from an LLD lesson (e.g. `/lld/creational-patterns`) to confirm the keyword coloring reads sensibly for a different language and the copy button actually populates the clipboard. Check both themes — the gradient direction/contrast and the `text-subtle/60` gutter numbers need to stay legible on light too, not just dark. Also confirm a short (<14 line) code block still renders fine with no collapse button and no leftover empty space from the removed fade overlay.

**Learn content de-emoji'd — ✅/❌ (and stray 💥/😴/😅/🔀/🎉) replaced with tone-colored list bullets** — new
- Reported complaint: Foundations lesson content was full of raw ✅/❌ emoji glyphs inline in prose/list items/table cells, reading as a "Claude-generated checklist" rather than professional copy. Root cause: ~100 occurrences across 13 `src/content/foundations/lessons/*.ts` files (05-dns, 06-http, 07-rest-apis, 09-sql, 10-nosql, 11-indexing, 12-scaling, 14-caching, 16-message-queues, 17-kafka, 18-cdn, plus 03/04/01), all rendered by `LessonBlockRenderer.tsx` (`/foundations/[slug]`, `/lld/[slug]`).
- Fix: `LessonBlock`'s `list` kind (`src/content/shared/lesson.ts`) now accepts items as `string | { text; tone?: BoxTone }` — `LessonBlockRenderer.tsx` colors the bullet dot via `LIST_TONE_DOT` (healthy=green/critical=red, same tokens diagrams already use) instead of an inline glyph. Converted every good/bad list item to `{ text, tone }`, stripped emoji from paragraphs/table cells/headers/insight text/SQL code-comment blocks/diagram sublabels where the words (or an existing `tone`/color on that diagram element) already carried the meaning on their own. Also removed unrelated decorative emoji (💥/😴 sublabels in 13-load-balancers, 😅 in 01-what-is-system-design, 🔀/💥 in 06-http, 🎉 in 04-client-server) and two raw ✕/✓ glyphs baked into `DnsFailoverDiagram.tsx`'s box text (redundant with its own `tone`-driven color).
- Not yet live-checked: a `list` block with mixed tones (e.g. `/foundations/nosql-deep-dive`'s "When to use MongoDB" list, or `/foundations/vertical-vs-horizontal-scaling`'s trade-off lists) renders green/red dots correctly in both themes, at a size that reads as a deliberate status color rather than a rendering glitch; a page with no toned items (most of the app) still renders the plain signal-colored dot unchanged; `/foundations/dns-deep-dive`'s failover diagram box text reads fine without the old ✓/✕ marks. `tsc`/lint/vitest (646 tests) all pass.

**Batman Mode: three new night-ops features — Detective Vision, villain traffic attacks, boss-fight recall check** — new
- Three separate additions, all gated to `night-ops` except the last (which is theme-agnostic — see below):
  1. **Detective Vision** (`DetectiveVisionHUD.tsx`): a night-ops-only toggle that reskins the existing (theme-agnostic) `findBottleneckNodeId` "Bottleneck" badge as a full-screen scan — everything fades to near-black except a spotlight cutout around the flagged node, with a pulsing signal-yellow ring, a scanning sweep sourced across the cutout, and a "Threat flagged — <node> — N% load" readout. No bottleneck ⇒ dim-only "No threats detected" state. `useLiveRect` (promoted from `components/tour/` to `src/lib/` — a second, unrelated caller now needs the exact same rect-tracking hook TourOverlay already used) drives the live cutout position via the node's `.react-flow__node[data-id=...]` DOM rect. Toggle button carries an explicit `z-[90]` (overlay itself is `z-[80]`, pointer-events-none) so it stays lit and clickable above its own dimming rather than getting swallowed into the dark along with everything else.
  2. **Villain traffic attacks** (`content/workshop/villainAttacks.ts`, `VillainAttackPicker.tsx`, wired into `workshopStore.runSimulation` via a new `activeVillainAttackId` field): a night-ops-only dropdown that loads one of three attack profiles — Ra's al Ghul (ramp, slow siege), Joker (burst, chaotic), Bane (constant, sustained overload) — as a `TrafficPattern` override for the next run, scaled off whatever's already driving the canvas (`workshopBridge.baselineRequestRate`, new + tested) rather than a fixed number. Pure pattern-building logic has real test coverage (`content/workshop/__tests__/villainAttacks.test.ts`, `lib/__tests__/workshopBridge.test.ts`); the picker UI itself is unchecked.
  3. **Boss-fight recall check** (`lib/villainInterrogation.ts`, wired into `LockInChapterAction.tsx`) — **not** night-ops-gated, since Lock-In itself already forces night-ops for its whole run. Clicking "Defeat {villain}" no longer advances on trust: it now opens a one-question check built from the lesson's own `keyTakeaways` (one real takeaway + two decoys pulled from unrelated lessons, see the module doc for why this reuses existing content instead of hand-authoring quizzes for all 35 lessons) — pick the one that's actually true about what was just read; a wrong pick just taunts again with no attempt limit, matching the existing soft-enforcement contract. Falls back to the old trust-based advance if a question can't be built. Has real test coverage (`lib/__tests__/villainInterrogation.test.ts`, including "every real Foundations/LLD lesson produces a valid 3-option question").
- **Reorg (user request)**: Detective Vision and the villain attack picker used to live apart (Detective Vision pinned to the canvas's `bottom-left` FlowPanel; the attack picker in `WorkshopHeader.tsx` next to Run Simulation) — user flagged the attack picker as buried in the header and Detective Vision as sitting too low to notice. Both are now stacked under `ComponentSidebar.tsx`'s "Choose Weapon" trigger in the canvas's top-left corner (`Choose Weapon` button → `VillainAttackPicker` → `DetectiveVisionHUD`, one `flex flex-col` group), so all three night-ops toolbelt controls live in one cluster. `WorkshopHeader.tsx` no longer renders the attack picker; `ArchitectureCanvas.tsx` no longer has the `bottom-left` FlowPanel. Neither component's own internals changed (both still self-gate to `theme === "night-ops"` and render `null` otherwise).
- Not yet live-checked (all three, on `/workshop` and one `/foundations/[slug]` or `/lld/[slug]` lesson mid a Lock-In run): the new top-left stack in night-ops — "Choose Weapon" / attack dropdown / Detective Vision toggle rendering as one clean vertical cluster with no overlap/misalignment, and that it doesn't collide with anything opened from it (WeaponWheel modal, the attack `<select>`'s open option list, Detective Vision's full-screen scan); confirm the header no longer shows the attack dropdown and the canvas's bottom-left corner no longer shows Detective Vision; Detective Vision's scan visuals (cutout tracking a node through a drag, the sweep/pulse timing, the "No threats" empty state, the toggle button staying visibly lit above the dim); the villain attack dropdown's option list/selected-state styling and that picking one + Run actually produces a visibly harsher run (check node status colors, results panel); the interrogation panel's layout replacing the Defeat card in place, the wrong-answer red state, and that answering correctly still advances/routes exactly like before. `tsc`/lint/vitest (646 tests, 14 new) all pass.

**Batman Mode: ambient bat-signal spotlight replaced with a one-shot full-screen transition on toggle** — previous entry, still pending
- Reported complaint: the always-on `BatSpotlight` ambient glow (corner beam + disc, breathing forever) sitting behind every night-ops page felt like clutter/annoying. Deleted `BatSpotlight.tsx` entirely and folded its beam+disc visual into the toggle-triggered transition instead — `night-ops` pages no longer render any persistent spotlight, only `RainCanvas`'s ambient rain remains as background decoration.
- `BatSwarmTransition.tsx` renamed/expanded into `BatSignalTransition.tsx` (context hook renamed `useBatSwarmTrigger` → `useBatSignalTrigger`, prop renamed `swarmKey` → `signalKey` in `NightOpsAtmosphere`). Still fires only on the actual toggle click into night-ops (`ThemeToggle`), not on `theme` changing. New behavior: a `fixed inset-0 z-[100]` overlay (above `RainCanvas`'s `z-[60]` and everything else in the app) fades in with `backdrop-blur-xl` + a dark scrim over whatever page content is underneath, the beam+disc signal scales in at the top-right, the same 12-bat flock sweeps across, and a centered serif line fades/rises in over it — one of three, chosen by trigger count so repeated toggles rotate rather than repeat: "Welcome, Vengeance." / "It's not who I am underneath, but what I do that defines me." / "Why do we fall, Master Wayne? So that we can learn to pick ourselves up." Whole thing runs ~2.6s then fades out via `AnimatePresence` (was an abrupt unmount before). Still skipped entirely under `prefers-reduced-motion`, same as the old swarm.
- Not yet live-checked: the toggle click on `/workshop` (and one other route) actually plays the new overlay — blur/scrim reads correctly against page content, beam+disc position, bat flock timing/visibility unchanged from before, the three lines are legible and rotate correctly across repeated toggles (dark→night-ops several times in a row), the overlay sits above `RainCanvas` and any open dropdowns/toasts, and the fade-out on `AnimatePresence` exit looks smooth rather than jarring. `tsc`/lint both pass; no remaining references to the deleted `BatSpotlight`/`BatSwarmTransition`/`useBatSwarmTrigger` names.

**Components palette de-cluttered — on-demand toggle (light/dark) / WeaponWheel radial picker (Batman Mode)** — new
- Reported complaint: the permanent left (`w-72`) + right (`w-80`) docked slabs around the canvas felt cluttered. `ComponentSidebar.tsx` no longer renders a permanent `<aside>` — it's now a small floating trigger pinned to the canvas's top-left corner (`absolute left-3 top-3`), and the actual picker only mounts on top of the canvas when opened (doesn't push/resize it). Canvas is full-width by default now. Behavior branches on theme: light/dark get a "Show/Hide Components" toggle that opens the same docked list as before (now floating, with its own close ×); `night-ops` ("Batman Mode") gets a "Choose Weapon" button that opens a new full-screen radial picker (`WeaponWheel.tsx`) instead — a ring of the 12 catalog entities as SVG wedges around a hollow center, center echoes the hovered entity's name/description (styled after a game weapon-select wheel, matches night-ops's existing HUD aesthetic), click a wedge to place it and close, Escape/backdrop-click to cancel without selecting.
- `componentsPanelOpen` moved into `workshopStore` (was local state) so `TutorialRunner` can force the panel open for guided-tour steps that spotlight the catalog list (`requiresComponentsPanel` on the relevant `TourStep`s in `tutorialPlanner.ts`) — otherwise those steps would spotlight nothing now that the list isn't always mounted. `TutorialPanel.tsx`'s own fixed offset is now conditional (`left-3` when the dock isn't visible, `left-[20rem]` when it is) instead of a permanent `left-[19rem]` assuming the old dock's width.
- **`/tutorial` never gets the wheel, in any theme**: `WorkshopShell` gained a `forceComponentsList` prop (`TutorialRunner` always passes it), threaded down to `ComponentSidebar`'s new `forceListMode` prop — this pins `isBatman` to `false` regardless of the actual theme, so the guided tour always gets the plain docked list, whose cards carry the `data-tour-id` the tour's spotlight needs. WeaponWheel only ever appears on `/workshop` (and only in night-ops there). Closes the earlier gap where a `night-ops` tutorial run would've shown the tour's no-spotlight fallback for "add a component" steps.
- Not yet live-checked: the floating trigger + panel positioning in both themes on `/workshop` (no overlap with `ScenarioCompletionToast`'s centered toast or the canvas's own top-right `StatusLegend`); the WeaponWheel's wedge hover/click/keyboard-focus behavior, center-hub text updating correctly, Escape/backdrop-click closing it, and the glow/backdrop-blur rendering as intended against night-ops's near-black background, on `/workshop`; `/tutorial` with the theme set to `night-ops` — confirm it shows the plain list (not the wheel) and a step that requires the panel still force-opens it correctly; `/tutorial` in light/dark with the same force-open check; general layout at ~1024px. `tsc`/lint/vitest (632 tests) all pass.

**`/learn` hub visual polish — system-topology background + Fig. 01 reading-rooms diagram** — new, supersedes an earlier unverified attempt on this same page
- Reported complaint (round 1): the hub read as bland (flat `bg-bg`, only three cards, no visual language tying it to the rest of the app). Fix: swapped the ad-hoc tag span for the shared `Badge` component; added a static "Fig. 01 — Reading Rooms" specimen box (same hairline-frame + corner-tag convention as the landing page's `Fig. 01 — Live Architecture`) containing `ReadingRoomsDiagram` — three node boxes (Foundations/LLD/Entities) on a connecting hairline. First pass also added a flat repeating-dot-grid CSS tile (`.bg-blueprint-grid`) as the page background.
- Follow-up complaint (round 2, before the round-1 grid was ever live-checked): the dot-grid "doesn't do much" — too generic, not evocative of the app's actual subject. Replaced it entirely: `.bg-blueprint-grid` and its `--color-grid-line` token are deleted from `globals.css`; new `src/components/learn/SystemMeshBackground.tsx` renders a hand-placed, non-repeating network topology instead — 26 nodes in three loose clusters + scattered bridge nodes across a `1600×900` viewBox (`preserveAspectRatio="xMidYMid slice"`, so it covers the block like a background-image regardless of viewport height), sparse edges between them (not all-to-all), one 5-node/4-edge path picked out in the signal color as a "highlighted request path" (echoes the landing page's `HeroDiagram` packet idea, but static), and 8 faint rotated mono-font labels scattered around using real system-design vocabulary (`p95 latency`, `round robin`, `cache-aside`, `ttl 60s`, `consistent hashing`, `backpressure`, `circuit breaker`, `sharding`) as blueprint-style margin annotations. Mounted via `absolute inset-0 -z-10` on a `relative` wrapper (needs the explicit negative z-index — a merely-positioned decorative layer with `z-index:auto` paints *above* in-flow siblings per normal CSS stacking order, not below). Still fully static, no motion, per the app's "animation communicates state change, not decoration" rule. All color via existing `fill-*`/`stroke-*` design tokens, so no new theme-specific code needed.
- Not yet live-checked (round 2 supersedes round 1's unchecked items): the topology renders recognizably as a network diagram (not noise) and stays subtle enough not to compete with the cards/specimen box on top of it, in both dark and light theme; the `slice` crop looks reasonable at a normal ~1024px+ viewport height (tall/short viewport aspect could crop the topology asymmetrically — acceptable if it still reads as background texture, worth eyeballing); the accent (signal-colored) path is visible but not distracting; the scattered labels are legible enough to read as intentional annotations rather than illegible smudges. `tsc`/lint both clean, no leftover references to the deleted grid token/class.

**Batman Mode: theme toggle now actually refuses to leave night-ops mid-run, app-wide** — new
- `ThemeProvider.tsx`'s `toggleTheme` no-ops when leaving `night-ops` while `getLockInState().active` is true, regardless of which page's toggle is clicked — closes the gap where `LockInHeaderNav` only hid the toggle on `/foundations/[slug]`/`/lld/[slug]`, leaving every other route's plain `<ThemeToggle />` (`/workshop`, `/entities*`, `/learn`, `/problems*`, the landing page) a working way to bail out of night-ops mid-run (e.g. via the browser Back button landing on one of those unguarded routes). `ThemeToggle.tsx` now reflects this: disabled + swapped to a `Lock` icon + relabeled ("Theme locked — Batman Mode is active") whenever `theme === "night-ops"` and a run is active, instead of a click that silently did nothing.
- Not yet live-checked: start a lock-in run from a lesson page, then navigate (Back button, or any other route's own link) to `/workshop`/`/entities`/`/learn`/landing — confirm that route's theme toggle now renders disabled/locked with the tooltip, and clicking it does nothing; confirm it's back to normal (enabled, cycles dark→night-ops→light) immediately after finishing the run (victory screen's "Return to Gotham") or otherwise resetting it; confirm starting a *new* transition into night-ops (dark→night-ops) still works normally when no run is active; confirm the disabled visual (opacity/pointer-events, per `Button`'s built-in `disabled` styling) doesn't look broken next to the other icon-only header buttons in either theme.


**Bat-signal direction flip + redrawn bat silhouette + beam angle/width/softness fixes — CONFIRMED live on `/` (landing page, Night Ops theme)**
- `BatSpotlight.tsx`: disc moved from top-center to top-right (`right-[6vw] top-[3vh]`); beam is a `clip-path` trapezoid anchored at the bottom-left viewport corner, widening to overlap the disc's footprint at top-right — points mix `vw`/`vh` units (not `%`) so the far edge tracks the disc regardless of viewport aspect ratio. Went through 3 live-checked iterations on the real page: v1 (thin, gradient faded out before reaching the disc — visible gap) → v2 (fixed the gap and width, but rendered as a solid hard-edged mustard ribbon, clashing with the site's own "minimal, no loud gradients" design language) → v3, current: `filter: blur(64px)` (well past what's needed to hide the clip-path edges) + a low, gradually-tapering gradient (`0.14 → 0.07 30% → 0.03 60% → 0.01 85% → transparent`) so it reads as genuinely hazy background light — soft, low-key, continuously connects source-to-disc with no hard edges and no visible gap. Verified: sits correctly behind hero text/buttons/the "FIG. 01" panel (unchanged DOM-order stacking, still no z-index needed); on `/workshop` the effect is fully covered by the canvas's own opaque background, confirmed pre-existing/unaffected by this change, not a regression.
- `BatMark.tsx`: polygon redrawn from an 18-point uniform in/out starburst (read as a many-legged bug) to a 14-point silhouette — two ear peaks + notch, two wingtips with one trailing-edge scallop each, a single tail point. viewBox `0 0 100 60` → `0 0 200 100`. Verified live at the ambient spotlight size (~130px, ~16vh) on `/` — reads clearly as a bat now. Not individually re-checked at the small icon call sites (theme toggle, lock-in nav, victory screen, swarm transition) this pass, but all size it via `size-*`/percentage boxes with default `preserveAspectRatio` so the same shape just renders smaller — low risk.
- **Resolved, no longer a browser-check item**: the `trilogies.ts` build breakage this note originally flagged (`LockInGuard` and friends importing a module that no longer existed) is gone — `grep -r trilogies src/` now finds nothing, every `lockIn/` file imports from `villains.ts`/`resolveChapters.ts`, and `npx tsc --noEmit` + the full `vitest` suite are both clean. Leaving this line rather than silently deleting it, since the original note said "still needs real attention" and that's no longer true.
- Not yet live-checked: reduced motion (disc breathing animation should just hold still — no tool support for forcing it in this environment); the small `BatMark` icon call sites listed above; a normal full-app pass with `LockInGuard` actually mounted (safe to do now that the import is fixed).

**4 new live/data-driven canvas features on `ComponentNode`/`AnimatedEdge`, same "real evidence, not decoration" formula as the utilization bar** — new
- **Edge heat lines** (`AnimatedEdge.tsx`, `packetSampling.ts`, `workshopStore.ts`): a soft copper halo behind each edge, width/opacity scaled by that edge's real (unsampled) share of this run's traffic relative to the busiest edge — rendered as a second `<path>`, deliberately not layered onto `.react-flow__edge-path` itself (that class is `!important`-styled in `globals.css`, which would fight inline per-edge width). `PacketSample` gained a `total` field; the store computes `maxEdgeTotal` once per run and attaches a normalized `trafficWeight` (0–1) to each edge's `data`.
- **Bottleneck spotlight** (`bottleneckDetection.ts`, new): after a run, whichever non-Client node has utilization ≥ 75% (and isn't already crashed/erroring, which has its own red pulse) gets a pulsing amber outline + a small "Bottleneck" badge pinned to its top-right corner. Returns `null` — nothing highlighted — whenever no node is meaningfully loaded, by design.
- **Live request/cost row**: a new row per node (between the config summary and the utilization bar) showing live request count (from `playbackMetrics`, ticks during playback) and `estimateCost`'s per-entity monthly cost (same "+" pre-run convention `BudgetRow`/`CostPanel` already use). Skipped for Client (not priced) and any entity `estimateCost` doesn't price.
- **Mini distribution sparkline**: a single segmented bar on Load Balancer / Replica Pool / Reverse Proxy / Kafka / topic-mode Message Queue nodes, one segment per downstream target sized by its share — same data `LoadBalancerDistributionSection` in the Inspector already renders as full bars, condensed to one row (Kafka prefers `routingDistribution` over `kafkaPartitions` here, unlike the Inspector which shows both).
- Not yet live-checked: any of the 4 rendering correctly on a real multi-node run (Load Balancer fan-out scenario is the natural test — should show heat lines, a bottleneck if one target's overloaded, cost/request rows on every priced node, and the sparkline on the LB itself), reduced-motion fallback for the bottleneck pulse, card layout not overflowing with all rows present at once (top strip + header + config + cost row + sparkline + utilization bar is the deepest a card can get), both themes. `tsc`/lint/vitest (615 tests) all pass.

**Night Ops rain overlay stacking fix** — new
- `RainCanvas.tsx`: the `<canvas>` was `fixed inset-0` with no explicit `z-index`, so it painted behind any page content that establishes its own stacking context (diagram SVGs, the workshop's React Flow canvas) per plain DOM order, contrary to the intent described in `NightOpsAtmosphere.tsx`'s comment. Added `z-[60]` (above the app's existing highest usage, `z-50` on `Modal`/`TourOverlay`/`InspectorPanel` popovers) so rain now reads as falling in front of everything; still `pointer-events-none` so it never blocks interaction. Updated the stacking comment in `NightOpsAtmosphere.tsx` to explain why RainCanvas needs an explicit z-index while BatSpotlight still relies on DOM order.
- Not yet live-checked: switch to Night Ops theme, confirm rain streaks now visibly cross over diagram SVGs (e.g. a foundations lesson page) and the workshop's canvas/React Flow nodes, and that it doesn't visually overwhelm an open Modal/InspectorPanel popover. `tsc`/lint clean.

**Entity node + edge redesign — the actual "canvas looks bad" complaint, not just the background** — new
- `ComponentNode.tsx` reworked: icon moved into a bordered square badge (was a bare inline icon), a new uppercase eyebrow tag showing the entity type name above the instance label (e.g. "API SERVER" / "API Server 2"), a full-width 2px status-color strip across the top of the card (status dot still there too — same meaning, now also readable zoomed out), a hairline divider before the config summary line, a new live utilization bar along the bottom edge (only renders once a simulation has produced `entityMetrics.utilization` for that node — reads the same field the Inspector's "Live Metrics" already shows), and copper `CornerBrackets` framing the card when selected (previously just a plain border-color swap). Card width grew 208px → 224px to fit the new header row.
- `AnimatedEdge.tsx` / `ArchitectureCanvas.tsx`: every edge now gets a static `MarkerType.ArrowClosed` arrowhead via `defaultEdgeOptions`, so direction is legible even before any simulation has run (previously the only directional cue was the post-run animated packet dots).
- `Handle` (connection dots) now tint copper on hover, previously no hover state at all.
- Not yet live-checked: the new node layout at both a single node and a dense multi-node graph (Load Balancer fan-out, CDN edge map scenarios), the utilization bar appearing/updating correctly during and after a run, corner brackets on selection not clipping/overlapping the Handle hit-boxes, the arrowhead rendering in both themes and not fighting the `.selected` edge color override in `globals.css`. `tsc`/lint/vitest (615 tests) all pass.

**Workshop canvas/chrome redesign — Blueprint + Polish, now permanent (no more toggle)** — new
- Was a 4-way design-review toggle (Classic/Blueprint/Vignette/Polish) for one round of feedback; user picked Blueprint (canvas) + Polish (chrome) together as the final look, so `WorkshopStyleProvider`/`WorkshopStyleToggle` are deleted and every call site now hardcodes that combination instead of branching on a style prop.
- **Canvas** (`ArchitectureCanvas.tsx`, always on now): fine 18px line grid + a coarser 126px dot grid layered on top, a radial vignette (`bg` → `bg-panel`) behind both, and `CornerBrackets` (new, reusable — `CornerBrackets.tsx`) framing the whole canvas viewport.
- **Chrome** (`WorkshopHeader.tsx`, `ComponentSidebar.tsx`, `InspectorPanel.tsx`, `Panel.tsx`, always on now): a signal-colored gradient hairline under the header, and `Panel.Header`'s new `accent` prop (small copper tick + tinted bottom rule) set unconditionally on the Components/Scenario/Inspector panel headers.
- Not yet live-checked: renders as intended with no z-index/stacking surprise against React Flow's own background layer (defaults to `transparent` per `@xyflow/react`'s `style.css`), in both dark and light theme. `tsc`/lint/vitest (615 tests) all pass.

**Connection-latency mismatch — FIXED for all 27 scenarios (decided: teach the concept, not the objectively fastest topology)**
- Root cause (recap): every scenario's `optimalSolution` was tuned/tested against 0ms connection latency, but the live Workshop always applies a 5ms-per-hop default (`DEFAULT_CONNECTION_LATENCY_MS`, now canonically in `src/lib/simulationDefaults.ts`, applied unconditionally by `workshopBridge.ts`'s `buildSimulationConfig`). Fixed every test harness to apply the real 5ms default (`scenarioScoring.test.ts`'s `run()`, `validator.test.ts`'s `runUnmodified()`) instead of loosening the live default to match fictional 0ms tuning.
- 24/27 scenarios needed no build changes — gate still passes, two with an honestly lower star count than documented (corrected in both the scenario header and the regression test): `adAuctionBidding` 2★→1★, `couponCodeRedemption` 2★→1★.
- The remaining 3 — `viralVideoComments`, `trendingProductSearch`, `globalLeaderboardUpdates` (the catalogue's "Cache + database-type, both required" scenarios) — turned out to be structurally broken at 5ms latency, not just mistuned: adding a Cache in front of the Database never improves p95 relative to skipping it, because p95 is a tail stat dominated by cache-miss traffic, and a miss pays the cache hop's network cost on top of the database hop's, for any hit rate this engine's key-distribution model produces. **Decision: keep cache+database-type as the taught `optimalSolution` anyway — the goal is the concept, not the objectively fastest topology — and retuned each scenario's p95 bar (26/30/26ms → 46/46/45ms respectively) so the shipped answer genuinely passes at real latency, with the unmodified starting scaffold still failing by a solid 3-6ms margin in every case.** A plain no-cache/NoSQL-only build also clears every gate in all three (and scores a higher composite — "legendary" territory) — left as a real, working, better-scoring alternative a student can discover, documented plainly in each header as a feature, not a bug.
- `npx vitest run`: 615/615 pass. `tsc`/`lint` both clean.
- **Needs a live spot-check, not urgent**: open `viral-video-comments`, `trending-product-search`, and `global-leaderboard-updates` in `/workshop`, reveal each reference solution, run it, confirm the toast/criteria panel shows 1★ (not "not solved") and the p95 row reads against the new bar (46ms/45ms/46ms). Both themes not required — this is a numbers check, not a visual one.

**Starting scaffold (Client→API→Database on every scenario) + root-cause Suggestions + budget/cost toggle — CONFIRMED**
- All 3 nodes render on `/workshop?scenario=recipe-of-the-day` (Client locked, API/Database unlocked, no lock icon) and on `checkout-timeout-mystery` (Client + API both locked/lock icon, Database unlocked) — both themes checked.
- Suggestions popover: root-cause SQL-vs-NoSQL, missing-Cache, and diagnose-the-bottleneck rules all fire with the exact copy written in `suggestionEngine.ts`, alongside (not duplicating) the existing generic "close to capacity" message.
- Budget Checking toggle: renders in the Inspector under Duration/Connection Latency, defaults to On, toggling Off hides the Budget row and flips the live score/toast to pass on latency+success alone, toggling back On correctly re-applies the budget gate (row reappears, stars drop back). Confirmed in both themes.

**Difficulty/topic filter chips on `/problems` — CONFIRMED correct; two stale prose counts fixed below**
- Live count across all 27 rows: difficulty 1→4, 2→7, 3→8, 4→6, 5→2. Clicking each difficulty chip filters correctly and matches this exactly (5/5 chip → exactly `global-leaderboard-updates` + `wildfire-alert-broadcast`; 4/5 chip → exactly `viral-video-comments`, `flight-status-push-updates`, `coupon-code-redemption`, `fitness-tracker-step-sync`, `api-gateway-slowdown`, `trending-product-search`). The wave-3 note below saying "4 at difficulty 4" was a stale headcount in this file's own prose, not a real bug — no code issue, just correcting the note here.
- `wildfire-alert-broadcast` and `api-gateway-slowdown` story copy read as clearly distinct from each other and from `slow-search-endpoint`/`warehouse-inventory-sync`. Both confirmed unaffected by the connection-latency fix above (neither is one of the 3 structurally-broken scenarios).

**8 new scenarios (validation wave) — Internal Admin Dashboard, Trending Hashtags Feed, IoT Sensor Ingestion, Concert Ticket Drop Confirmations, Slow Search Endpoint, Viral Video Comments, Ad Auction Bidding Events, Checkout Timeout Mystery**
- `/problems` topic/difficulty chip filtering confirmed generally correct (see entry above, superseding the per-wave counts originally written here).
- `ad-auction-bidding` retuned/corrected for the connection-latency fix (see entry above) — 1★, not 2★.
- Still open, not yet live-checked this pass: `checkout-timeout-mystery`'s bare-run success rate against its current header numbers (corrected earlier this session to ~10.3%, not the ~44% originally written here — unaffected by the connection-latency fix). `viral-video-comments`'s reference-solution spot-check is now folded into the connection-latency entry above.

**API Server failed every request when it had nothing wired downstream — broke 5 tutorial recipes; Reverse Proxy failed every request until a route was manually configured; Client's own tutorial ended by running a simulation with nothing to simulate**
- Reported live via a Load Balancer tutorial screenshot ("why are packets discarded at the client?") — see `docs/TUTORIAL-ENTITIES.md` §"Six real bugs", items 4, 5, and 6, for the full root-cause writeups. All three confirmed via `runSimulation`/`computeCurrentStep` run directly (not just unit tests) against the exact broken cases before and after each fix — `successRate` went from `0` to `1` in every simulation case; the Client step no longer returns `"run"`/`"results"`.
- Fix 1 (`src/simulation/entities/APIServer.ts`): a terminal API Server (nothing downstream — no Database) now answers directly via a new `respond()` helper instead of failing with `no_downstream_connection`. This was the root cause behind 5 separate broken recipes at once: the bare `[client, api]` recipe itself, plus `load_balancer`/`cdn`/`rate_limiter`/`reverse_proxy` (all of which deliberately end on `api`).
- Fix 2 (`src/simulation/entities/ReverseProxy.ts`): `selectTarget` now falls back to the first downstream target when `routes` is still completely empty (the untouched default), instead of matching nothing. Configuring even one real route switches it back to the documented strict matching behavior.
- Fix 3 (`src/lib/tutorialPlanner.ts`): `computeCurrentStep` now skips the run/results phase for any single-entity recipe (`recipe.length === 1` — only `client` today) and goes straight to a "complete" step whose body explains there's nothing downstream yet to run, instead of prompting a run that would just show a 0%-success results panel on the very first tutorial step. Multi-entity recipes are unchanged.
- **Needs a live pass** (unit-level `tsc`/lint/vitest — 469 tests — already pass): walk the `/tutorial` picker for API Server, Load Balancer, CDN, Rate Limiter, and Reverse Proxy from an empty canvas each time, all the way to "Run it" — confirm the results panel shows a healthy success rate (not the all-red/all-critical status this bug produced) and no failed-packet (✕) animation on any edge. Also spot-check a Reverse Proxy built with a real route configured in its "Routes" bespoke section still correctly drops traffic aimed at an unmatched route (the intentional lesson, must not have regressed). Separately, pick "Client" from the tutorial picker on an empty canvas — confirm it walks add → select → read config → straight to a completion step explaining there's nothing to run yet, with no "Run it"/results prompt in between. Both themes.

**`/tutorial` was a one-way door — no way back to `/workshop`, and the guide overlay persisted even after loading a scenario mid-tutorial**
- Reported bug: `WorkshopHeader`'s nav has a link *to* `/tutorial` but none back to `/workshop`, so once on `/tutorial` there was no way out short of editing the URL. Separately, `ScenariosMenu` (in the shared header) loads a scenario into the store without navigating away, so switching to a scenario mid-tutorial left `TourOverlay`/`TutorialPanel` sitting on top of it with only "choose a different tutorial" (which stays on `/tutorial` and re-plans) or "hide guide" (pause, not close) as options.
- Fix (`src/app/tutorial/TutorialPanel.tsx`): both panel states (entity picker and active-target readout) now have a persistent × ("Exit tutorial") that's a real `Link` to `/workshop`, distinct from the existing "choose a different tutorial" control (now a refresh icon, unchanged behavior). Since `WorkshopShell`/the zustand store are shared by both routes, the canvas is untouched by the navigation — only the tutorial overlay goes away.
- **REGRESSED by a later fix, confirmed live**: the × does navigate to `/workshop` and the tutorial overlay does go away cleanly, but the canvas is no longer intact — it now always lands blank. Root cause is `src/app/workshop/page.tsx`'s `ScenarioDeepLink`, added later by the "Three Workshop header/routing fixes" entry (still below): it unconditionally calls `reset()` on mount whenever the URL has no `?scenario=` param, on the assumption (stated in its own code comment) that "resetting is harmless when the canvas is already blank" — which is false for this exact path, since arriving at plain `/workshop` from the tutorial's × is precisely the case where the canvas often isn't blank. Live-verified: built a Client→API Server architecture via the API Server tutorial, clicked the picker's ×, landed on `/workshop` titled "Untitled Architecture" with an empty canvas — the just-built nodes were gone. This is a real conflict between two intentionally-shipped behaviors (tutorial exit preserving canvas vs. plain-`/workshop` always resetting stale scenario state), not a simple bug — needs a product decision on which should win (e.g. the reset could special-case navigation that arrives with existing nodes already on the canvas, or the tutorial exit could route through a param that opts out of the reset) rather than a one-line fix. Left open rather than guessed at.
- Still confirmed working as designed: picking a target shows both icons (refresh/×) and both navigate correctly; the Scenarios-dropdown-mid-tutorial → × path also exits cleanly with no leftover guide overlay (though per the regression above, a scenario loaded this way is likely also wiped by the same `reset()` unless it re-arrives via a `?scenario=` link). Not re-checked in light theme.

**Learn diagrams get a zoom button; DNS gets a real packet-transfer animation** — **CONFIRMED**: zoom button opens a larger centered modal on `/foundations/client-server-architecture`'s request-response figure (and earlier, on the Kafka zero-copy `compare` figure); that figure and the DNS "swiggy.com resolution, step by step" rail are both fully static (no dot); the new DNS recursive-lookup figure shows the dot looping Browser↔Resolver↔{Root,TLD,Authoritative} with per-leg captions crossfading in sync, no overlap, comfortable pace; play/pause holds/resumes state correctly and the zoom modal copy has independent state from the inline thumbnail. **Not verified**: `prefers-reduced-motion` (no tool support for forcing it in this environment) and light theme for the DNS figure specifically.

A queue of UI/behavior changes that need a manual claude-in-chrome pass
before they're considered done, batched instead of opening a browser tab
per small change. See `AGENTS.md` for the workflow this file supports.

When you finish a browser-verification session: delete the entries you
confirmed (don't just check them off — this file should only ever list
what's still unverified). If something failed verification, leave it
with a note on what broke instead of removing it.

**Diagram conversion pass — ascii `diagram` blocks → generic figure kinds (DIAGRAM-CONVERSION-PLAN.md)** — **SPOT-CHECKED, looks solid**: visited 6 of the highest-risk pages across both the Foundations and LLD conversions — `/foundations/nosql-deep-dive`'s decision-framework tree (deepest/widest tree at the time, scrolls cleanly within its own frame, YES/NO labels legible, terminal nodes correctly green), `/foundations/redis-deep-dive`'s clustering hash-slot architecture (widest architecture figure, scrolls within frame, each master's replicas visually grouped), `/foundations/kafka-deep-dive`'s zero-copy compare (checked earlier, see the text-shrink-fix entry) and its Kafka-vs-RabbitMQ decision tree (deepest tree in the app, renders cleanly, correct green terminal nodes), `/entities/rate-limiter` and `/entities/cache` (animated entity diagrams, unaffected), and `/lld/uml-class-diagrams`'s worked-example figure (composition's filled diamonds, inheritance's hollow triangle, multiplicity labels, and transition labels all render distinctly and correctly). All 6 rendered with no overflow, no clipped text, no crossing/tangled edges, and correct color-coding. Given this consistency across both diagram families (generic figures + the newer `uml` kind) and the shared-primitive text-sizing fix already confirmed separately, the remaining ~24 converted lesson pages (full list was in `DIAGRAM-CONVERSION-PLAN.md` §6) were not individually spot-checked this pass — flagging that rather than claiming a full sweep. Worth a closer look later at the ones the original notes called out as structurally novel: `/foundations/browser-request-lifecycle`'s two `sequence` figures (first lifeline diagrams in the app), `/foundations/vertical-vs-horizontal-scaling`'s bidirectional Request↔Cache pair, `/foundations/message-queues`'s `transitionLabel` compare, and `/lld/lru-cache`'s long node labels in a fixed-width `UmlDiagram` box.

## Files touched by everything below

Every file behind a pending item, deduplicated, so a verification pass
can see the full surface area at a glance instead of re-deriving it from
prose. Grouped by feature; a file repeated across groups is touched by
more than one pending item.

**New `/tutorial` page — guided, hands-on Workshop walkthrough, planned live against whatever's on the canvas**
- See `docs/TUTORIAL-ENTITIES.md` for the per-entity verification checklist (recipe sanity-check, bespoke-section coverage, the four canvas-state cases) — that doc owns tracking which entities are actually confirmed; don't duplicate that tracking here.
- **CONFIRMED live** — Load Balancer fan-out: walked API Server, Core Flow, and Load Balancer tutorials end to end this pass. After Client→Load Balancer→API Server connects, the next step correctly says "Add another API Server" (with the "nothing to spread work across" explanation) rather than jumping straight to selecting the Load Balancer; the second node auto-labels "API Server 2"; after connecting and running, the Inspector's Request Distribution shows two distinct rows ("API Server" 98/50%, "API Server 2" 97/50%) with traffic genuinely split. Connect-step callouts consistently anchor below the source node, never on top of the node being dragged to (checked across 3 different connect steps). (Note: while testing this, a React Flow node-drag that started a few pixels off the tiny connector handle grabbed and moved the whole node instead of drawing an edge, landing it visually on top of a neighboring node — this is the pre-existing "flaky synthetic-pointer-event" automation limitation this file already warns about elsewhere, not a product bug; a precise re-drag fixed it immediately.)
- **Not re-checked this pass**: `extendsRecipe` (picking a smaller target after building further shouldn't ask to remove the extra work), Message Queue/Replica Pool/Kafka building all the way to a connected Database, a 3rd API Server on a Load Balancer tutorial getting flagged for removal, and Replica Pool's own 2-Database fan-out.
- `src/components/tour/types.ts` (`TourStep` — `requiresAck` replaces the earlier `isComplete`), `useLiveRect.ts` (rAF-polled target-rect tracker), `TourOverlay.tsx` (spotlight box + anchored callout bubble; no more step counter — there's no fixed step count, see below)
- `src/components/workshop/WorkshopShell.tsx` (Workshop layout extracted out of `src/app/workshop/page.tsx` so `/workshop` and `/tutorial` share one implementation; takes an optional `overlay` slot; `data-tour-id="results-bar"` on the bottom bar)
- `src/app/workshop/page.tsx` (now just `<ScenarioDeepLink/>` + `<WorkshopShell/>`, no layout of its own)
- `src/lib/tutorialPlanner.ts` (new — `TUTORIAL_RECIPES`, one canonical Client-rooted chain per entity type, e.g. Cache → `[client, api, cache, database]`; `computeCurrentStep` diffs that recipe against the *live* canvas and returns the single next thing to do: remove an extraneous node, add a missing one, connect two, remove a stale shortcut edge that bypasses a newly-inserted entity, select it, read its config, run, read results, or a terminal "complete" step — recomputed fresh every call, no stored step index)
- `src/app/tutorial/TutorialRunner.tsx` (rewritten — owns `activeTarget`/`paused`/`acknowledgedIds`, no canvas reset on mount)
- `src/app/tutorial/TutorialPanel.tsx` (new — fixed bottom-left, non-modal; entity picker (Core Flow featured + full catalog) when no target is active, a compact status readout (current step, Pause/Resume, Choose another) once one is)
- `src/components/workshop/ComponentSidebar.tsx` (`data-tour-id="sidebar-component-<type>"` on each catalog card)
- `src/components/workshop/InspectorPanel.tsx` (`data-tour-id="inspector-config"` wraps the Configuration section)
- `src/components/workshop/WorkshopHeader.tsx` (`data-tour-id="run-simulation"` on the Run button; new "Tutorial" nav link next to "Learn")
- `src/app/page.tsx` (landing header: new "Tutorial" nav link next to "Learn")
- **Fixes a reported bug**: the × on the guide used to end the tutorial with no way back short of restarting. It now only pauses (`TutorialRunner`'s `paused` state) — `activeTarget`/`acknowledgedIds` are untouched, and the corner panel's "Resume guide" picks up exactly where it left off, recomputed live.
- **Needs a live pass** (mechanism-level — per-entity recipe correctness is tracked in `docs/TUTORIAL-ENTITIES.md` instead): the spotlight/callout tracking (Inspector opening on selection, canvas pan/zoom after `fitView`, window resize); Pause → Resume genuinely resumes (including after manually editing the canvas while paused); "Choose another tutorial" returns to the picker without touching the canvas; the picker panel and the TourOverlay callout never visually collide; full pass in both themes.

**All 4 budget-gated scenarios (Movie Ticket Booking, Flash Sale, URL Shortener, Parking Reservation Platform) — shared "given + budget + star rating" model**
- Movie Ticket Booking was already fully live-verified in an earlier session (locked Client, fail→pass→reference-solution→★★★ toast, all confirmed) — only light theme, ~1024px, and the 5-star tier itself (by design hard to beat live, covered by an automated test instead) were left unverified, and still are.
- **This pass, live-verified on `/workshop`**: loaded each of the 4 scenarios via the header Scenarios dropdown. URL Shortener walked end-to-end — locked Client (Request Rate + Key Pool Size both disabled with lock icons), "Before You Run" capacity prompt with working "Show the worked answer" toggle, reference solution loaded via "See a reference solution" (4-node Client→API→Cache→Database, correctly fit to view), Run Simulation produced 100% success / 22ms p95 / $510 of $1,400 → ★★★, a "🎉 URL Shortener solved ★★★" toast fired top-center, and — the new mechanism this scenario introduced — a 4th Success Criteria row, "Route traffic through a real service layer," appeared and went green alongside the other three (only renders post-run, not before, which reads as intentional since the whole Success Criteria list is post-run). Flash Sale spot-checked for its distinctive two-lock structure — Request Rate *and* Key Pool Size both disabled, Route Pool Size/Missing Key Rate left unlocked — confirmed correct. Parking Reservation Platform spot-checked for load correctness — its distinctive "wrong lever" briefing copy (API-vs-Database backpressure) rendered correctly with the same locked-field/budget structure.
- Also confirms, in passing: the Cost panel showing base cost pre-run with a trailing `+` ($94/mo+) and switching to a full post-run figure with no `+` ($510/mo) on the same architecture — the "Cost shown before a simulation ever runs" feature.
- **Not independently re-verified this pass** (time-boxed rather than exhaustive): Flash Sale's and Parking Reservation Platform's exact numeric fail/pass/★★★ thresholds and their own reference solutions; the architecture-gate's *red* state (wiring a Client straight to a Database, bypassing the API Server) — only its green/passing state was observed live; light theme and ~1024px for any of the 4 (see the top-of-file note on the `resize_window` tool limitation in this environment).

**New `/lld` module — third reading room, all 15 lessons complete (Fundamentals + Design Patterns + Case Studies)**
- `src/content/shared/lesson.ts` (new — `LessonBlock`/`LessonSection`/`LessonExercise` extracted out of `foundations/types.ts`, now shared by both modules)
- `src/content/foundations/types.ts` (re-exports the shared types instead of defining its own; no shape change)
- `src/content/foundations/lessons/01-what-is-system-design.ts` (one added "insight" block pointing at `/lld`, noted in the file's own header comment)
- `src/components/content/LessonBlockRenderer.tsx` (moved from `src/components/foundations/`, now module-agnostic)
- `src/app/foundations/[slug]/page.tsx` (import path updated for the move above; no behavior change)
- `src/content/lld/types.ts`, `src/content/lld/index.ts`, `src/content/lld/__tests__/content.test.ts`
- `src/content/lld/lessons/01-what-is-lld.ts` through `05-lld-interview-approach.ts` (Fundamentals — OOP pillars, SOLID, UML class-diagram relationships, the 7-step interview framework)
- `src/content/lld/lessons/06-creational-patterns.ts` through `08-behavioral-patterns.ts` (Design Patterns — Singleton/Factory Method/Abstract Factory/Builder/Prototype; Adapter/Decorator/Facade/Proxy/Composite; Strategy/Observer/State/Command/Chain of Responsibility/Template Method)
- `src/content/lld/lessons/09-parking-lot.ts`, `10-elevator-system.ts`, `11-tic-tac-toe.ts` (Case Studies — Parking Lot applies Singleton + Strategy over the exact class diagram from Lesson 4's worked example; Elevator System pairs State with a separate dispatch Strategy; Tic-Tac-Toe generalizes to N×N/K-in-a-row via one Strategy)
- `src/content/lld/lessons/12-lru-cache.ts`, `13-splitwise.ts`, `14-movie-ticket-booking.ts`, `15-rate-limiter.ts` (Case Studies, continued — LRU Cache cross-links the Cache entity's own LRU/LFU/FIFO/MRU eviction comparison; Splitwise combines Strategy (split types) + Observer (balance-change notifications); Movie Ticket Booking is the code-level correctness counterpart to `src/scenarios/movieTicketBooking.ts`'s infrastructure-capacity story — pessimistic seat locking, not the Workshop's connection-pool bottleneck; Rate Limiter cross-links the Rate Limiter entity's own Token Bucket vs. Sliding Window comparison)
- `src/app/lld/page.tsx`, `src/app/lld/[slug]/page.tsx` (new routes)
- `src/app/learn/page.tsx` (third card added, grid now 3-column, copy updated from "Two reading rooms" to "Three reading rooms")
- Remaining Case Studies (LRU Cache, Splitwise, Movie Ticket Booking, Rate Limiter — 4 lessons) still to come — this entry will grow, not get re-created, as the final batch lands.

**New `/foundations` module — System Design theory, all 18 Phase 1 lessons**
- `src/content/foundations/types.ts`, `src/content/foundations/index.ts`, `src/content/foundations/lessons/01-what-is-system-design.ts` through `18-cdn.ts` (18 lesson files, transcribed from a shared chat, Indian-placement-interview framing kept per the project's own adapt-closely decision; a handful of short spans behind the source chat's own client-side "cookie/query-string" content filter were paraphrased tightly from surrounding verbatim text rather than invented — noted in each affected lesson's own file header comment)
- `src/components/foundations/LessonBlockRenderer.tsx` (new — paragraph/diagram/table/code/insight/list/qa block renderer)
- `src/app/foundations/page.tsx`, `src/app/foundations/[slug]/page.tsx` (new routes, mirror `/entities`'s visual pattern, reusing `EntityTableOfContents`)
- `src/app/learn/page.tsx` (new hub page — links to both Foundations and Entities)
- `src/components/workshop/WorkshopHeader.tsx` ("Learn" button retargeted from `/entities` to `/learn`)
- `src/app/page.tsx` (landing page: header "Learn" link retargeted, new Foundations chip section added before the Entities chip section)

**New mechanism: architecture-change remedies — first demo, API Server / Queue Saturation**
- `src/lib/entityDeepDive.ts` (`ConfigRemedy`/`ArchitectureRemedy` union, `kind` added to all prior remedies, `api`'s `Queue Saturation (Backpressure Collapse)` failure mode's `demo` field — 1 architecture remedy: Load Balancer + 2nd API Server)
- `src/store/failureDemoStore.ts` (`addNode`, `onConnect`, `activeArchitectureRemedy`, `applyRemedy`/`compareRemedy`/`onEdgesChange` branching on `remedy.kind`)
- `src/lib/failureDemoBridge.ts` (`buildDemoSimulationConfig` now derives connections from live edges; new `buildReferenceSimulationConfig`)
- `src/components/failure-demo/FailureDemoCanvas.tsx` (drag/drop, `onConnect`, `deleteKeyCode` wired — all no-op outside architecture mode)
- `src/components/failure-demo/RemediesPanel.tsx` (`ArchitectureRemedyPalette`, `RemedyCard`'s architecture branch, `knownEntitiesFor`)

**New "Try It" demo — Cache / Cache Penetration**
- `src/lib/entityDeepDive.ts` (`cache`'s `Cache Penetration` failure mode's `demo` field — 1 remedy: Negative Caching On)

**New "Try It" demo — Cache / Cache Avalanche**
- `src/lib/entityDeepDive.ts` (`cache`'s `Cache Avalanche` failure mode's `demo` field — 1 remedy: Raise TTL Jitter; a deliberately partial-improvement demo, not a crash/recovery flip — see the demo's own code comment)

**New "Try It" demo — Message Queue / Backlog Overflow**
- `src/lib/entityDeepDive.ts` (`message_queue`'s `Backlog Overflow` failure mode's `demo` field — 2 remedies: Raise Consumer Count (fix), Raise Max Queue Length (honest partial))

**New "Try It" demo — Kafka / Wasted Consumers Past the Partition Ceiling**
- `src/lib/entityDeepDive.ts` (`kafka`'s `Wasted Consumers Past the Partition Ceiling` failure mode's `demo` field — 2 remedies: Raise Partition Count (fix), Raise Consumers per Group (deliberately inert, bit-for-bit identical to broken — proves "wasted"); no client-facing Crashed→healthy flip, only Kafka's own node status changes — see the demo's own code comment)

**New "Try It" demo — Load Balancer / Weighted Misconfiguration**
- `src/lib/entityDeepDive.ts` (`load_balancer`'s `Weighted Misconfiguration` failure mode's `demo` field — 1 remedy: Fix Target Weights, set to the real 1:25 capacity ratio rather than a naive value swap)

**New "Try It" demo — Reverse Proxy / No Matching Route**
- `src/lib/entityDeepDive.ts` (`reverse_proxy`'s `No Matching Route` failure mode's `demo` field — 1 remedy: Configure a Catch-all Target; broken state reads steady-red "error" not pulsing "Crashed" — a structural ceiling (88.5%, Route Pool Size caps at 8), not undertuning, see the demo's own code comment)

**New "Try It" demo — API Server / Latency Cliff from Processing Time**
- `src/lib/entityDeepDive.ts` (`api`'s `Latency Cliff from Processing Time` failure mode's `demo` field — 1 remedy: Lower Processing Time; only Processing Time changes between broken and fix, Max Concurrent/Max Queue Length held constant)

**New "Try It" demo — Client / Defeating a Cache with Key Pool Size**
- `src/lib/entityDeepDive.ts` (`client`'s `Defeating a Cache with Key Pool Size` failure mode's `demo` field — 1 remedy: Lower Key Pool Size; Cache set to coalesced stampede protection so a hot key's concurrent misses don't confound the measurement, see the demo's own code comment)

**New "Try It" demo — Load Balancer / Uneven Backend Divergence**
- `src/lib/entityDeepDive.ts` (`load_balancer`'s `Uneven Backend Divergence` failure mode's `demo` field)

**Try It Compare panel — per-entity breakdown + gated on a real Run**
- `src/simulation/metrics/MetricsCollector.ts` (requestCount fallback for zero-capacity routers)
- `src/lib/costEngine.ts` (comment only, no logic change)
- `src/lib/__tests__/costEngine.test.ts`
- `src/store/failureDemoStore.ts` (`compareRemedy`)
- `src/components/failure-demo/RemediesPanel.tsx` (`EntityMetricsComparison`, Compare button gating)

**Cache Penetration / Avalanche `simulated` flags corrected**
- `src/lib/entityDeepDive.ts` (`cache`'s `pros`/`cons`, and the `Cache Penetration`/`Cache Avalanche` failure modes — now `simulated: true` with real `reproduce` steps, matching what `Cache.ts` already does)

**New Reverse Proxy entity**
- `src/simulation/entities/ReverseProxy.ts`
- `src/simulation/__tests__/ReverseProxy.test.ts`
- `src/lib/entityCatalog.ts`
- `src/lib/entityDeepDive.ts` (`reverse_proxy` entry)
- `src/components/workshop/InspectorPanel.tsx` (`ReverseProxyRoutesSection`)
- `src/app/entities/[slug]/page.tsx` → `/entities/reverse-proxy`

**New Kafka entity**
- `src/simulation/entities/Kafka.ts`
- `src/simulation/__tests__/Kafka.test.ts`
- `src/lib/entityCatalog.ts`
- `src/lib/entityDeepDive.ts` (`kafka` entry)
- `src/components/workshop/InspectorPanel.tsx` (generalized `LoadBalancerDistributionSection` → "Partition Distribution" + "Consumer Group Distribution")
- `src/app/entities/[slug]/page.tsx` → `/entities/kafka`

**CDN Edge Map — interactive plane with draggable User/Origin pins**
- `src/components/workshop/CDNEdgeMap.tsx`
- `src/simulation/entities/CDN.ts`
- `src/simulation/entities/weightedRouting.ts`
- `src/components/workshop/InspectorPanel.tsx` (`useCDNActivePulse`, `CDNEdgeSection`)

**Inspector config-field hint overflow fix**
- `src/components/workshop/InspectorPanel.tsx` (`FieldHint`)

**`/entities` index + `/entities/[slug]` deep-dive pages + Learn tab + landing chips**
- `src/app/entities/page.tsx` — also the source of the `GET /entities 500` (`tagline` undefined) seen mid-session; re-check first
- `src/app/entities/[slug]/page.tsx`
- `src/lib/entityDeepDive.ts`
- `src/app/page.tsx` (landing page entity chips)
- `src/components/workshop/WorkshopHeader.tsx` ("Learn" button)

**Cost shown before a simulation ever runs**
- `src/lib/costEngine.ts`
- `src/lib/__tests__/costEngine.test.ts`
- `src/components/workshop/CostPanel.tsx`
- `src/components/workshop/InspectorPanel.tsx` (`EstimatedCostSection`)

**Cache Penetration + Avalanche (negative caching, TTL jitter)**
- `src/simulation/entities/Cache.ts` (`negativeCaching`, `ttlJitterPercent`)
- `src/simulation/entities/CacheStore.ts` (per-entry TTL, `"hit"/"cold"/"expired"` lookup)
- `src/simulation/engine/TrafficGenerator.ts` (`assignPhantomKey`, `assignRequestExistence`)
- `src/simulation/entities/Client.ts` (Client's new Missing Key Rate field)
- `src/lib/entityConfigSchema.ts` (`missingKeyRate` on client, `negativeCaching`/`negativeCacheTtlMs`/`ttlJitterPercent` on cache)
- `src/components/workshop/InspectorPanel.tsx` (`CachePenetrationSection`, `CacheAvalancheSection`)

**New "Try It" demo — Database / Connection Pool Exhaustion**
- `src/lib/entityDeepDive.ts` (`database`'s `Connection Pool Exhaustion` failure mode's `demo` field — 3 remedies: Raise Max Connections, Raise Max Queue Length, Switch to NoSQL)

**New "Try It" demo — Rate Limiter / Burst Rejection Divergence**
- `src/lib/entityDeepDive.ts` (`rate_limiter`'s `Burst Rejection Divergence` failure mode's `demo` field — 2 remedies: Raise Requests/Second, Switch to Token Bucket)

**"Try It" failure-mode demo pages (Cache Stampede, first one built)**
- `src/lib/entityDeepDive.ts` (`Remedy`, `FailureModeDemo`, `slugFromFailureModeName`, `getFailureModeDemo`, Cache Stampede's `demo`)
- `src/store/failureDemoStore.ts` (new, isolated from `workshopStore`)
- `src/lib/failureDemoBridge.ts`
- `src/lib/nodeStatus.ts` (extracted from `workshopStore.ts` — shared by both stores now)
- `src/components/failure-demo/` (`FailureDemoWorkspace`, `FailureDemoHeader`, `RemediesPanel`, `FailureDemoCanvas`, `FailureDemoInspector`, `FailureDemoPlaybackBar`, `FailureDemoResultsBar`)
- `src/app/entities/[slug]/try/[failureModeSlug]/page.tsx` (new route)
- `src/app/entities/[slug]/page.tsx` (new "Try it" CTA on failure-mode cards that have a `demo`)

## Pending

- **"Try It" Remedies Compare popup + collapsed Inspector sections — CONFIRMED** on `/entities/load_balancer/try/uneven-backend-divergence`: Inspector sections all start collapsed, clicking a node expands only its own section; Compare opens a centered "Compare — Least Connections" dialog with the "Same seed & traffic" badge, "What changed" (Algorithm: Round Robin → Least Connections), worst-hit component row, two colored stat cards, and a readable per-component table; Escape closes it. Not re-checked: the architecture-change demo's "What to build" variant, the weights/routes id-keyed fallback formatting, light theme, ~1024px.

- **Stale pre-redesign color classes fixed — CONFIRMED**: on `/workshop`, a running simulation's packet dots resolve to `fill-status-healthy` → `rgb(47, 188, 136)` (real green, verified via computed style, not black); `CDNEdgeMap.tsx` no longer references any of the old broken class names (grepped clean). Not re-checked live: the failure/red-X dot color, light theme, or the CDN map's own ping/pin/legend colors in the browser.

- **"Try It" failure-demo header Reset button — CONFIRMED** on the same page: clicking it after a run cleared the results/playback bar and returned the canvas to the broken baseline, Compare re-disabled. Not re-checked on Cache Stampede specifically or in light theme.

- **"Show the worked answer" exercise guidance restructured — CONFIRMED**: `/lld/lru-cache`'s worked answer shows a proper Java syntax-block code sample followed by clearly spaced bullets with real bold inline labels (`frequency`, `freqToKeys`, `onAccess`/`onInsert`, `evictionCandidate()`, `minFreq`) and inline-code-styled identifiers (`LinkedHashSet`, `freqToKeys.get(minFreq)`) — not a wall of text, not literal `**`/backticks. Not spot-checked: `/foundations/browser-request-lifecycle`'s table-shaped worked answer, `/lld/uml-class-diagrams`/`behavioral-patterns`'s numbered-list markers, or the `gap-3` list-spacing regression check on non-guidance lists.

- **New `/quest` route — milestones 4–7, Landing + Learning World Map + the Load Balancer lesson as a real sequence** (`src/styles/quest.css`, `src/app/quest/layout.tsx`, `src/app/quest/page.tsx`, `src/app/quest/map/page.tsx`, `src/app/quest/lesson/load-balancers/page.tsx`; `src/components/quest/mascot/Mascot.tsx`, `mascotStates.ts`, `skins/sparkySkin.tsx`; `src/components/quest/GameButton.tsx`, `DialogueBox.tsx`, `LessonNode.tsx`, `MapPath.tsx`, `ConceptObject.tsx`, `SystemDiagram.tsx`, `AnswerChoice.tsx`, `SceneStage.tsx`; `src/content/quest/worldMap.ts`, see `docs-game/CLAUDE.md`) — a brand-new, isolated design system (bright navy/cream/yellow/blue/coral/green palette, Fredoka/Nunito fonts) composed into its first three real screens, never rendered in a browser. Supersedes and replaces the earlier milestone 1–3 preview-page check (that page no longer exists). Needs a check that: **on `/quest`** — loads full-bleed with the bright palette and looks nothing like the rest of the site (Trace's dark hairline theme must not bleed in); a faint static starfield + hill-silhouette shows behind the content (not animated — intentional); a yellow "SYSTEM DESIGN QUEST" title renders in the rounded display font with a visible offset drop-shadow; Sparky greets in "explaining" pose via a typewriter dialogue box, advances to line 2 (mascot → "excited"), and a "Begin Adventure" button appears; clicking it navigates to `/quest/map`. **On `/quest/map`** — a "← Back" link and "The World Map" title sit above a tall (scrollable) area showing 5 circular nodes on a dashed trail, zigzagging bottom to top: "The Load Balancer Gate" pulsing yellow at the bottom (the only clickable one, navigates to `/quest/lesson/load-balancers`); 4 dimmed locked nodes above it do nothing when clicked. **On `/quest/lesson/load-balancers`** — this is now a real 3-beat sequence, not a static page: **intro** — Sparky in "warning" pose (small shake) above a dialogue box reading "Uh oh! Everyone is trying to enter through ONE door!", above a diagram of 4 blue user icons converging via coral lines onto one wobbling coral server labeled "The one server"; clicking/tapping the dialogue line (or Enter/Space) completes the typewriter instantly if still running, then advances to **concept** — Sparky switches to "explaining", the dialogue box explains the load-balancer fix, and the diagram now shows the same 4 users converging onto a load-balancer icon (circle with a forking-arrow glyph) which fans out via plain lines to 3 green, non-wobbling servers (A/B/C); advancing again reaches **question** — the dialogue box asks "what actually fixes it?" and, once its text finishes typing, shows 3 clickable answer cards ("Spread the requests across multiple servers" / "Buy one much bigger server" / "Cache every response forever"); the mascot is "thinking" here until an answer is picked. Clicking the correct option locks it in green with a checkmark and a small pop, the other two cards dim, and the mascot switches to "happy" — **this is the current stopping point, there is no further "continue"/celebration/return-to-map action yet, that's expected, not a bug** (milestone 8). Clicking a wrong option turns it coral with an X and a quick shake, the mascot switches to "confused", and after roughly a second it automatically resets — all 3 cards become clickable again, mascot back to "thinking" — confirm this reset actually happens and doesn't get stuck. Confirm none of this is affected by the site's light/dark theme toggle (Quest has no theme toggle). No horizontal scroll/overflow at ~1024px and on a narrow (~375px) viewport on any of the three pages.
- **New `/lld` module — third reading room, all 15 lessons complete — CONFIRMED core structure, spot-checked content**: `/learn` shows exactly 3 cards with "Three reading rooms"; `/lld` groups 5 Fundamentals / 3 Design Patterns / 7 Case Studies cards correctly, in number order; prev/next nav correctly crosses both group boundaries (5→6, 8→9 checked live); `/lld/uml-class-diagrams`'s relationship markers (diamonds/triangles) and `/lld/behavioral-patterns`'s back-to-back Java code blocks both render cleanly with no overflow; `/lld/lru-cache`'s "Related entities" box correctly links to and lands on `/entities/cache`; `/foundations/load-balancers` still renders correctly post-move (regression check passed).
  - **Not individually checked**: lesson-1-prev/lesson-15-next being blank, `/lld/creational-patterns`+`structural-patterns` code blocks, `/lld/parking-lot`/`elevator-system`/`tic-tac-toe`/`splitwise`/`movie-ticket-booking`/`rate-limiter`'s specific dense blocks and tables, `rate-limiter`'s own related-entities link, `/foundations/what-is-system-design`'s hld-vs-lld callout, and light theme throughout.
- **New "Try It" demo — Client / Defeating a Cache with Key Pool Size** (thirteenth demo built with the pattern from `docs/Learn-Problem-Solution.md`) — never rendered in a browser yet. Needs a check that: on `/entities/client`, the "Defeating a Cache with Key Pool Size" failure-mode card shows a "Try it" button ("Thundering Herd" should not, it has no `demo`); it navigates (same tab) to `/entities/client/try/defeating-a-cache-with-key-pool-size`; the page loads a 3-node Client → Cache → Database graph already positioned and connected, Client's Configuration showing Key Pool Size 100000, Cache's Configuration showing Capacity 50, Stampede Protection "Coalesced", Database's Configuration showing Max Connections 1, Max Queue Length 6, Processing Time 60ms; the Inspector lists all 3 nodes as expanded-by-default dropdowns; clicking Run Simulation plays back and the Results bar + canvas show the Database pulsing red/Crashed (~91% failure rate), overall success rate ~10%, and the Cache's own Inspector shows hit rate collapsed near 0%; the Cache's own node status should read "error"/steady red at most (not pulsing "Crashed" — it forwards the Database's failures but only ~31% of its own attempts fail); in the Remedies panel (left, under "Solutions"), there is exactly one remedy card, "Lower Key Pool Size" — clicking it sets the Client's Key Pool Size to 10, clears the stale results back to "Run the simulation…", and re-running shows the Cache's hit rate jump to ~95%, the Database fully healthy (green, 0 errors), overall success rate 100% — confirm the remedy's description correctly notes the Cache's own Capacity/Eviction Policy never changed; the Compare button is disabled ("Run the simulation once to enable Compare") until Run Simulation has been clicked once, then shows an inline before/after (status line, per-entity utilization/error breakdown for Cache/Database, success rate, avg latency); the "Broken (starting state)" card restores Key Pool Size to 100000 after the remedy was applied; breadcrumb "← Client" returns to `/entities/client`. No layout overflow at ~1024px, light and dark theme.
- **New "Try It" demo — API Server / Latency Cliff from Processing Time** (twelfth demo built with the pattern from `docs/Learn-Problem-Solution.md`, second for API Server after Queue Saturation) — never rendered in a browser yet. Needs a check that: on `/entities/api`, the "Latency Cliff from Processing Time" failure-mode card shows a "Try it" button; it navigates (same tab) to `/entities/api/try/latency-cliff-from-processing-time`; the page loads a 2-node Client → API Server → Database graph (3 nodes total, Database included) already positioned and connected, Client's Configuration showing Request Rate 150, API Server's Configuration showing Max Concurrent 5, Max Queue Length 6, Processing Time 200ms; the Inspector lists all 3 nodes as expanded-by-default dropdowns; clicking Run Simulation plays back and the Results bar + canvas show the API Server pulsing red/Crashed (~91% failure rate), overall success rate ~6.5%, p95 latency ~987ms; in the Remedies panel (left, under "Solutions"), there is exactly one remedy card, "Lower Processing Time" — clicking it sets Processing Time to 10ms (Max Concurrent and Max Queue Length unchanged), clears the stale results back to "Run the simulation…", and re-running shows the API Server fully healthy (green, 0 errors), overall success rate 100%, p95 latency dropping sharply (~42ms) — confirm the Results bar's latency figure is visibly, not just numerically, different; the Compare button is disabled ("Run the simulation once to enable Compare") until Run Simulation has been clicked once, then shows an inline before/after (status line, per-entity utilization/error breakdown for Client/API Server/Database, success rate, avg latency); the "Broken (starting state)" card restores Processing Time to 200ms after the remedy was applied; breadcrumb "← API Server" returns to `/entities/api`. No layout overflow at ~1024px, light and dark theme.
- **New "Try It" demo — Reverse Proxy / No Matching Route** (eleventh demo built with the pattern from `docs/Learn-Problem-Solution.md`, first for Reverse Proxy) — never rendered in a browser yet, and its broken state has a real, structural ceiling worth checking for specifically: it reads steady-red "error" on the Reverse Proxy node, not pulsing red "Crashed" (see the demo's own code comment for why — Route Pool Size caps at 8, so the worst possible miss rate here is 7/8 = 87.5%, always under this app's 90% threshold). Needs a check that: on `/entities/reverse-proxy`, the "No Matching Route" failure-mode card shows a "Try it" button ("Route Misconfiguration (Silent Starvation)" should not — it has no `demo`, deliberately skipped, see `docs/Learn-Problem-Solution.md` §8); it navigates (same tab) to `/entities/reverse-proxy/try/no-matching-route`; the page loads a 4-node Client → Reverse Proxy → API Server (Orders) → Database (Orders) graph already positioned and connected, Client's Configuration showing Route Pool Size 8, Reverse Proxy's Routes section showing "API Server (Orders)" assigned to "/orders" (not the catch-all); the Inspector lists all 4 nodes as expanded-by-default dropdowns; clicking Run Simulation plays back and the Results bar shows overall success rate ~11%, the Reverse Proxy's node dot reads steady red ("error"), not pulsing ("Crashed") — confirm this explicitly, it's the point of this check, not a bug; in the Remedies panel (left, under "Solutions"), there is exactly one remedy card, "Configure a Catch-all Target" — clicking it updates the Reverse Proxy's Routes so "API Server (Orders)" is assigned to the catch-all (`*`) instead of "/orders", clears the stale results back to "Run the simulation…", and re-running shows the Reverse Proxy fully healthy (green, 0 errors), overall success rate 100%; the Compare button is disabled ("Run the simulation once to enable Compare") until Run Simulation has been clicked once, then shows an inline before/after (per-entity utilization/error breakdown for Reverse Proxy/API Server/Database, overall success rate, avg latency); the "Broken (starting state)" card returns the route to "/orders" when clicked after the remedy was applied; breadcrumb "← Reverse Proxy" returns to `/entities/reverse-proxy`. No layout overflow at ~1024px, light and dark theme.
- **New "Try It" demo — Load Balancer / Weighted Misconfiguration** (tenth demo built with the pattern from `docs/Learn-Problem-Solution.md`, second demo for Load Balancer after Uneven Backend Divergence) — never rendered in a browser yet. Needs a check that: on `/entities/load_balancer`, the "Weighted Misconfiguration" failure-mode card shows a "Try it" button ("Load Balancer as SPOF" should not, it has no `demo`); it navigates (same tab) to `/entities/load_balancer/try/weighted-misconfiguration`; the page loads a 6-node graph (Client → Load Balancer → API Server (low capacity) / API Server (high capacity), each → its own Database) already positioned and connected, Load Balancer's Configuration showing Algorithm "Weighted Round Robin" and a Target Weights section with API Server (low capacity)=4, API Server (high capacity)=1; the Inspector lists all 6 nodes as expanded-by-default dropdowns; clicking Run Simulation plays back and the Results bar + canvas show "API Server (low capacity)" pulsing red/Crashed (~90% failure rate) while "API Server (high capacity)" and both Databases stay healthy, overall success rate around 21%; the Load Balancer's own node status should read "error"/steady red at most (not pulsing "Crashed" — it forwards ~4,097 failures but routed ~5,163 requests total, ~44% of its own attempts, under the 90% threshold); in the Remedies panel (left, under "Solutions"), there is exactly one remedy card, "Fix Target Weights" — clicking it updates the Load Balancer's Target Weights to low=1/high=25 (not a simple 1:4 swap — confirm the description explains why), clears the stale results back to "Run the simulation…", and re-running shows "API Server (low capacity)" fully healthy (green, 0 errors) and "API Server (high capacity)" also fully healthy, overall success rate 100%; the Compare button is disabled ("Run the simulation once to enable Compare") until Run Simulation has been clicked once, then shows an inline before/after — a status line naming "API Server (low capacity)" (not "Load Balancer") as Crashed → Healthy, a per-entity utilization/error row for each of Load Balancer / both API Servers / both Databases, overall success rate, and avg latency; the "Broken (starting state)" card returns the Load Balancer's weights to low=4/high=1 when clicked after the remedy was applied; breadcrumb "← Load Balancer" returns to `/entities/load_balancer`. No layout overflow at ~1024px, light and dark theme.
- **New "Try It" demo — Kafka / Wasted Consumers Past the Partition Ceiling** (ninth demo built with the pattern from `docs/Learn-Problem-Solution.md`, and a new shape — no client-facing "Crashed → healthy" flip at all, see the demo's own code comment for why) — never rendered in a browser yet. Needs a check that: on `/entities/kafka`, the "Wasted Consumers Past the Partition Ceiling" failure-mode card shows a "Try it" button ("One Consumer Group Falling Behind" should not, it has no `demo`); it navigates (same tab) to `/entities/kafka/try/wasted-consumers-past-the-partition-ceiling`; the page loads a 3-node Client → Kafka → Analytics Service (an API Server) graph already positioned and connected, Client's Configuration showing Request Rate 720 / Key Pool Size 200, Kafka's Configuration showing Partition Count 1, Consumers per Group 12, Max Queue Length 20, Dispatch Time 15ms; the Inspector lists all 3 nodes as expanded-by-default dropdowns; clicking Run Simulation plays back and the canvas shows the Kafka node pulsing red/Crashed while the Results bar's overall success rate stays ~100% (this is expected, not a bug — confirm the remedy card text explains why); Kafka's own Inspector shows its Consumer Group Distribution reflecting the high rejection rate for the one connected group; in the Remedies panel (left, under "Solutions"), there are exactly two remedy cards — "Raise Consumers per Group" and "Raise Partition Count" — mutually exclusive; clicking "Raise Consumers per Group" sets Consumers per Group to 20, clears stale results, and re-running shows the Kafka node **still pulsing red/Crashed**, numbers bit-for-bit identical to the broken run (422 admitted/3818 rejected) — confirm the remedy's own description explicitly frames this as proof of zero effect, not an attempted fix that happened to fail; clicking "Raise Partition Count" instead sets Partition Count to 12 (Consumers per Group unchanged at 12) and re-running shows the Kafka node fully healthy (green, 0 rejections); each remedy's Compare button is disabled with a "Run the simulation once to enable Compare" hint until Run Simulation has been clicked at least once, then shows a live-computed before/after (per-entity utilization/error breakdown for Client/Kafka/Analytics Service, success rate, avg latency) — confirm the "Raise Consumers per Group" comparison visibly shows identical before/after numbers, not just a small delta; the "Broken (starting state)" card restores Partition Count=1, Consumers per Group=12 together after any remedy was applied; breadcrumb "← Kafka" returns to `/entities/kafka`. No layout overflow at ~1024px, light and dark theme.
- **New "Try It" demo — Message Queue / Backlog Overflow** (eighth demo built with the pattern from `docs/Learn-Problem-Solution.md`, after Cache Stampede, Load Balancer, Database, Rate Limiter, API Server's architecture-change demo, Cache Penetration, and Cache Avalanche) — never rendered in a browser yet. Needs a check that: on `/entities/message_queue`, the "Backlog Overflow" failure-mode card shows a "Try it" button ("Fan-out Load Multiplication" should not, it has no `demo`); it navigates (same tab) to `/entities/message_queue/try/backlog-overflow`; the page loads a 3-node Client → Message Queue → Database graph already positioned and connected, Client's Configuration showing Request Rate 500 / Key Pool Size 200, Message Queue's Configuration showing Delivery Mode "Queue", Consumer Count 2, Max Queue Length 20, Dispatch Time 30ms; the Inspector lists all 3 nodes as expanded-by-default dropdowns; clicking Run Simulation plays back and the Results bar + canvas show the Message Queue pulsing red/Crashed (~92% failure rate), overall success rate around 14%; in the Remedies panel (left, under "Solutions"), there are exactly two remedy cards — "Raise Consumer Count" and "Raise Max Queue Length" — mutually exclusive; clicking "Raise Consumer Count" sets Consumer Count to 40, clears stale results, and re-running shows the Message Queue fully healthy (green, 0 rejections), overall success rate 100%; clicking "Raise Max Queue Length" instead sets Max Queue Length to 500 (Consumer Count unchanged at 2) and re-running shows a meaningfully better but still unhealthy queue (~82% failure rate, no longer pulsing "Crashed" red but still showing rejections) — confirm the card's description reads as an honest partial mitigation, not a fix; each remedy's Compare button is disabled with a "Run the simulation once to enable Compare" hint until Run Simulation has been clicked at least once, then shows a live-computed before/after (status line, per-entity utilization/error breakdown for Client/Message Queue/Database, success rate, avg latency); the "Broken (starting state)" card restores Consumer Count=2, Max Queue Length=20 together after any remedy was applied; breadcrumb "← Message Queue" returns to `/entities/message_queue`. No layout overflow at ~1024px, light and dark theme.
- **New scenario — Parking Reservation Platform** (`src/scenarios/parkingReservationPlatform.ts`, registered in `src/scenarios/index.ts`, catalogue entry in `docs/scenarios.md`) — a 4th scenario, first to showcase Reverse Proxy as the intended fix rather than Load Balancer/Cache; never rendered in a browser yet. Needs a check that: it appears as a 4th card in the Scenarios tab of the Component Library (title "Parking Reservation Platform", difficulty ★★★), selecting it replaces the canvas with the 3-node Client→API→Database starting graph; the Inspector's "Before You Run" box shows the capacity-estimate prompt with a working "Show the worked answer" toggle; clicking Run Simulation shows both the API Server and the Database reading as unhealthy/near-capacity together (not just one of them, unlike the other 3 scenarios) with overall success rate ~84%; the "What Happened" box appears post-run with real resolved numbers, no literal `{{...}}` placeholders; dragging in a Reverse Proxy plus a second API Server + Database, wiring `/checkout` to one pair and `*` (catch-all) to the other, deleting the original Client→API edge, and re-running should be able to clear both constraints (~99%+ success) — confirm this is buildable through normal canvas drag/connect/delete, not just the test suite's programmatic config; Restart returns to the starting graph. No layout overflow at ~1024px, light and dark theme.

- **New "Try It" demo — Cache / Cache Avalanche** (seventh demo built with the pattern from `docs/Learn-Problem-Solution.md`) — never rendered in a browser yet, and deliberately a different shape than every prior demo: it does NOT produce a "Crashed → Healthy" flip, only a real, honest partial improvement — see the demo's own code comment in `entityDeepDive.ts` for why. Needs a check that: on `/entities/cache`, the "Cache Avalanche" failure-mode card shows a "Try it" button; it navigates (same tab) to `/entities/cache/try/cache-avalanche`; the page loads a 3-node Client → Cache → Database graph already positioned and connected, Client's Configuration showing Request Rate 1000 / Key Pool Size 8, Cache's Configuration showing Capacity 8, TTL 2000ms, TTL Jitter 0%, Stampede Protection "Naive"; the Inspector lists all 3 nodes as expanded-by-default dropdowns; clicking Run Simulation plays back and the Results bar shows overall success rate ~96% (most traffic is cache hits, unaffected), while the Cache's own Inspector "Avalanche" section shows Expired Misses ~16, Peak Burst (100ms) ~5, with callout text warning about a synchronized wave — **the Database's node-health dot should read steady red ("error"), not pulsing red ("Crashed")**, since its own failure rate (~70%) doesn't cross this app's 90% threshold, unlike every other demo's broken state; in the Remedies panel (left, under "Solutions"), there is exactly one remedy card, "Raise TTL Jitter" — clicking it sets TTL Jitter to 50%, clears stale results, and re-running shows the Cache's Avalanche section's Peak Burst drop to ~3 with the callout flipping to "no synchronized wave detected", and the Database's failure rate visibly drop (~70% → ~41%) but the node-health dot **still reads steady red, not green** — confirm the remedy's own description reads honestly as a partial improvement, not a fix, and doesn't claim (or imply via a status-flip expectation) a full recovery; the Compare button is disabled ("Run the simulation once to enable Compare") until Run Simulation has been clicked once, then shows an inline before/after — per-entity utilization/error rows for Cache and Database, overall success rate, avg latency (no "Crashed → Healthy" status line is expected here, since neither run crosses that threshold); the "Broken (starting state)" card returns TTL Jitter to 0% when clicked after the remedy was applied; breadcrumb "← Cache" returns to `/entities/cache`. No layout overflow at ~1024px, light and dark theme.
- **New "Try It" demo — Cache / Cache Penetration** (sixth demo built with the pattern from `docs/Learn-Problem-Solution.md`, after Cache Stampede, Load Balancer, Database, Rate Limiter, and API Server's architecture-change demo) — never rendered in a browser yet. Needs a check that: on `/entities/cache`, the "Cache Penetration" failure-mode card shows a "Try it" button ("Cache Avalanche" should not, it has no `demo`); it navigates (same tab) to `/entities/cache/try/cache-penetration`; the page loads a 3-node Client → Cache → Database graph already positioned and connected, Client's Configuration showing Missing Key Rate 88%, Cache's Configuration showing Negative Caching "Off"; the Inspector lists all 3 nodes as expanded-by-default dropdowns; clicking Run Simulation plays back and the Results bar + canvas show the Database pulsing red/Crashed (~90% failure rate), overall success rate around 9%, and the Cache's own Inspector "Penetration" section showing almost all misses as "Downstream (not found)" with ~0 "Negative cache (avoided)"; in the Remedies panel (left, under "Solutions"), there is exactly one remedy card, "Negative Caching On" — clicking it updates the Cache's Negative Caching config, clears the stale results back to "Run the simulation…", and re-running shows the Database fully healthy (green, 0 errors, utilization way down), the Cache's Penetration section now showing the overwhelming majority as "Negative cache (avoided)"; overall success rate barely moves (~9% → ~12%, both low) — confirm this reads as expected given the remedy's own description explicitly frames the win as protecting the database, not raising the client's success rate (88% of all traffic targets a key that will never exist, so most traffic is unavoidably "not found" either way); the Compare button is disabled ("Run the simulation once to enable Compare") until Run Simulation has been clicked once, then shows an inline before/after — a status line naming "Database: Crashed → Healthy", a per-entity utilization/error row for Cache and Database, overall success rate, and avg latency; the "Broken (starting state)" card returns Negative Caching to "Off" when clicked after the remedy was applied; breadcrumb "← Cache" returns to `/entities/cache`. No layout overflow at ~1024px, light and dark theme.
- **Scenario capacity-estimate prompt + post-run reflection — all 3 scenarios** (`src/scenarios/types.ts` new `CapacityEstimate`/`ScenarioReflection`/`resolveReflection`, content added to `urlShortener.ts`, `movieTicketBooking.ts`, and `flashSale.ts`, `src/components/workshop/InspectorPanel.tsx`'s new `CapacityEstimatePrompt` + "What Happened" section — see `docs/primer-gap.md` Part A) — never rendered in a browser yet. Needs a check, repeated for all 3 scenarios (URL Shortener, Movie Ticket Booking, Flash Sale): loading the scenario (Scenarios tab in Component Library) and selecting it in the Inspector, *before* running, shows a "Before You Run" box with the capacity-estimate prompt and a "Show the worked answer" toggle that reveals the worked-math text on click; after clicking Run Simulation, the "Before You Run" box disappears entirely (not just collapses) and a "What Happened" box appears between Success Criteria and the pass/fail learning-goals callout, with its text showing real resolved numbers (a percentage for success rate, "N req/s" for throughput, "Nms" for p95 latency — not literal `{{...}}` placeholders, and — this is the interesting case — Movie Ticket Booking's text should read as making sense given the database is the saturated entity, Flash Sale's given the API server is); restarting the scenario (the Restart button) brings back the "Before You Run" box and hides "What Happened" again until the next run. No layout overflow in the Inspector sidebar, light and dark theme.

- **New mechanism: architecture-change remedies — first demo, API Server / Queue Saturation** (see `docs/Learn-Problem-Solution.md` §5/§7 for the design writeup) — brand-new interaction pattern, never rendered in a browser yet. Needs a check that: on `/entities/api`, the "Queue Saturation (Backpressure Collapse)" failure-mode card shows a "Try it" button ("Latency Cliff from Processing Time" should not, it has no `demo`); it navigates to `/entities/api/try/queue-saturation-backpressure-collapse`; the page loads a 3-node Client → API Server → Database graph; Inspector lists all 3 as expanded dropdowns; Run Simulation shows the API Server pulsing red/Crashed-or-error (~51% failure rate), overall success rate ~45%; in the Remedies panel, one card — "Scale Horizontally — Load Balancer + 2nd API Server" — reads "Build it" (not "Apply"); clicking it resets the canvas to the broken baseline and reveals a numbered instructions list plus a small palette showing exactly "Load Balancer" and "API Server" (draggable and click-to-add, both work); dragging a Load Balancer onto the canvas actually adds it (drop position roughly under the cursor); dragging a second API Server in works the same way; selecting the original direct Client→API Server edge and pressing Delete/Backspace removes it (but pressing Delete with a *node* selected does nothing — nodes are never deletable); drawing new connections (Client→LB, LB→each API Server, new API Server→Database) via the normal React Flow connect-handle drag works; clicking "Run Simulation" now reflects whatever the student actually built (test both a correctly-wired version — should recover to a healthy, mostly-green result — and a deliberately incomplete one, e.g. missing the new API Server's connection to Database — should still run without crashing the page, just fail more); the Compare button is disabled until a Run happens, then always shows the reference fix's numbers (98.8% success, both API Servers ~0.5-0.7% failure) *regardless* of what the student actually built or whether they've built anything at all — confirm this explicitly by comparing before building anything vs. after; per-entity comparison rows correctly show "API Server (2)" and "Load Balancer" (not just the original API Server/Database) with real labels, not raw ids; switching to the "Broken (starting state)" card after a partial build cleanly removes any dragged-in nodes/edges (no orphans left on canvas); clicking "Broken" then re-clicking "Build it" resets cleanly for a second attempt. No layout overflow at ~1024px, light and dark theme.
- **New "Try It" demo — Rate Limiter / Burst Rejection Divergence** (fourth demo built with the pattern from `docs/Learn-Problem-Solution.md`, after Cache Stampede, Load Balancer, and Database) — never rendered in a browser yet. Needs a check that: on `/entities/rate_limiter`, the "Burst Rejection Divergence" failure-mode card shows a "Try it" button; it navigates (same tab) to `/entities/rate_limiter/try/burst-rejection-divergence`; the page loads a 3-node Client → Rate Limiter → Database graph already positioned and connected, Rate Limiter's Configuration showing Algorithm "Sliding Window", Requests/Second 20, Burst Capacity 100; the Inspector lists all 3 nodes as expanded-by-default dropdowns; clicking Run Simulation plays back and the Results bar + canvas show the Rate Limiter pulsing red/Crashed (~93% failure rate), overall success rate around 7%; in the Remedies panel (left, under "Solutions"), there are exactly two remedy cards — "Raise Requests/Second" and "Switch to Token Bucket" — mutually exclusive; clicking "Raise Requests/Second" sets Requests/Second to 400, clears stale results, and re-running shows the Rate Limiter fully healthy (green, 0 rejected), overall success rate 100%; clicking "Switch to Token Bucket" instead sets Algorithm to "Token Bucket" (Requests/Second unchanged at 20) and re-running shows a meaningfully better but still unhealthy limiter (~82% failure rate, no longer pulsing "Crashed" red but still showing rejections) — confirm the card's description reads as an honest partial mitigation, not a fix; each remedy's Compare button is disabled with a "Run the simulation once to enable Compare" hint until Run Simulation has been clicked at least once, then shows a live-computed before/after (status line, per-entity utilization/error breakdown for Client/Rate Limiter/Database, success rate, avg latency); the "Broken (starting state)" card restores Algorithm=Sliding Window, Requests/Second=20 together after any remedy was applied; breadcrumb "← Rate Limiter" returns to `/entities/rate_limiter`. No layout overflow at ~1024px, light and dark theme.
- **Try It Compare panel worst-entity attribution bug — CONFIRMED FIXED**: the Compare dialog on the Load Balancer/Uneven Backend Divergence demo (the case that surfaced the bug) correctly names "API Server (slow)" as the worst-hit component, never "Load Balancer" itself. Not re-checked: the per-entity utilization/error row block specifically, the Load Balancer node's own dot reading "error" not "Crashed" in the live Workshop canvas, Cache Stampede spot-check, light theme.
- **New "Try It" demo — Database / Connection Pool Exhaustion** (third demo built with the pattern from `docs/Learn-Problem-Solution.md`, after Cache Stampede and Load Balancer) — never rendered in a browser yet. Needs a check that: on `/entities/database`, the "Connection Pool Exhaustion" failure-mode card shows a "Try it" button ("Independent Failure (Flaky Infrastructure)" should not, it has no `demo`); it navigates (same tab) to `/entities/database/try/connection-pool-exhaustion`; the page loads a 2-node Client → Database graph already positioned and connected, Database's Configuration showing Database Type "SQL", Max Connections 2, Max Queue Length 0; the Inspector lists both nodes as expanded-by-default dropdowns; clicking Run Simulation plays back and the Results bar + canvas show the Database pulsing red/Crashed (~96% failure rate), overall success rate around 8%; in the Remedies panel (left, under "Solutions"), there are exactly three remedy cards — "Raise Max Connections", "Raise Max Queue Length", "Switch to NoSQL" — each mutually exclusive (applying one shouldn't leave another showing "Applied"); clicking "Raise Max Connections" updates Max Connections to 40, clears stale results, and re-running shows the Database fully healthy (green, ~0.4% failure rate), overall success rate ~99.8%; clicking "Raise Max Queue Length" instead sets Max Queue Length to 500 and re-running still shows the Database reading as crashed/near-crashed (~85.6% failure rate) — confirm the card's description reads as an honest partial mitigation, not a fix, and that the node visibly still looks broken, not just numerically; clicking "Switch to NoSQL" sets Database Type to "NoSQL" and re-running shows a meaningfully better but still unhealthy database (~72.1% failure rate, no longer pulsing "Crashed" red but still showing errors) — a middle ground distinct from both other remedies; each remedy's "Compare" button is disabled with a "Run the simulation once to enable Compare" hint until Run Simulation has been clicked at least once, then shows a live-computed before/after (status line, per-entity utilization/error breakdown for Client/Database, success rate, avg latency); the "Broken (starting state)" card restores Max Connections=2, Max Queue Length=0, Database Type=SQL together after any remedy was applied; breadcrumb "← Database" returns to `/entities/database`. No layout overflow at ~1024px, light and dark theme.
- **New "Try It" demo — Load Balancer / Uneven Backend Divergence — CONFIRMED core flow**: "Try it" navigates to the page, loads the correct 6-node graph with Algorithm "Round Robin", broken run gave 51.5% overall success rate (matches the documented ~51%) with API Server (slow) as the worst-hit component; applying "Least Connections" and re-running gave 100% success. Not re-checked: the failure-mode card gating (only this card, not "Weighted Misconfiguration"/"SPOF"), Inspector's expanded-by-default state (this pass found them collapsed by default instead — see the Compare-modal entry above, which changed this intentionally after this entry was written), Request Distribution shift, the "Broken (starting state)" reset-to-round_robin behavior, breadcrumb, light theme.
- **Cache Penetration / Avalanche `entityDeepDive.ts` text corrected to `simulated: true`** — text-only change (no simulation code touched), but on the public `/entities/cache` page. Needs a check that: the "Cache Penetration" and "Cache Avalanche" failure-mode cards now show numbered reproduce steps (previously empty/"not simulated") and no longer read as unsimulated; neither card shows a "Try it" button (no `demo` was added, only the reproduce text/flag); the Cache page's "Cons" list no longer claims Penetration/Avalanche are unsimulated. No layout overflow at ~1024px.
- **Session note (resume here):** a 2026-08-06 verification pass got partway through before pausing — dev server had heavy concurrent activity (other edits landing live: Reverse Proxy, Kafka, this file itself) causing repeated Fast Refresh full-reloads that wiped the in-memory canvas mid-test, plus one real transient error worth re-checking first: `GET /entities 500 — TypeError: Cannot read properties of undefined (reading 'tagline') at src/app/entities/page.tsx:73` (a `deepDive` entry — likely Kafka or Reverse Proxy, added concurrently — missing a `tagline`; may already be fixed, confirm before anything else). Also: connecting two nodes by dragging a React Flow handle didn't script reliably via synthetic pointer events (worked once, then flaked) — do that step with a real mouse when resuming rather than automating it. Confirmed so far this pass: empty-canvas onboarding note (below, now removed from this list) and the CostPanel's pre-run `$61/mo+` toggle both looked correct. One thing to double check: CDN's own Inspector "Estimated Cost" section read `$0/mo+ Normal` immediately after adding a fresh CDN node (before it had a Client/origin connected) — confirm whether that's expected (no traffic/base cost yet for an unconnected CDN) or the bug the pre-run-cost item was meant to fix.

- **New Reverse Proxy entity** (`src/simulation/entities/ReverseProxy.ts`, `src/lib/entityCatalog.ts`, `src/components/workshop/InspectorPanel.tsx`'s new `ReverseProxyRoutesSection`, `/entities/reverse-proxy` deep-dive page) — brand new sidebar entity, never dropped on a canvas in a browser yet. Needs a check that: it appears in the Component Library sidebar (Phase 2 / Modules section) and drags onto the canvas cleanly; selecting it shows a "Routes" Inspector section listing each connected downstream target with a dropdown (Unassigned / `*` — Catch-all / each of the 8 route labels); changing a target's route persists and is reflected after reselecting the node; connecting it to 2+ API Servers, assigning each a distinct route, and running a simulation shows per-target traffic in the "Request Distribution" section proportional to the Client's Route Pool Size; the Client node's Configuration section shows the new "Route Pool Size" field; `/entities/reverse-proxy` renders cleanly with no layout overflow at ~1024px.
- **New Kafka entity** (`src/simulation/entities/Kafka.ts`, `src/lib/entityCatalog.ts`, `src/components/workshop/InspectorPanel.tsx`'s generalized `LoadBalancerDistributionSection` (now takes a `title` prop) wired to a new "Partition Distribution" section plus a retitled "Consumer Group Distribution" section, `/entities/kafka` deep-dive page) — brand new sidebar entity, never dropped on a canvas in a browser yet. Needs a check that: it appears in the Component Library sidebar (Phase 2 / Modules) and drags onto the canvas cleanly; its Configuration section shows Partition Count / Consumers per Group / Max Queue Length / Dispatch Time; connecting it to 2+ downstream targets (consumer groups) and running a simulation shows a "Partition Distribution" section (synthetic "Partition N" rows, no real node match — confirm the fallback label reads fine unstyled) and a separate "Consumer Group Distribution" section with real node labels; both distributions update sensibly when Partition Count or Client Key Pool Size changes; `/entities/kafka` renders cleanly with no layout overflow at ~1024px, including its two failure-mode cards.

- **CDN Edge Map rebuilt into an interactive map with draggable User/Origin pins that actually drive routing** (`src/components/workshop/CDNEdgeMap.tsx`, `src/simulation/entities/CDN.ts`, `src/simulation/entities/weightedRouting.ts`, `src/components/workshop/InspectorPanel.tsx`'s `useCDNActivePulse` + `CDNEdgeSection`) — supersedes the earlier static radial diagram entirely (not an incremental change to it). The old key-hashed edge routing is gone; edges are now proximity-weighted from a draggable User pin (reusing Load Balancer's Weighted Round Robin mechanism), and per-edge latency is that edge's real distance to the User pin. Both User and Origin pins are draggable directly on an SVG plane (pointer-capture based drag, plus numeric X/Y fallback inputs below the diagram for keyboard access), edges stay auto-arranged. Misses draw a line to the Origin pin (opacity by miss count); the active-request ping animation from the previous pass is preserved, now positioned on the new plane. Needs a check that: dragging the User pin around the plane visibly moves it smoothly (no jitter/lag), snaps/clamps correctly at the plane's edges (can't drag it outside 0–100), and after a re-run, both the per-edge latency numbers and which edge has the most requests visibly shift toward whichever edge the pin is now closest to; dragging the Origin pin similarly updates where miss lines point; the numeric X/Y inputs below the diagram stay in sync with drag position both directions (typing a value moves the pin, dragging updates the inputs) and are reachable/usable by Tab+type; "Reset positions" restores both pins to default; on a touch device / touch emulation, dragging a pin works (not just mouse); no layout overflow or horizontal scrollbar in the Inspector sidebar at the panel's normal width; the live ping animation (from the prior pass) still fires correctly on the new plane positions during playback.

- **Inspector config-field hint overflow fix** (`src/components/workshop/InspectorPanel.tsx`) — the per-field "what does this do" reveal was rebuilt from an absolutely-positioned tooltip bubble (could overflow the sidebar's right edge and force a horizontal scrollbar) to an in-flow `FieldHint` that expands below the field via a `grid-rows-[0fr]→[1fr]` transition. Needs a check that: hovering/focusing the info icon on a field near the right edge of a long label no longer causes horizontal overflow/scrollbar in the Inspector; the reveal animates smoothly; keyboard focus (Tab to the icon) also reveals it.
- **New `/entities` index + `/entities/[slug]` deep-dive pages, plus WorkshopHeader's new "Learn" tab** (`src/app/entities/page.tsx`, `src/app/entities/[slug]/page.tsx`, landing page entity chips in `src/app/page.tsx`, `src/components/workshop/WorkshopHeader.tsx`) — brand new standalone routes, never rendered in a browser yet. Needs a check that: `/entities` grid renders all 9 implemented entities with working links; a `/entities/[slug]` page (e.g. `/entities/cache`) renders cleanly end to end — including the new "How to connect & use it" section (Where it goes + topology string, Normal usage, Extremes cards, and the highlighted "Edge cases to try" callout) sitting between "In production" and "Tradeoffs" — plus pros/cons, failure-mode cards with numbered reproduce steps, prev/next nav, CTA, with no layout overflow at both desktop and ~1024px width; the landing page's entity chips link out instead of being static; an invalid slug (e.g. `/entities/nonsense`) 404s; the Workshop header's new "Learn" button opens `/entities` in a new tab without disturbing the canvas. Also cover `/entities/load_balancer` specifically now that its deep-dive content grew substantially (5 algorithms, 3 failure-mode cards including the new "Weighted Misconfiguration" one) — check it doesn't overflow or crowd the page at ~1024px.
- **Cost shown before a simulation ever runs** (`src/lib/costEngine.ts`, `src/components/workshop/CostPanel.tsx`, `src/components/workshop/InspectorPanel.tsx`) — `estimateCost` now accepts a null `SimulationResult` and prices base (provisioned) cost from config alone; the canvas CostPanel button and the Inspector's "Estimated Cost" section no longer wait for a run to appear, they now show base cost immediately and add a trailing `+` (with a "run a simulation for usage" note) until one exists. Needs a check that: dropping an API Server/Database/Cache/CDN/Load Balancer/Message Queue on a blank canvas immediately shows a nonzero cost in both the CostPanel toggle and that node's Inspector section, both display the `+` / base-only messaging pre-run, and both switch to full base+usage numbers (no `+`, no "run a simulation" note) once a simulation is run — with the base figure staying identical across that transition.

- **New real-SVG diagram infra + Foundations Lesson 5 (DNS Deep Dive) pilot** (`src/content/shared/lesson.ts` — new `figure` `LessonBlock` kind; `src/components/content/diagrams/` — new `primitives.tsx`, `DnsHierarchyDiagram.tsx`, `DnsResolutionFlowDiagram.tsx`, `DnsFailoverDiagram.tsx`, `registry.tsx`; `src/components/content/LessonBlockRenderer.tsx` — new `"figure"` case; `src/content/foundations/lessons/05-dns-deep-dive.ts` — 3 of its ascii `diagram` blocks replaced with `figure` blocks) — first real vector diagrams anywhere in `/foundations`/`/lld`, everything before this was `<pre>` ascii art. This is a style-approval checkpoint before rolling the same treatment out to the rest of Foundations and then LLD, so look closely rather than just glancing. Needs a check on `/foundations/dns-deep-dive`, both themes, at both a wide desktop width and ~1024px: the "DNS is a distributed database" section renders the hierarchy tree (Root → three TLD boxes → two Authoritative boxes under `.com`, with the two Authoritative boxes visibly accent-colored/bordered differently from the rest) with no overlapping boxes/text and no arrowheads floating off their line; the "The full resolution journey" section renders the 8-step vertical rail diagram (start → 6 numbered steps → the final "Browser connects..." step in a green/healthy accent) with every box's two lines of text fitting inside its box (no clipped/overflowing text) and the rail's arrows pointing down between each node; the "DNS as a system design tool" section's failover diagram renders two side-by-side panels ("Before — primary healthy" / "After — failover") with Primary/Backup boxes swapping healthy↔critical color correctly between panels and the center "Primary fails / health check / DNS updates" transition text legible and not overlapping either panel; none of the three figures cause horizontal page overflow (they may scroll within their own bordered frame at narrow widths — confirm that scroll works rather than clipping); the three untouched ascii `diagram` blocks elsewhere on the same page (TTL bars, DNS-as-load-balancing IP list, CDN geo-routing) still render as before, for comparison against the new figures. Once approved, note explicitly whether to (a) convert the remaining 3 ascii diagrams on this same lesson too, or (b) leave them and move on to the next lesson.

- **"Try It" failure-mode demo pages, starting with Cache Stampede** (see file list above) — brand new route + store + component tree, never rendered in a browser yet, the first vertical slice of a new pattern (deep-dive articles linking to a runnable, pre-built demo of a specific failure mode). Needs a check that: on `/entities/cache`, the "Cache Stampede" failure-mode card shows a new "Try it — a pre-built, broken architecture, ready to run" button (only that card — Penetration/Avalanche have no `demo` yet and shouldn't show one); clicking it navigates to `/entities/cache/try/cache-stampede` (same tab, not a new one — no live Workshop canvas to protect on the article page); the page loads a 3-node Client→Cache→Database graph already positioned and connected, with the Cache's "Stampede Protection" showing "Naive"; the Inspector (right) lists all 3 nodes as expanded-by-default dropdowns (Client/Cache/Database), each showing its own Configuration + Live Metrics without needing to click the node on canvas first — clicking a node on canvas visibly highlights (not just opens) its section, and each dropdown still collapses/expands on click; clicking Run Simulation plays back and the Results bar + the Cache's own Live Metrics (in its Inspector dropdown, no click needed) show a real Stampede section with independent-fetch count high and 0 coalesced, and the Cache/Database node dots read as genuinely crashed (pulsing red) on canvas, not just "elevated" — this demo is tuned to be dramatic (~97% DB failure rate), confirm it reads that way visually, not just in the numbers; in the Remedies panel (left, replacing the Component Library slot, now with a "Solutions" heading above the two remedy cards), clicking "Coalesced (single-flight)" updates the Cache's config, clears the stale results/playback bar back to "Run the simulation…", and re-running now shows a fully healthy Database (green, 0 errors) vs. the broken run; clicking "Raise TTL" instead (mutually exclusive with Coalesced — applying one should not still show the other as "Applied") shows a real but partial improvement — the Database still shows a crashed dot when it does fail, but the overall Results bar success rate is much closer to healthy since it happens far less often; the "Broken (starting state)" card returns the Cache to `stampedeMode: naive`/`ttlMs: 100` when clicked after a remedy was applied; each remedy's "Compare" button shows an inline before/after — a "Database: Crashed → Healthy" (or similar) status line plus overall success rate and avg latency, all as plain text with a small arrow icon between before/after values (no strikethrough — confirm the "before" number is actually legible, not just present in the DOM) — without needing a manual Run first, and doesn't disturb whatever's currently live on the canvas/Results bar; the Inspector's Configuration fields for the Cache (Capacity, Eviction Policy, TTL, Stampede Protection) are directly editable and a Run reflects any hand-edited value, not just the ones a Remedy sets; dragging nodes around the canvas works, but there's no Component Library to drag *new* components in and no way to delete a node (Backspace/Delete should do nothing); breadcrumb "← Cache" returns to `/entities/cache`; an invalid failure-mode slug (e.g. `/entities/cache/try/nonsense`) and a valid entity with no demo at all (e.g. `/entities/database/try/anything`) both 404. No layout overflow at ~1024px, light and dark theme, including the now-taller Inspector with all 3 nodes expanded at once.
- **Cache Penetration + Avalanche simulated (negative caching, TTL jitter)** (`src/simulation/entities/Cache.ts`, `CacheStore.ts`, `TrafficGenerator.ts`, `Client.ts`, `src/lib/entityConfigSchema.ts`, `src/components/workshop/InspectorPanel.tsx`'s new `CachePenetrationSection`/`CacheAvalancheSection`) — Cache Stampede's two-comparable-options pattern extended to the other two named cache failure modes; never exercised in a browser. Needs a check that: a Client node's Configuration shows the new "Missing Key Rate" field; a Cache node's Configuration shows "Negative Caching" (select), "Negative Cache TTL" (only meaningfully different once Negative Caching is on), and "TTL Jitter" (percent); wiring Client → API → Cache → Database, setting the Client's Missing Key Rate to ~30% and running with Negative Caching "off" shows a new Inspector "Penetration" section on the Cache node with mostly/all "Downstream (not found)" bar and ~0 "Negative cache (avoided)"; switching Negative Caching to "on" and re-running shifts that balance toward "Negative cache (avoided)" and reduces the Database's request count/utilization for the same traffic; setting Cache's TTL to something short (e.g. 200ms) with high traffic and TTL Jitter at 0% shows an Inspector "Avalanche" section with a high "Peak burst (100ms)" number relative to "Expired misses" (most/all expiries clustered) and the callout text warning about a synchronized wave; raising TTL Jitter toward 50% and re-running (same architecture/traffic) lowers that peak-burst number and flips the callout to "no synchronized wave detected" once it drops low enough; neither new Inspector section renders (or renders empty/hidden, per `CollapsibleSection`'s conditional mount) when the corresponding metric is undefined (e.g. Missing Key Rate at 0%, or Avalanche with TTL at 0). No layout overflow in the Inspector sidebar at its normal width in either light or dark theme.

- **New Foundations Lesson 19 — Consistent Hashing** (`src/content/foundations/lessons/19-consistent-hashing.ts`, registered in `src/content/foundations/index.ts`) — brand new lesson, never rendered in a browser yet. Needs a check on `/foundations/consistent-hashing`, both themes, at both a wide desktop width and ~1024px: the page loads via the lesson index/prev-next nav (appears after CDN, lesson 18) with correct title/tagline/estimated minutes; all 7 sections render — "Intuition first", "The rehashing problem" (2 tables, 8 rows each, no column overflow), "The hash ring" (2 tables), "Adding and removing a node" (2 `flow` blocks plus a 3-row comparison table), "Virtual nodes" (1 table), "Real usage" (list + insight), "Interview perspective" (3 `qa` blocks — confirm the middle one renders correctly, its answer field was written across a multi-line string); summary/key takeaways/exercise render at the bottom; the "Related entities" cross-link box links to `/entities/replica-pool`, `/entities/cache`, and `/entities/kafka` (not `/entities/replica_pool` with an underscore) and each link resolves; no layout overflow anywhere on the page.

- **Icon wiring finished — `entityType` on `ArchNode`, tagged across all 18 Foundations lessons** (`src/content/shared/lesson.ts` — new optional `ArchNode.entityType`; `src/components/content/diagrams/generic/ArchitectureDiagram.tsx` — `ArchitectureLayer` now passes `getEntityCatalogItem(node.entityType).icon` into `DiagramBox`, which covers `CompareDiagram` for free since it reuses the same layer; ~10 lesson files got `entityType` added to unambiguous `architecture`/`compare` nodes — Client/API Server/Database/Load Balancer/Cache/CDN/Message Queue/Kafka icons, no new color). Never rendered in a browser yet. Needs a check on `/foundations/load-balancers`, `/foundations/caching`, `/foundations/message-queues`, `/foundations/kafka-deep-dive`, and `/foundations/cdn` (all touched, all previously had icon-less boxes), both themes: every tagged box now shows a small top-left glyph (matching the icon already used on the entity's own `/entities/[slug]` page and Workshop canvas card) that doesn't collide with the box's title/sub-line text or get clipped at small box sizes; untagged boxes (generic "Server", "Origin", "Worker", ambiguous combined boxes) render exactly as before with no icon; no layout regression in any `compare` panel (icons shouldn't push panels wider or misalign the side-by-side layout). Also spot-check `/foundations/client-server-architecture` (the N-tier microservices diagram — several distinctly-named services all sharing the same generic API-server icon should still read fine, not look like a mistake) and `/foundations/redis-deep-dive` (cluster/sentinel diagrams — many boxes all tagged `cache`, confirm it doesn't look repetitive/wrong at a glance).

- **New Foundations Lesson 20 — Database Sharding** (`src/content/foundations/lessons/20-database-sharding.ts`, registered in `src/content/foundations/index.ts`; also touched: `12-vertical-vs-horizontal-scaling.ts`'s "Phase 6" sharding-hotspot line now points here by name, `19-consistent-hashing.ts`'s closing insight gained a forward-pointing sentence to this lesson) — brand new lesson, inserted between Consistent Hashing and the (now further-renumbered) Estimation lesson, never rendered in a browser yet. Needs a check on `/foundations/database-sharding`, both themes, at both a wide desktop width and ~1024px: loads via prev/next nav between Consistent Hashing (19) and Database Replication (21); all 8 sections render — "Intuition first", "Why sharding earns its complexity" (table), "The shard key" (toned list — 3 green/healthy items then 2 red/critical items, confirm the tone colors read correctly in both themes), "Three sharding strategies" (2 tables + one `architecture` block showing Application → Shard directory → Shard A/B/C, confirm the dashed "2. Shard B" return edge and the 3-shard row don't overlap or overflow at ~1024px), "What sharding breaks" (list + insight), "Resharding without downtime" (6-step `flow` block — confirm all 6 steps render in order with distinct tones), "Real usage" (list + insight), "Interview perspective" (3 `qa` blocks); summary/key takeaways render at the bottom; the exercise has a `guidance` reveal (worked answer) with a paragraph + a 6-item list — confirm it's collapsed by default and expands on click, matching other lessons with guidance; "Related entities" cross-link box links to `/entities/database` and `/entities/replica-pool`, both resolve; no layout overflow anywhere on the page.

- **New Foundations Lesson 21 — Database Replication** (`src/content/foundations/lessons/21-database-replication.ts`, registered in `src/content/foundations/index.ts`; also touched: `12-vertical-vs-horizontal-scaling.ts`'s replication-lag insight now points here by name and gained `replica-pool` to its `relatedEntitySlugs`, `19-consistent-hashing.ts`'s "Lesson 10's replica sets" aside corrected to point here instead) — brand new lesson, never rendered in a browser yet. Needs a check on `/foundations/database-replication`, both themes, at both a wide desktop width and ~1024px: loads via prev/next nav between Database Sharding (20) and Rate Limiting (22); all 7 sections render — "Intuition first", "Three replication topologies" (1 `architecture` block — Write → Leader → 2 Followers — plus a 3-row comparison table), "Synchronous vs. asynchronous replication" (3-row table), "What replication lag breaks" (list + insight), "Failover" (4-step `flow` block, last step toned `critical` for split brain), "Real usage" (list + insight), "Interview perspective" (3 `qa` blocks); summary/key takeaways/exercise render at the bottom; "Related entities" links to `/entities/replica-pool` and `/entities/database`, both resolve; no layout overflow anywhere.

- **New Foundations Lesson 22 — Rate Limiting** (`src/content/foundations/lessons/22-rate-limiting.ts`, registered in `src/content/foundations/index.ts`) — brand new lesson, closing a gap where this app's simulated Rate Limiter entity had no corresponding theory lesson at all. Never rendered in a browser yet. Needs a check on `/foundations/rate-limiting`, both themes, at both a wide desktop width and ~1024px: loads via prev/next nav between Database Replication (21) and Circuit Breakers (23); all 5 sections render — "Intuition first" (with a forward-pointing insight to Lesson 23), "Four algorithms" (a 2-row worked-example table demonstrating Fixed Window's boundary flaw, then a 4-row comparison table), "Where a rate limiter sits" (a 3-row placement table + an insight about distributed counters needing Redis), "Real usage" (list citing Stripe/GitHub/AWS/nginx, plus an insight tying directly back to this app's own Rate Limiter benchmark numbers), "Interview perspective" (3 `qa` blocks); summary/key takeaways/exercise render at the bottom; "Related entities" links to `/entities/rate-limiter` and resolves; no layout overflow anywhere.

- **New Foundations Lesson 23 — Circuit Breakers** (`src/content/foundations/lessons/23-circuit-breakers.ts`, registered in `src/content/foundations/index.ts`; also touched: `13-load-balancers.ts`'s health-check insight gained a forward-pointing sentence to this lesson) — brand new lesson, closing the same kind of gap as Rate Limiting — this app's simulated Circuit Breaker entity had no corresponding theory lesson. Never rendered in a browser yet. Needs a check on `/foundations/circuit-breakers`, both themes, at both a wide desktop width and ~1024px: loads via prev/next nav between Rate Limiting (22) and Back-of-the-Envelope Estimation (24, renumbered — see below); all 7 sections render — "Intuition first", "The state machine" (a 5-step `flow` block cycling Closed→Open→Half-Open, plus a 3-row state table — confirm the "Failure Threshold reached"/"Open" steps read as `critical`-toned and "Closed"/"Half-Open"-adjacent steps read correctly), "Worked example" (a 7-row timed table tracing one full trip-and-recover cycle — check it doesn't overflow at ~1024px, it's the widest table in the lesson), "Failing fast is only half the fix" (list + insight), "Where it fits" (a 3-row table contrasting Rate Limiter/Circuit Breaker/Load Balancer), "Real usage" (list + insight), "Interview perspective" (3 `qa` blocks); summary/key takeaways/exercise render at the bottom; "Related entities" links to `/entities/circuit-breaker` and `/entities/load-balancer`, both resolve; no layout overflow anywhere.

- **Foundations Lesson renumbered 20 → 24 — Back-of-the-Envelope Estimation & the HLD Interview Framework** (moved `src/content/foundations/lessons/20-estimation-and-interview-framework.ts` → `24-estimation-and-interview-framework.ts`, `number: 24`, via an intermediate `21-...` rename earlier in the same work) — pure renumber to make room for the three new lessons above (Database Sharding, Database Replication, Rate Limiting, Circuit Breakers); slug and content unchanged. Needs a check that `/foundations/estimation-and-interview-framework` still loads correctly via prev/next nav, now appearing directly after Circuit Breakers (23) instead of after Consistent Hashing (19), and that the lesson index page (`/foundations`) shows it as #24 in the ordered list.

- **New "Night Ops" preview theme + ThemeToggle now cycles 3 states** (`src/app/globals.css` — new `:root[data-theme="night-ops"]` block; `src/components/theme/ThemeProvider.tsx` — `Theme` widened to `"light" | "dark" | "night-ops"`, new `THEME_CYCLE`/`colorMode`; `src/components/theme/ThemeToggle.tsx` — Moon/BatMark/Sun icon cycle; `src/app/layout.tsx`'s blocking init script updated for the third value; `ArchitectureCanvas.tsx`/`FailureDemoCanvas.tsx` switched their React Flow `colorMode` prop from `theme` to the new binary `colorMode`) — true-black void + hard saturated signal-yellow, an experimental third theme reachable only by clicking the header's theme toggle (not a default for anyone yet). Never rendered in a browser. Needs a check that: clicking the theme toggle in any header cycles dark → night-ops → light → dark, each click swapping the icon (Moon/bat/Sun) and the `title`/`aria-label` naming the *next* theme; on night-ops, the whole app (landing, `/workshop`, `/entities/[slug]`, a Foundations lesson) reads as true-black background with yellow buttons/links/focus rings/active nav — not just a few components; status colors (node health dots, success/error states in a run simulation) are still visibly distinct from the yellow signal accent, not close enough to be confused with it; hairline borders and text are still legible against the near-black background (no borders vanishing to invisible, no low-contrast text); reloading the page while night-ops is active shows no flash of the dark or light theme first (the blocking init script should set the attribute before first paint); the React Flow canvas in both Workshop and a "Try it" failure demo switches to its dark node/edge/background styling under night-ops (not light) since `colorMode` treats it as dark; toggling away from night-ops and back preserves state via `localStorage` across a reload. Both the existing light and dark themes should look completely unchanged by this change — spot-check one page in each to confirm no regression.

- **Bat-signal atmosphere for Night Ops — flying-bat swarm transition + ambient spotlight/rain backdrop** (`src/components/theme/icons/BatMark.tsx` — the generic bat-silhouette glyph, also now used as the toggle's night-ops icon in place of the earlier flashlight; `src/components/theme/night-ops/BatSpotlight.tsx`, `RainCanvas.tsx`, `BatSwarmTransition.tsx`; `src/components/theme/NightOpsAtmosphere.tsx` — orchestrates all three plus the `useBatSwarmTrigger` context; `src/app/layout.tsx` now wraps `children` in `<NightOpsAtmosphere>`; `ThemeToggle.tsx` calls `triggerSwarm()` right when a click is about to switch into night-ops) — never rendered in a browser. This is a personal-project flourish, not part of the earlier no-literal-branding product plan — see conversation. Needs a check that: clicking the toggle from dark or light *into* night-ops plays a ~1.5–2s flock of small bat silhouettes sweeping left-to-right across the whole viewport (varied heights/sizes/timing, a subtle wing-flap flicker, not a rigid grid), then the swarm disappears and the ambient backdrop is left showing — a soft yellow searchlight glow near the top of the screen with a bat silhouette sitting inside it, plus faint falling rain streaks over the whole page; the swarm does **not** replay on a plain page reload/navigation while night-ops is already the active/saved theme (only an actual toggle-click should trigger it — this was a deliberate design choice, worth confirming it actually holds); toggling *out* of night-ops (to light) removes the spotlight/rain backdrop; the rain and the spotlight's slow breathing animation both stop (spotlight/bat symbol likely still visible as a static image, rain either absent or a single still frame of streaks) when the OS "reduce motion" setting is on, and the swarm transition should not play at all in that case; the ambient layer never blocks clicks/drags on real content underneath it (the canvas, buttons, nav — everything should still be fully interactive while night-ops is active, including on `/workshop`); no visible performance stutter from the rain canvas while scrolling or dragging nodes on `/workshop`; resizing the browser window keeps the rain filling the new viewport size without artifacts. Both the flock and the backdrop are decorative/`pointer-events-none` and `aria-hidden` — also sanity-check a screen reader doesn't announce anything from them.

- **Diagram-first restructuring pass — 3 sections across message-queues/kafka-deep-dive** (`src/content/foundations/lessons/16-message-queues.ts`'s "delivery-guarantees" section, `17-kafka-deep-dive.ts`'s "producers" and "brokers-and-replication" sections) — after reviewing all `architecture`/`compare`/`flow` blocks in lessons 16/17/18, most sections already led with their diagram or a single one-line label (already diagram-first from earlier work this session); these 3 sections were the only genuine "several paragraphs of setup before the diagram" cases, so each was reordered to lead with its `flow`/`architecture` block right after the heading, with the explanatory prose that used to precede it now following as elaboration instead (18-cdn.ts needed no changes — already diagram-forward throughout). Never rendered in a browser yet. Needs a check on `/foundations/message-queues` ("Message queue guarantees" section — flow diagram now leads, followed by the At-Most/At-Least/Exactly-once prose, confirm the transition sentence reads naturally) and `/foundations/kafka-deep-dive` ("Producers — writing to Kafka" section — the ORDER_PLACED→DELIVERED flow now leads, confirm the first step's new "order_9981" detail line renders correctly; "Brokers and replication" section — the Broker 1/2/3 LEADER/FOLLOWER architecture diagram now leads, followed by the broker definition and the full 6-row cluster table, confirm nothing reads like it's missing context it needs). Both themes, ~1024px width, no overflow.

- **Segregated "Distributed Systems" vs. "Agentic AI" in `/entities`** (`src/app/entities/page.tsx` — flat grid split into two `EntityGroup` sections by `domain`) — unchanged since it last shipped. Needs a check that: `/entities` shows two headed sections ("Distributed Systems" then "Agentic AI"), each with its own 01-prefixed numbering, all links resolve, no layout overflow at ~1024px in both themes.
- **Workshop Component Library split into two independent, mutually-exclusive panels — "Distributed Systems" and "AI Flow"** (`src/components/workshop/ComponentSidebar.tsx` — the single "Show Components" trigger replaced with two trigger buttons (`PackTriggerButton`, LayoutGrid/Brain icons), each opening its own full panel (`DistributedSystemsPanel`/`AIFlowPanel`, sharing `PackPanel` chrome — header+search+body, each with its own local search query); backed by a new `workshopStore.openComponentPack: "distributed" | "ai-flow" | null` (replaces the old boolean `componentsPanelOpen`), mutually exclusive the same way `tracePanelOpen`/`reliabilityPanelOpen` already are — opening one pack's trigger closes the other rather than letting both stack vertically and push past the bottom of the screen (that stacking was the previous design, both packs' full lists open together in one panel). `tutorialPlanner.ts`'s `requiresComponentsPanel` now carries the specific pack id (`packIdForItem`, `entityCatalog.ts`) instead of a bare boolean, so `TutorialRunner` force-opens exactly the pack a given step's target lives in. `TutorialPanel.tsx`'s left-offset (avoiding overlap with the open panel) now reads `openComponentPack` instead of the old boolean. WeaponWheel's twin dials are unchanged.) — never rendered in a browser. Needs a check that: in `/workshop` **light/dark**, two separate buttons now show ("Distributed Systems", "AI Flow") instead of one "Show Components" toggle; clicking one opens only its own panel (own header, own search box, own item list); clicking the other while the first is open closes the first and opens the second (never both at once, panel never grows tall enough to run off-screen); each panel's search filters only its own list and shows its own "No components match" text when empty; dragging/clicking an item in either panel still adds the right node type; the close (X) on a panel closes it without opening the other. In `/tutorial`: starting a walkthrough whose target lives in the distributed catalog (e.g. Load Balancer) auto-opens the "Distributed Systems" panel and spotlights the right card; picking a target from the AI Flow catalog (e.g. Retriever) auto-opens the "AI Flow" panel instead — confirm the *other* panel is not also open, and that the TutorialPanel readout box shifts right to avoid overlapping whichever panel is currently open. Batman Mode ("Choose Weapon") is unchanged from the last pass — two side-by-side ring dials, no need to re-check unless something else touched it.
- **Pillar A sealed off — 9 new `/agentic` lessons (12-20) closing out the last 3 content categories** (`src/content/agentic/lessons/12-rag-architectures.ts` through `20-evaluation-and-reliability-economics.ts`, registered in `src/content/agentic/index.ts`) — content-only, reusing only already-rendered `LessonBlock` kinds (table/list/insight/qa/paragraph), no new diagram components. Never rendered in a browser yet. Needs a check on `/agentic` (index page now shows 3 lessons under "Protocols & Infra", 4 under "Inference & Serving", 3 under "Production" — previously those last two categories were empty) and spot-checks of `/agentic/rag-architectures`, `/agentic/quantization-tradeoffs` (has a 3-row precision/multiplier table — confirm it doesn't overflow at ~1024px), `/agentic/the-agent-failure-taxonomy` (has a wide 4-column, 10-row table — the widest new one, most likely to overflow), and `/agentic/guardrails-as-architecture`: each loads via prev/next nav in the new position, all sections render with no missing/empty blocks, toned list items (the two `critical`/`healthy` bullets in `rag-architectures`) show correct colors in both themes, tables don't cause horizontal page overflow (may scroll within their own container), the exercise's collapsed guidance reveal expands on click, and each lesson's "Related entities" links (e.g. `/entities/retriever`, `/entities/model-router`, `/entities/guardrail-validator`, `/entities/human-in-loop-gate`) resolve correctly. Both themes, ~1024px width.
- **Removed the "Data Layer" and "Distributed Resilience" region labels from the `/foundations` map** (`src/content/foundations/mapRegions.ts` — deleted those two `FOUNDATIONS_MAP_REGIONS` entries; only "Networking & the Web" and "Delivery & Speed" remain). Lesson nodes themselves are untouched, just no longer labeled by those two clusters. Needs a check on `/foundations`: confirm the map still renders cleanly with only two region labels placed (no leftover blank gap or misaligned label from the removed regions), and that the previously-labeled nodes (databases-the-big-picture, sql-deep-dive, nosql-deep-dive, database-indexing-deep-dive, consistent-hashing, database-sharding, database-replication, rate-limiting, circuit-breakers) still appear positioned normally, just without a region label near them. Both themes.

- **Interview Questions — cross-company topic tags + Topic filter + "Same pattern, asked elsewhere"** (`src/content/interviewQuestions/types.ts` — new `QuestionTag` union (32 values) + `QUESTION_TAG_LABEL`, `InterviewQuestion.tags: QuestionTag[]` required; `src/content/interviewQuestions/index.ts` — new `INTERVIEW_QUESTION_TAGS`, `getRelatedQuestions`; all 13 `companies/*.ts` files — every one of the 102 existing questions got a `tags` array backfilled (some 2 tags, most 1); `src/app/interview-questions/page.tsx` — new "Topic" filter section (button-per-tag, sorted by count desc, `(N)` suffix) alongside the existing Company/Category filters, plus small tag `Badge`s on each question card; `src/app/interview-questions/[id]/page.tsx` — tag badges next to the existing Company/Category/Verified badges, and a new "Same pattern, asked elsewhere" section listing other questions sharing a tag) — never rendered in a browser. Needs a check on `/interview-questions`, both themes: the new "Topic" filter row renders below "Category" with ~32 buttons each showing a count, clicking one filters the list to only matching questions and highlights the button (same active-state styling as Company/Category), multiple topic buttons OR together (not AND), combining a Topic filter with a Company/Category filter ANDs across the three groups correctly; each question card now shows small neutral-badge tag chips below the context blockquote (or below the prompt if no context) — confirm they wrap cleanly and don't overflow at ~1024px. Then open a question that has a populated tag cluster (e.g. any `payments-idempotency` or `ai-ml-infra` question — Stripe's idempotent-charge or Amazon's RAG one are good picks) at `/interview-questions/[id]`: confirm the tag badges appear in the header badge row, and a new "Same pattern, asked elsewhere" section appears above the answer workspace listing the other same-tag questions as company-badge + title links that navigate correctly; open a question whose tags are otherwise unique (e.g. `amazon-ab-experimentation-platform`, tag `experimentation`) and confirm that section is simply absent (not an empty heading). No layout overflow anywhere, light and dark.

- **Interview Questions — Topic filter collapsed by default + search now matches topic labels** (`src/app/interview-questions/page.tsx`) — follow-up to the tag-filter entry above: the 32-button Topic row read as overwhelming always-open, so it's now a collapsed disclosure (chevron toggle, closed by default) showing just active-tag chips (with a "×" affordance to remove) when collapsed and the full button grid when expanded; the header shows a small primary-Badge count of active topic filters even while collapsed. Search's text matching was also extended to match topic labels (e.g. typing "idempotency" or "fanout" now surfaces matching questions without ever opening the Topic panel). Needs a check on `/interview-questions`, both themes: Topic section starts collapsed on page load with a right-pointing chevron; clicking it rotates the chevron and reveals all tag buttons; selecting 2-3 tags, then collapsing, shows those exact tags as small filled chips (with ×) in place of the full grid, and clicking a chip's × removes just that filter and updates the list live; typing "idempotency" into Search with the Topic panel collapsed and no tags selected narrows the list to `payments-idempotency`-tagged questions; the active-topic-count badge next to the "Topic" label appears only when ≥1 tag is selected and shows the right number.
- **New "Reference solution" viewer on `/lld/editor` challenges — CONFIRMED all 7 slugs** (`src/lld-modeling/referenceSolutions.ts`, `src/components/lld/ReferenceSolutionModal.tsx`, wired from `ChallengeBriefing.tsx`'s "Reference solution →" action next to "Re-read the lesson →"): on each of `parking-lot`/`elevator-system`/`tic-tac-toe`/`lru-cache`/`splitwise`/`movie-ticket-booking`/`rate-limiter`, the action opens a modal titled "Reference solution — <Title>" with a "Read-only" badge, a one-line design note, and a boxed diagram whose class boxes and edges (filled-diamond composition, hollow-triangle inheritance/realization, association labels/multiplicities) all render correctly and legibly; dragging a box pans the canvas instead of moving it (confirmed on Rate Limiter) and Escape closes the modal. Caught and fixed a real bug in the process: the layered auto-layout left pure-root nodes (Car/Bike/Truck, an interface's implementers) stranded at column 0 while their one target sat several columns right (pulled there by an unrelated edge), so the connecting edge ran a long diagonal grazing past unrelated boxes and, on Parking Lot, exactly overlapped/hid behind the ParkingFloor/ParkingSpot boxes — fixed with an ALAP backward-compaction pass in `computeLayeredPositions` that pulls a childless-inbound node up to just before its nearest target; re-verified visually on Parking Lot and Elevator System post-fix, and the other 5 were checked already-fixed. Not yet checked: light theme and behavior at ~1024px.
- **New "Paper" (light) theme + landing page widened to full-bleed layout with soft cards and scroll-reveal motion** (`src/app/globals.css`'s `:root[data-theme="light"]` block + `--landing-*` tokens, `src/components/theme/ThemeProvider.tsx`/`ThemeToggle.tsx` — Theme is now `"dark" | "light" | "night-ops"`, toggle cycles dark → Paper (Sun icon) → Night Ops → dark, `src/app/layout.tsx`'s blocking init script updated to also recognize a saved `"light"` preference, `src/app/page.tsx`, new `src/components/landing/Reveal.tsx`): the landing page's header/sections widened from `max-w-4xl`/`5xl` to `6xl`/`7xl`, the hero got a full-bleed decorative radial-gradient glow (visible only under Paper — `--landing-glow` is `none` under dark/night-ops) and a bigger heading (`text-5xl`→`text-7xl` vs. the old `4xl`/`5xl`), and the diagram/process/scenario cards pick up rounded corners + real shadows only under Paper via `--landing-radius`/`--landing-shadow-card` tokens that resolve to `0px`/`none` everywhere else. Hero content and each below-the-fold block are wrapped in `<Reveal>`, which fades+rises them in via IntersectionObserver — but the CSS that makes that visible (`.landing-reveal` in globals.css) only exists under `data-theme="light"`, so dark/night-ops should render exactly as before (instant, no motion). Needs a check on `/`: cycling the header ThemeToggle through all three themes shows the right icon each time (Sun while in dark, bat mark while in Paper, moon while in night-ops) and Paper actually looks meaningfully different (warm paper background, wider layout, glow behind the hero, rounded/shadowed diagram + process strip + scenario cards, sections fading+rising in on load/scroll); reloading on a saved Paper preference shows no flash of dark before Paper applies; switching back to dark from Paper shows the original flat/hairline look with zero motion (nothing fades in); at ~1024px nothing overflows horizontally in either theme; reduced-motion (OS setting) shows Paper's sections visible immediately with no animation.

- **`/foundations` composition redesign — readout strip, Continue/Start Here band, five tracks, rail, completed-state styling** (`src/components/foundations/FoundationsJourney.tsx` replacing the deleted `FoundationsList.tsx`, `src/content/foundations/tracks.ts`, `.bg-blueprint-grid` in `src/app/globals.css`, wired from `FoundationsIndexView.tsx`) — verified headlessly (desktop + mobile widths, top through the last track) with zero progress; the one state NOT yet visually checked is with lessons actually marked complete. Needs a check (mark a few lessons complete via each lesson page's "Mark complete," or seed `localStorage['engineering-studio:foundations-progress']` with some slugs, then reload `/foundations`) that: the Continue band switches from "Start Here" to "Continue learning" pointing at the earliest incomplete lesson; completed `LessonCard`s show the green `Done` badge, a faint status-healthy background tint, and status-healthy corner brackets (not the default border/signal-on-hover ones); a track with every lesson complete shows a green checkmark in its rail marker instead of its track number; the readout strip's completed count/percent updates correctly; and once all 24 are complete, the Continue band swaps to the "All 24 lessons complete" state. Also worth a glance: hover states on the Continue band and lesson cards (lift/background-hover transitions), and that the rail/marker alignment still looks right at a narrower desktop width (not just 1600px and 390px, which is all that got checked).

`/foundations` — Journey/Atlas toggle, the phase stepper, "you are here", per-lesson glyphs, current/done card states, and the per-track light-theme colors have all been checked directly in-browser (dark, light, and the dark/night-ops-unaffected spot-check) and confirmed working — including the `isolate` stacking-context fix that made both themes' fixed backdrops actually visible (they were fully occluded by `<main>`'s own background before; see `FoundationsIndexView.tsx`'s doc comment on that class). Still not checked: both views at a narrow (~390px) width — the phase stepper's wrapping and the per-lesson tick/spine alignment specifically.

Noticed but not investigated: switching Journey↔Atlas causes a brief (~1-3s in dev) render hitch — plausibly the several large `blur-[90-115px]` backdrop blobs plus a big DOM swap happening at once in unminified Turbopack dev mode. Recovers on its own; unconfirmed whether it's noticeable in a production build. Worth a look if it turns out to bother anyone in practice.

Foundations cards (Atlas + Journey, light theme) got a "craft" pass in response to direct feedback that the previous flat-tinted cards read as amateurish next to real reference UIs — checked directly in-browser and confirmed: Atlas's upcoming-lesson cards now show a bigger `--landing-radius-lg` corner radius, a solid colored icon panel (not a faint tint) behind each glyph, and a soft colored drop shadow at rest that deepens on hover (`accent.restShadow`/`hoverShadow` in `trackAccent.ts`); titles are bolder (`font-semibold`). Journey's cards keep their square corner-bracket reticle marks (unchanged) but picked up the same colored rest/hover shadow. Still worth a glance next pass: the shadow intensity at a few different zoom levels/screen brightnesses, since colored shadows can read as muddy on some monitors — flag if so.

Pulled the blurred multi-color-blob backdrop back out of both Foundations views entirely (Atlas's `AtlasBackdrop` light branch, Journey's now-deleted `JourneyBackdrop`) — direct feedback called it an ugly "spray" (overlapping blurs desaturating toward muddy brown). Saturation now lives in sharp-edged, solid zone `wash` panels instead (bumped 7%→22% opacity) plus much stronger `iconPanel` blocks (28%→40%). Checked directly in-browser: no more blur/mud anywhere, each of the 5 tracks reads as a confident, distinct solid color block, hero/readout margins are back to plain white (no backdrop layer at all now) but that's a small fraction of the page next to the now much-bolder zone panels. Worth a last look: whether the plain-white hero/stepper area feels like a regression now that the zones below it are so much more saturated by contrast.
`SystemMeshBackground` (`/learn`, `/foundations`, reading-room index pages): added `Packet`, a small glowing dot that travels the existing highlighted accent path (node 2→18→15→17→8) and loops, per direct feedback asking for motion on this background's line/dots. Could not get a live rAF-driven confirmation in this session's automated browser tab (it reported `document.hidden: true` with 0 animation frames delivered even after click/focus — a background-tab throttling artifact of the automation tool itself, not the app). Check live in a normal foreground tab: the dot should travel smoothly along the diagonal accent line (visible near the top of `/foundations`' hero, faint orange line with a few nodes), fading in leaving one waypoint and fading out arriving at the next, looping continuously with a ~1s pause between laps. Also confirm `prefers-reduced-motion` hides it entirely (no dot, just the existing static line/nodes).

Lesson/course body text (Foundations, LLD, Agentic, Case Studies reading rooms) bumped from `text-sm` (14px) to `text-base` (16px) — direct feedback that course reading content felt too small compared to a reference site (fontawesome docs). First pass also swapped the color from `text-text-muted` (grey) to `text-text` (near-white/near-black) but direct feedback said that looked worse, so the color was rolled back to `text-text-muted` — this entry now covers a size-only change. Changed in the shared `LessonBlockRenderer` (paragraph, ordered/unordered list items, insight quote, Q&A block, table body cells) plus each course's own `page.tsx` (Summary paragraph, key-takeaways list, exercise prompt) — all four course modules share this exact markup pattern. Headings, captions, mono labels, and card/index-page taglines were left untouched. Worth a look: open a lesson in each of the 4 modules in both light and dark theme, confirm body text reads more comfortably at 16px and nothing wraps awkwardly in the `max-w-5xl` article column or inside `bg-bg-panel` boxes (summary/exercise/takeaways panels).

`/learn` hero (light theme) — reworked twice per direct feedback, ending with no button at all under the copy. First pass: removed the redundant "Open the Workshop" button (the header already has "Enter Workshop"); the five room icons (`RoomPath`, previously an unlabeled row below the buttons) moved up into that button row's old spot with titles added underneath (mirroring `PhaseStepper`'s marker+label shape), and the one remaining CTA (`Continue: <lesson>` / `Start with <room>`) moved down into the old icon row's spot. Second pass: that lone CTA was flagged back as reading like an odd unpaired button once it lost its sibling — removed entirely rather than paired with anything, so the hero now ends at `RoomPath` (badge → title → copy → labeled icon row, no button). `foundationLessons`/`useFoundationsProgress` plumbing that only existed to drive that CTA was removed from both `LearnHubView` and `page.tsx` along with it. Worth a look: confirm the icon row doesn't wrap awkwardly at narrow widths (five labeled icons is wider than five bare dots — "Low-Level Design" is the longest title), the hairline connectors still line up with the (vertically taller, icon+label) circles, and the hero doesn't feel like it's missing a call to action now that it ends on the icon row (each room card below still has its own "Explore" chip, and the header keeps "Enter Workshop").

`ReadingRoomJourney` (LLD/Agentic/Case Studies index pages, light theme) — replaced the old plain-text `ReadoutStrip` + left-accent-border `ContinueBand` with `FoundationsJourney`'s newer dashboard row (`ContinueCard` + `ProgressCard` side by side, plus a `MilestoneStrip` group-summary strip underneath), so these three reading rooms match the "Continue Learning" card treatment Foundations already had — direct feedback specifically asked for this ("I love this part in the foundation page... it is not there in the other modules"). Same components Foundations uses, generalized over `ReadingRoomGroup`/`itemLabel` (e.g. "case study/case studies", "lesson/lessons") instead of hardcoded lesson language. Container width also widened from `max-w-5xl` to `max-w-7xl` to match Foundations' own dashboard-row width. Worth a look: open `/lld`, `/agentic`, and `/case-studies` in light theme, confirm the new dashboard row renders correctly at 0% and partway-through completion states (the "all complete" branch too, if easy to fake via localStorage), the milestone strip's group labels don't get cut off, and dark/night-ops theme on the same three pages is unaffected (this dashboard row's `workspace-*` tokens should no-op there, same as Foundations).

Light-theme page background fixed on `/lld`, `/agentic`, `/case-studies`, and `/entities` — direct feedback that these pages read "too blue" next to Foundations' whiter ground. All four were using `.bg-reading-room` (resolves to `--color-bg-panel`, `#eef4ff`, noticeably bluer than plain `--color-bg`'s `#f4f7fc`) for a "cards need contrast" reason Foundations itself moved past once its cards got a real border+shadow — Foundations' own light-theme journey view already uses plain `bg-workspace-bg` instead, and LLD/Agentic/Case Studies additionally had a `bg-blueprint-grid` dot-texture painted right on `<main>` in their light journey view (backwards from Foundations, which only ever paints that texture for the dark atlas view) making them read even busier on top of the bluer tint. All four now use `bg-workspace-bg` under Paper, matching Foundations exactly; dark/night-ops theme is unaffected (still `bg-reading-room`/`bg-bg`, unchanged). Worth a look: open all five reading-room index pages (`/foundations`, `/lld`, `/agentic`, `/case-studies`, `/entities`) side by side in light theme and confirm the page ground now reads as the same whitish tone across all of them, with no dot-grid texture visible on the light journey/list views; then flip to dark theme and confirm nothing changed there.

`/entities` progress card (light theme) — new "X/Y components viewed" card in the hero, mirroring `ReadingRoomJourney`'s `ProgressCard` visually but with no paired "Continue" card (no natural next entity to recommend). Viewed state is new: `entitiesProgress.ts` (localStorage, same pattern as `foundationsProgress.ts`) marked via a new invisible `EntityViewTracker` client component mounted on `/entities/[slug]`'s page on every visit — this is the first tracking of any kind `/entities` has ever had. Worth a look: visit a few entity detail pages, then back on `/entities` confirm the progress card's count and percent bar update to match (also check it persists across a reload, and that dark/night-ops theme doesn't render the card at all).

Login/register/report-bug forms redesigned to a modern glass-card style, a deliberate scoped exception to Trace's flat/zero-radius/no-shadow rule (design.md) — user explicitly asked to break from the "instrument" aesthetic on just these three surfaces, chose the "full redesign" option over a strictly-flat polish pass. `/login` and `/register` are now a two-column split screen at `lg`+ (glass card with gradient hairline border + backdrop blur on the left, a new `AuthShowcasePanel` branded feature panel on the right; collapses to just the centered card below `lg`), each with an icon badge, rounded/backdrop-blurred inputs with leading icons (`Mail`/`Lock`/`User`) and a built-in password show/hide toggle, and a full-width rounded "pill" primary button. `ReportBugButton`'s trigger chip is now a floating rounded pill (was a flat rectangle) and its modal uses `Modal`'s new `variant="modern"` (rounded corners, gradient hairline border, icon badge next to the title) with the matching modern `Textarea` and a success state that fades in a checkmark icon instead of swapping to plain text. `Button`/`Input`/`Textarea`/`Modal` all gained additive, default-off props (`shape="pill"` / `variant="modern"`) — every other call site (dashboards, panels, CompareModal, FigureFrame, LockInEntryPrompt, the reading-room `Card` grid, etc.) is unaffected and should still render exactly as before. Worth a look: open `/login` and `/register` at a wide (`lg`+) viewport in dark theme (the default) and confirm the split-screen layout, glow, glass card, icon-adorned inputs, and password toggle all render correctly with no console errors; resize below `lg` and confirm the right-hand showcase panel disappears cleanly and the card stays centered; check light theme too; then sign in, click "Report bug" (bottom-left), and confirm the modal's icon badge, rounded card, and submit → success-checkmark transition all look right; finally spot-check a couple of unrelated pages/dialogs (e.g. a Workshop panel, CompareModal) to confirm the flat/zero-radius instrument look elsewhere in the app is untouched.

Landing page (light theme): two fixes from an external UI/UX review. (1) `--color-text-muted` (light theme) darkened `#667085` → `#5b6577` — the old value measured 4.19:1 against `--color-bg`, below WCAG AA's 4.5:1 floor for the token's smallest use (`PrimaryNav`'s 12px tabs), and it got worse when `--color-bg` moved to the more saturated `#e6ecf7` earlier in this diff; the new value clears AA against `--color-bg`, `--color-bg-elevated`, and `--color-bg-panel` (4.96/5.88/5.32:1). Global token, so every light-theme surface using muted text gets the fix, not just the landing page. (2) Removed `HomeView`'s header-right "Enter Workshop" `LinkButton` — it sat directly beside `PrimaryNav`'s own "Workshop" tab (same destination, same header row) and duplicated the hero's primary CTA's exact label at a weaker visual weight; `PrimaryNav`'s tab plus the hero's filled CTA already cover it, matching `AppHeader`'s own guidance that per-page `right` content is for the cases `PrimaryNav` doesn't cover. Reviewed and deliberately left alone: the review's radius complaint (buttons flat/0px vs. landing cards' 20px) and the five-hue `trackAccent` category colors are both intentional, documented decisions (Trace's "no exceptions" flat CTA language; Paper's feedback-driven gapped-card treatment; the shared cross-page track palette) — not bugs. Worth a look: reload `/` in light theme and confirm nav labels, footer text, and scenario-card body copy all read a shade darker/clearer than before with no other color shift; confirm the header's right side now shows only the GitHub icon (no second "Enter Workshop" button) at both mobile and desktop widths, and that the hero's own "Enter Workshop" CTA and `PrimaryNav`'s "Workshop" tab still both work; check dark/night-ops theme is untouched (token only exists under `data-theme="light"`).

Follow-up to the login/register/report-bug glass redesign above: fixed two real bugs from that pass, found via user feedback rather than a browser check. (1) The leading `Mail`/`Lock`/`User` icons in the modern `Input` variant were invisible — the `<input>` element is a later DOM sibling than the icon `<span>` and painted its own semi-transparent, backdrop-blurred background directly over it; both the icon and the password show/hide button now get `z-10` so they always paint above the field regardless of DOM order. (2) The split-screen layout's right-hand `AuthShowcasePanel` used to be a second, separately-styled column (its own `border-l` + solid `bg-bg-panel` fill), which read as a hard seam down the middle of the screen once removed it also lost its "glass" look entirely. Restructured `/login` and `/register` so the form and the showcase panel are now two grid cells *inside one shared glass card* (single gradient hairline border, single `bg-bg-elevated/90` + `backdrop-blur-xl` fill spanning the full ~`max-w-5xl` width) instead of two boxes side by side — same background on both halves by construction, so there's nothing left to seam. Worth a look: open `/login` and `/register` at `lg`+ width in dark theme and confirm (a) the Mail/Lock/User icons are clearly visible inside each field and the password eye-toggle still works, (b) the card now reads as one continuous glass surface — no vertical line, no visible boundary — with the showcase panel's glow blobs blending smoothly across the whole width, (c) resizing below `lg` collapses to just the form half of that same card with no leftover asymmetry, and (d) light theme still looks right.

Reverted the two follow-up layout experiments above (the seamless borderless merge, then the single-shared-glass-card version) — direct feedback that the original split-screen ("the initial") looked better than either. `/login`, `/register`, and `AuthShowcasePanel` are now back to that first version: two separate grid columns, the showcase panel with its own `border-l border-border/60` + `bg-bg-panel/40` fill + `overflow-hidden`. The one thing kept from the follow-up work is the real bug fix, not a style change: the modern `Input`'s leading icon and password toggle both have `z-10` so they render above the field's own translucent/blurred background instead of invisibly underneath it. Worth a look: open `/login`/`/register` and confirm the layout matches the original two-panel split (visible seam included — that's the now-accepted look) and that the Mail/Lock/User icons and password toggle are visible and working.

Landing page heading order fixed across all three themes, per an external UI/UX review's follow-up (dark + night-ops, colors explicitly off-limits this round). `Kicker` (used by "How it works" and "Pick a problem to solve") was a plain `div`, so the page's real heading order jumped `h1` straight to `h3` (`Build`/`Observe`/`Iterate`, each scenario card's title) with no `h2` anywhere — a screen reader's heading-level navigation depends on that nesting, and it was broken identically in Trace, Paper, and Night Ops since none of this is theme-conditional. Now an `h2`, same className, zero visual change. The redundant header "Enter Workshop" button fixed for Paper last round was already shared markup, so dark/night-ops get it for free — verified directly (one `ENTER WORKSHOP` link on the page, not two) rather than assumed. Everything else from the original review (button/card radius, the five-hue track-accent palette) was already confirmed intentional and left alone; nothing new turned up in dark/night-ops beyond the heading gap. Worth a look: on `/`, switch through all three themes via the header toggle (dark → Paper → Night Ops, including the bat-signal transition into Night Ops) and confirm nothing shifted visually — the "How it works" and "Pick a problem to solve" labels should look exactly as before, just with a screen reader / accessibility-tree inspector now showing them as `h2`.
